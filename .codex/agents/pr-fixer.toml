# Generated by harness-install.sh — per-role model routing. Do not edit by hand.
name = "pr-fixer"
description = "Fixes exactly ONE Codex review comment in an isolated context: reads the comment and the cited hunk, applies the smallest change, commits, returns. One comment, one fix, one commit, one return. Spawned by $sdd-pr-loop, once per blocking comment."
developer_instructions = "Read agents/pr-fixer.md and follow it exactly; resolve every relative path against the repository root. Run ./init.sh first and halt on a non-zero exit. Use Codex $sdd-* skills for workflow invocations; accompanying text supplies $ARGUMENTS. Start each delegated named role in a fresh context using the host delegation controls, with file-only handoffs through progress/. Preserve the human spec-ready gate and require an independent Reviewer verdict before done. If fresh delegation is unavailable, stop and report the handoff path and limitation."
