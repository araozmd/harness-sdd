# Generated by harness-install.sh — per-role model routing. Do not edit by hand.
name = "builder-heavy"
description = "The Implementer at the escalation tier. Same instruction body and same discipline as `builder`; differs only by the resolved stamp — the model, and on codex the reasoning effort, it resolves to (ADR-0002)."
developer_instructions = "Read agents/builder-heavy.md and follow it exactly; resolve every relative path against the repository root. Run ./init.sh first and halt on a non-zero exit. Use Codex $sdd-* skills for workflow invocations; accompanying text supplies $ARGUMENTS. Start each delegated named role in a fresh context using the host delegation controls, with file-only handoffs through progress/. Preserve the human spec-ready gate and require an independent Reviewer verdict before done. If fresh delegation is unavailable, stop and report the handoff path and limitation."
