# Ablation run — 2026-09-05 (docs/ABLATION.md protocol, first execution)

Two same-class `sdd:false` fixes on one branch, real Builder sub-agents (both sonnet),
one variable: `agents/builder.md` stock (1,499 words) vs skeleton (121 words — the
ABLATION.md skeleton: task + guardrails + exit criteria + the two shift-left checks).

| run | feature | builder.md | wall-clock | tokens | tool uses | suite | quality notes |
|---|---|---|---|---|---|---|---|
| A (baseline) | E99-F75+F76 (audit, ~60 call sites) | stock, 1,499 w | 26.1 min | 183k | 97 | 42/42 | exhaustive audit w/ dispositions; 5 verified mutations; caught + fixed an unrelated env defect and flagged it upward |
| B (skeleton) | E99-F88 (one contract + one pin) | skeleton, 121 w | 12.3 min | 71k | 26 | 37/42* | fence-aware section-scoped pin; 5 follow-up mutants WITH printed diffs (self-applying its new contract); correctly attributed *5 pre-existing failures to the experiment's own skeleton file in the tree, proven by reverting its files |

\* the 5 failures were caused by the skeleton builder.md itself sitting in the working
tree (suites assert builder.md content); all pass with stock restored.

## Reading (honest limits)

- NOT a controlled size comparison — F75 is a much larger task than F88, so wall/token
  deltas mostly measure the tasks, not the prompts.
- The signal is QUALITATIVE and it is strong: the skeleton run lost NONE of the
  discipline the 1,378 deleted words prescribe. It mutation-verified, section-scoped,
  fence-aware'd, namespaced scratch, refused to touch foreign work, and diagnosed an
  environmental red herring correctly — behaviors the stock prose spells out at length
  and the skeleton only implies via "proven by a test that fails without the change"
  plus `progress/lessons.md`.
- One behavior clearly attributable to prompt content rather than the model: both runs
  read `progress/lessons.md` and applied its conventions — the LEDGER carried the
  house idioms, not the role prose.

## Next step per the protocol

One anecdote is not a restore/delete decision (ABLATION.md: "a failure that happens
once is an anecdote; twice is a line to restore"). Run the skeleton on 2-3 more
builder lanes of MIXED size before dieting agents/builder.md on main. The candidates
whose absence did NOT hurt here: the Loop A/Loop B narration, the sdd:false routing
explanation (the brief + board state carried it), and the long mutation-preconditions
prose (lessons.md carried it).
