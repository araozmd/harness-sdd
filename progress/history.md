# History

The durable changelog of what the agents did, across all runs. The Orchestrator and
Reviewer append one line per meaningful step. Newest at the bottom.

Format: `YYYY-MM-DD | <agent> | <feature-id> | <what happened>`

---

2026-05-28 | harness | — | harness-sdd scaffolded (local + obsidian stores, jira stubbed)
2026-05-30 | architect+builder | E00-F01 | Installer spec (4 files) + harness-install.sh + tests/test_install.sh co-authored; init.sh made self-locating; VERSION added. Tests green. Set in-review for the Reviewer dogfood.
2026-05-30 | reviewer | E00-F01 | Verified via PR #3 (merged 876ca48): R1–R11 traced to passing assertions in tests/test_install.sh (green under sh and dash); Codex review cycle closed (1 stale P1 + 4 P2 addressed across f4e2dca/e85192a/8711412). → done. Epic E00 → done.
- 2026-05-30 — E03 created (Multi-repo coordination). Architect wrote 4-file spec for E03-F01 (Umbrella coordinator) → spec-ready. PAUSED at human gate.
- 2026-05-30 — E03-F01 approved by human → in-progress; dispatching Builder. Registered E03-F02 (Cascade installer, pending, depends_on E03-F01) from install-cascade discussion.
- 2026-05-30 — E03-F01 Reviewer APPROVED (R1–R19 covered, tests green, pure-superset + no-fork verified) → done. Wired test_umbrella.sh into verification.test_command.
- 2026-05-31 — E03-F01 merged to main (PR #6, 4 Codex rounds), tagged v0.2.0. Starting E03-F02 (Cascade installer) on feat/cascade-installer.
- 2026-05-31 — E03-F02 Reviewer APPROVED (R1–R24, all suites green, non-regression + migration smoke-tested) → done. Epic E03 done. VERSION → 0.3.0.
- 2026-06-01 E04-F01 — Architect wrote 4-file spec (R1–R16) from intent brief → status spec-ready ⏸ human gate.
- 2026-06-01 E04-F01 — Builder implemented (T1–T14), Reviewer APPROVED (R1–R16, all suites green), nit fixed → done. Epic E04 done. VERSION → 0.4.0.
- 2026-06-06 E05 seeded (Inception): epic "Harness observability & review quality" + E05-F01 (reviewer cross-file, autonomous) + E05-F02 (telemetry, parks at gate).
- 2026-06-06 E05-F01 — Architect spec (R1–R13) → Builder (T1–T9, VERSION 0.6.0, new tests/test_reviewer.sh) → Reviewer APPROVED round 1 (all 5 suites green, cross-file check self-applied) → done.
- 2026-06-06 E05-F02 — Architect spec (R1–R21) + session-summary amendment (R22–R27 portable end-of-session summary) → spec-ready ⏸ human gate.
- 2026-06-06 E05-F02 — Reviewer APPROVED round 1: all 6 suites green; R1–R27 each traced to a passing test; report exercised against independent fixtures (durations/rounds/mean+median latency correct, session scoping excludes pre-marker records, best-effort no-op on absent/empty/unwritable, no token/USD surfaced); both gitignore layouts proven (source git check-ignore + end-to-end installer seed, seed-once); DO NOT TOUCH honored (schema/init.sh untouched, status enum intact); VERSION 0.7.0 (MINOR ✨) + CHANGELOG per policy → done.
- 2026-06-06 E05-F02 — gate-approved (storage gitignored-under-.harness, AGENTS.md pointer); Architect reconciled spec → Builder (T1–T20, VERSION 0.7.0, tools/telemetry-report.py + tests/test_telemetry.sh) → Reviewer APPROVED (R1–R27, report exercised across all granularities + session view, gitignore proven in both layouts) → done. Epic E05 done.
- 2026-06-10 E06-F01 — Architect wrote 4-file spec (R1–R14, D1–D3 decisions) from intent brief → status spec-ready ⏸ human gate.
- 2026-06-10 E06-F01 — gate-approved by human (D1–D3 accepted) → in-progress; dispatching Builder.
- 2026-06-10 E06-F01 — Builder round 1 (T1–T12, VERSION 0.14.0, new tests/test_epic_lifecycle.sh, all 8 suites green; flagged test_inception.sh diff-vs-main removal) → in-review.
- 2026-06-10 E06-F01 — Reviewer APPROVED round 1: init.sh + all 8 suites green (run independently); R1–R14 each traced to a passing check; zero-dep fallback path exercised with jsonschema shadowed (warn-only invariant + rejection both proven); cross-file story coherent (draft → planned → in-progress → done, pending = legacy alias); feature/slice enums byte-identical; test_inception.sh diff-vs-main removal ACCEPTED (documented permanent-suite anti-pattern, surgical); VERSION 0.14.0 (one MINOR ✨) + CHANGELOG per policy → recommend done.
- 2026-06-10 E06-F01 — done (Reviewer verdict persisted). Epic E06 → in-progress (F02–F06 roadmap deliberately unseeded, rolling-wave).
- 2026-06-12T04:43:33Z  E07-F01  pending → spec-ready (Architect: 4-file spec, 12 R-ids) — PAUSED at human gate
- 2026-06-12T05:01:33Z  E08-F01  pending → spec-ready (Architect: 4-file spec, 15 R-ids) — PAUSED at human gate
- 2026-06-12T05:12:17Z  E08-F01  spec-ready → in-progress (human approved, gate latency 644s) — spawning Builder (round 1)
- 2026-06-12T05:22:43Z  E08-F01  in-progress → in-review (Builder: T1–T13, full suite green, round 1)
- 2026-06-12T05:27:52Z  E08-F01  in-review → approve (round 1) — Reviewer verified suite + behavior
- 2026-06-12T05:27:52Z  E08-F01  in-review → done; E08 epic rolled up → done
- 2026-06-12T05:27:52Z  drift-check (trigger: E08 done): NO-OP — no specs/architecture.md / specs/adr to re-validate against (graceful degradation); no epic demoted
- 2026-06-12T14:18:39Z  E07-F01  spec-ready → in-progress (human approved spec, 4 open questions accepted as-specced, gate latency 34506s) — spawning Builder (round 1)
- 2026-06-12T14:27:14Z  E07-F01  in-progress → in-review (Builder round 1: T1–T11, §5c Antigravity glue, VERSION→0.22.0, deselect→scoped removal, all 13 suites green)
- 2026-06-12T14:31:21Z  E07-F01  in-review → approve (round 1) — Reviewer verified all 13 suites green, R1–R12 traced, both deviations (VERSION→0.22.0, deselect→scoped removal) verified sound + data-loss safe
- 2026-06-12T14:31:21Z  E07-F01  in-review → done (Reviewer verdict persisted); E07 epic rolled up → done
- 2026-06-12T14:31:21Z  drift-check (trigger: E07 done): NO-OP — no remaining draft/planned/pending epics (all epics done); no Scout spawned, no demotion
- 2026-06-12T15:39:15Z  E07-F01  in-review/round-3 → HELD (Codex r3 P2 #3404422700: .agent/ vs .agents/ + persona registration). PR #31 NOT merged, labeled needs-human.
- 2026-06-12T15:39:15Z  E07-F01  done → pending; E07 epic done → pending (premature rollup reverted — PR unmerged + re-spec). Human chose: re-spec via Architect.
- 2026-06-12T15:44:29Z  E07-F01  pending → spec-ready (Architect re-spec: R1–R13, .agents/ plural + confirmed-primitives persona model) — PAUSED at human gate
- 2026-06-12T16:36:52Z  E07-F01  spec-ready → in-progress (re-spec approved; .agents/ plural, primitives model, skills out-of-scope; gate latency 3143s) — Builder round 2 on PR #31 branch
- 2026-06-12T16:44:24Z  E07-F01  in-progress → in-review (Builder round 2: .agent/→.agents/ rename, strengthened tests, VERSION held 0.22.0, all 13 suites green; commit d78e40f)
- 2026-06-12T16:49:38Z  E07-F01  in-review → approve (round 2, re-spec) — Reviewer verified rename complete, deselect-safety preserved, persona-model honest; holding done until PR #31 merges
- 2026-06-12T16:56:38Z  E07-F01  in-review → done (PR #31 MERGED 2be3817; Codex r4 P2 deferred as known cross-front-end limitation). E07 epic rolled up → done.
- 2026-06-12T16:56:38Z  drift-check (trigger: E07 done): NO-OP — no remaining draft/planned/pending epics (ALL epics now done); no Scout spawned, no demotion.
2026-07-10T18:50:23Z | architect | E09-F01 | pending → spec-ready (Architect: 4-file spec, 18 R-ids) — PAUSED at human gate
2026-07-10T18:53:54Z | orchestrator | E09-F01 | spec-ready → in-progress (human approved, gate latency 211s) — spawning Builder (round 1)
2026-07-10T19:06:27Z | builder | E09-F01 | in-progress → in-review (Builder: T1–T12, VERSION 0.26.0, all suites green)
2026-07-10T19:11:57Z | reviewer | E09-F01 | in-review → reject (round 1) — feedback → progress/review-E09-F01.md; returning to Builder
2026-07-10T19:17:55Z | builder | E09-F01 | in-progress → in-review (Builder round 2: planner.md contradiction reconciled, all suites green)
- 2026-07-10T19:25:00Z | reviewer | E09-F01 | in-review → approve (round 2) — round-1 contradiction in `agents/planner.md` resolved; every R1–R18 traced to a passing test; full `verification.test_command` green; fresh-install check passes; `agents/doc-critic.md` installed and referenced. → done recommended.
2026-07-10T19:22:18Z | reviewer | E09-F01 | in-review → approve (round 2) — all suites green, R1–R18 traced
2026-07-10T19:22:38Z | orchestrator | E09-F01 | in-review → done (Reviewer verdict persisted); E09 epic not rolled up — E09-F02 still pending
- 2026-07-10T19:28:34Z E09-F02 pending -> spec-ready: Architect wrote feature specs; paused at human approval gate.
- 2026-07-10T19:30:49Z E09-F02 spec-ready -> in-progress: human approved specs; Builder started.
- 2026-07-10T19:37:26Z E09-F02 in-progress -> in-review: Builder completed implementation and verification; Reviewer started.
- 2026-07-10T19:41:15Z E09-F02 in-review -> reject (round 1): Reviewer found missing named test traceability; returned to Builder.
- 2026-07-10T19:44:41Z E09-F02 in-progress -> in-review: Builder addressed review feedback (round 2); Reviewer restarted.
- 2026-07-10T19:48:02Z E09-F02 in-review -> approve (round 2): Reviewer approved; feature marked done.
- 2026-07-10T19:48:02Z E09 done rollup: all E09 features are done; drift check triggered.
- 2026-07-10T19:49:21Z E09 drift check: no stale epics; no demotions (no architecture/ADR context to re-validate).
- 2026-07-10T21:44:39Z E10-F01 pending -> spec-ready: Architect authored 4-file spec (15 R-ids, full R-id↔task↔test traceability, ADRs touched: none). PAUSED at human spec-approval gate (autonomous:false, require_spec_approval:true).
- 2026-07-10T22:24:36Z E10-F01 spec-ready -> in-progress: human approved spec at gate; Builder spawned with approved specs.
- 2026-07-10T22:33:34Z E10-F01 in-progress: Builder implemented ownership primitive (owner field + scoped /sdd-next --mine) per approved spec; T1-T13 done; ./init.sh + full test_command (incl. new test_ownership.sh) green; VERSION 0.27.3 -> 0.28.0. Handed back to Orchestrator for in-review.
- 2026-07-10T22:34:15Z E10-F01 in-progress -> in-review: Builder implemented 13 tasks (schema owner field, orchestrator contract, /sdd-next --mine glue, installer wiring, docs, tests/test_ownership.sh), VERSION 0.27.3->0.28.0; full suite green. Reviewer spawned.
- 2026-07-10T22:39:51Z E10-F01 in-review -> reject (round 1): Reviewer found R15 VERSION test uses git HEAD:VERSION OLD-vs-NEW diff (permanent-suite anti-pattern; would block next feature post-merge). Back to Builder for fix.
- 2026-07-10T22:43:44Z E10-F01 in-progress -> in-review: Builder fixed R15 to shape-only SemVer+CHANGELOG assertion (round 2); proven green post-commit; full suite EXIT=0. Reviewer restarted.
- 2026-07-10T22:46:59Z E10-F01 in-review -> approve (round 2): Reviewer verified R15 shape-only fix (proven green post-commit) + full-suite regression; local gate green. Proceeding to PR + Codex pr-loop before done (per do-not-roll-done-before-merge).
- 2026-07-10T22:48:05Z E10-F01 PR #40 opened (feat/E10-F01-owner-field, VERSION 0.28.0); entering Codex /pr-loop. Feature parked at in-review until merge (do-not-roll-done-before-merge).
- 2026-07-10T22:58:26Z E10-F01 PR #40 round 1: Codex filed 1 finding (P2 — zero-dep init.sh validator did not enforce owner string; R3 test skipped w/o jsonschema). Fixed via pr-fixer (init.sh fallback + R3 both-paths test); full suite green.
- 2026-07-10T23:08:31Z E10-F01 PR #40 round 2: Codex filed 1 new finding (P2 — migrate_config did not seed workflow.identity for upgraded installs; /sdd-next --mine fail-closed on upgrade). Fixed via pr-fixer (idempotent migrate_config seed + upgrade test). Round-1 P2 not re-raised (fix accepted).
- 2026-07-10T23:16:13Z E10-F01 PR #40 round 3: Codex filed 1 finding (P2 — identity migration presence-check matched any indented identity: file-wide, could suppress seeding workflow.identity). Fixed directly: added _cfg_has_workflow_identity awk helper scoped to the workflow: block (mirrors _cfg_has_umbrella_manifest) + scoping regression test. Full suite green.
- 2026-07-10T23:25:52Z E10-F01 PR #40 round 4: Codex clean ("Didn't find any major issues" on e249778); all gates green, CI pass. Auto-merged (merge commit 6c164e3).
- 2026-07-10T23:25:52Z E10-F01 in-review -> done: PR #40 merged; feature complete.
- 2026-07-10T23:25:52Z E10 done rollup: all E10 features done; epic E10 -> done (derived + persisted); drift check triggered.
- 2026-07-10T23:25:52Z E10 drift check (Scout, read-only): E11/E12/E13/E14/E99 all STILL-VALID; no demotions (E10-F01 purely additive; board epics already defer ownership/atomic-claims to E10-F03, one-way mirror preserved). Findings: progress/drift-check-E10.md.
- 2026-07-10T00:00:00Z E11-F01 pending -> spec-ready: Architect authored 4-file spec (specs/epics/E11-github-projects/F01-gh-cli-sync/), 14 EARS reqs R1–R14 each test-covered (extends tests/test_mirror.sh + tests/test_install.sh, no new suite). Decision: EXTEND the existing github-projects mirror (tools/sync-board.mjs, Projects v2), NOT fork/replace — F01 = gap-closure (fail-closed preflight R7, installer-executable assertion R12, doc contract-pin R13, VERSION MINOR bump R14). Assignee/owner->assignee deferred to E10-F03; status projection only. No ADR delta (planning tier never ran; recorded absent). PAUSED at human spec-approval gate (autonomous:false, require_spec_approval:true).
- 2026-07-10T00:00:00Z E11-F01 in-progress -> in-review: Builder implemented all 8 tasks (EXTEND sync-board.mjs preflight R7 fail-closed gh>=2.31.0 + project/repo scopes; docs contract-pin R13; test_install exec assertion R12; VERSION 0.28.0->0.29.0 + CHANGELOG R14). Full 15-suite verification.test_command GREEN, init.sh exit 0.
- 2026-07-10T00:00:00Z E11-F01 Reviewer verdict: APPROVE. Ran init.sh (0) + full 15-suite command (exit 0); R1–R14 each mapped to a passing test; confirmed gh-only/no-MCP, single github-projects code path, one-way invariant, installer-exec assertion real, no permanent-suite VERSION-freeze/main-diff anti-pattern, no Builder edits to DO-NOT-TOUCH files. Not rolling done before merge; proceeding to PR + Codex /pr-loop.
- 2026-07-11T00:35:01Z E11-F01 PR #42 Codex /pr-loop: r1 filed 2 P2 (gh auth-status stderr capture gh<2.33 cli#7920; exact `project` scope vs read:project) → fixed (spawnSync stdout+stderr, `[^-:]`). r2 filed 1 P2 (symmetric repo:status matched bare repo) → class-eliminating fix: tokenize scopes + exact Set membership (kills read:project/repo:status/repo_deployment/... substring class). r3 Codex CLEAN ("Didn't find any major issues", reviewed 04507d1). All P2 non-blocking but fixed for R7 fail-closed correctness. CI green (CodeQL + Analyze js/py). NOTE: r3 first trigger (00:16) was missed by Codex; re-triggered 00:29 naming the commit → landed 00:31.
- 2026-07-11T00:35:01Z E11-F01 in-review -> done: PR #42 merged (merge commit 0d11c01); feature complete.
- 2026-07-11T00:35:01Z E11 done rollup: all E11 features done; epic E11 -> done (derived + persisted; VERSION 0.29.0). Drift check triggered.
- 2026-07-11T00:35:01Z E11 drift check (Scout, read-only): E12/E13/E14/E99 all STILL-VALID; no demotions (E11 extended the mirror, preserved mirror.board shape + one-way invariant + provider-per-epic + jira/azure stubs; ownership deferred to E10, no new contradiction). Flag (pre-existing, from E10 rollup not E11): E12/E13/E14 reference E10-F03 which does not exist in tasks.json — for human doc-cleanup/re-seed decision. Findings: progress/drift-check-E11.md.
2026-07-11T00:51:39Z  E12-F01 pending → spec-ready — Architect wrote 4-file spec (Jira REST+PAT, no MCP); PAUSED at human gate (autonomous:false).
2026-07-11T00:52:41Z  E12-F01 spec-ready → in-progress — human approved spec at gate; spawning Builder.
2026-07-11T01:06:26Z  E12-F01 in-progress → in-review — Builder implemented T1–T14 (VERSION 0.30.0); all 15 suites green; spawning Reviewer (round 1).
2026-07-11T01:10:42Z  E12-F01 in-review → approve (round 1) — Reviewer verified all 15 suites green, R1–R18 traced, invariants + secret-hygiene clean. Proceeding to PR + pr-loop before done rollup.
2026-07-11T01:58:54Z  E12-F01 in-review → done — PR #44 merged (Codex clean r3, 2 rounds of P2 fixes: PAT-path, epic_name_field, PAT-log-scrub, transition-by-dest); Reviewer-approved, all 15 suites green.
2026-07-11T01:58:54Z  E12 done rollup: all E12 features done; epic E12 -> done (derived + persisted; VERSION 0.30.0). Drift check triggered.
2026-07-11T01:58:54Z  E12 drift check (Scout, read-only): E13/E14/E99 all STILL-VALID; no demotions (E12 filled the jira mirror branch additively, preserved mirror.board shape + one-way invariant + provider-per-epic + azure-boards no-op stub; ownership deferred to E10). No architecture.md/ADR set ⇒ degraded delta-basis. Carried flag: E13/E14 still reference nonexistent E10-F03 (human doc decision). Findings: progress/drift-check-E12.md.
2026-07-25T14:46:49Z  E99-F02 in-review → approve (round 2) — Reviewer verified F1 (VERSION 0.32.0 + CHANGELOG) and F2 (WORKFLOW.md) resolved; all 17 suites green. Proceeding to PR + pr-loop before done rollup.
2026-07-25T14:54:33Z  E99-F02 in-review → done — PR #47 merged (Codex r1: 1×P2 non-blocking only); Reviewer-approved r2, all 17 suites green; VERSION 0.32.0 tagged.
2026-07-25T16:04:17Z  E15-F01 in-review → reject (round 1) — Full 17-suite verification passed, but Reviewer found missing R12/R13 traceability rows and contradictory unlocked board-write instructions in collaborating role/store contracts; feedback: progress/E15-F01-review-current/review.md.
2026-07-25T16:22:28Z  E15-F01 in-progress → in-review (round 2) — Builder added R12/R13 traceability and routed all structural TaskStore writer contracts through the existing guarded apply --mutator path; VERSION 0.32.1; focused + full 17-suite verification green.
2026-07-25T16:45:58Z  E15-F01 in-review → approve (round 2) — Reviewer verified R1–R13 traceability, guarded writer-role/store coherence, PATCH versioning, focused suites, and the full 17-suite command. Feature remains in-review pending repair PR merge.
2026-07-25T16:58:20Z  E15-F01 in-review → done — Repair PR #48 merged at 9e62c13 after Codex round 1 (0 P0/P1 blockers; 2 P2 suggestions recorded); VERSION 0.32.1.
2026-07-25T17:06:49Z  E99 done rollup — all E99 features are done; epic E99 → done (derived + persisted). Drift check triggered before next-task selection.
2026-07-25T17:09:20Z  E99 drift check (Scout, read-only) — E13, E14, E15, and E16 still valid under S1/S2/S3; no demotions. Findings: progress/E99-drift/scout-drift-E99.md.
2026-07-25T17:24:09Z  E15-F02 pending → spec-ready — Architect authored R1–R14 four-file spec and applied five Doc-critic advisories; autonomous gate bypass engaged.
2026-07-25T17:24:26Z  E15-F02 spec-ready → in-progress — autonomous:true bypassed the human pause; Builder round 1 started from the approved spec.
2026-07-25T19:03:19Z  E15-F02 in-progress → in-review (round 1) — Builder implemented safe create/run/teardown worktree lifecycle, R1–R14 coverage, installer/docs wiring, and VERSION 0.33.0; focused + full 18-suite verification green.
2026-07-25T19:11:56Z  E15-F02 in-review → reject (round 1) — Reviewer reproduced an initialization-rollback branch leak from a clean divergent ambient branch and found incomplete boundary coverage for R2–R5 and R7–R13; feedback: progress/E15-F02-review-round1/review.md.
2026-07-25T19:26:23Z  E15-F02 in-progress → in-review (round 2) — Builder changed rollback to expected-OID compare-and-delete, closed every rejected R2–R5/R7–R13 fixture gap, and repaired the docs sentence; focused 21/21 and full 18-suite verification green.
2026-07-25T19:33:57Z  E15-F02 in-review → reject (round 2) — Reviewer approved the compare-delete repair and expanded traceability but reproduced destructive rollback when failed initialization retargets an ignored managed symlink; feedback: progress/E15-F02-review-round2/review.md.
2026-07-25T19:42:40Z  E15-F02 in-progress → in-review (round 3) — Builder made managed-link identity/removal a mandatory all-before-any rollback proof and added the retargeted ignored-link regression; focused 21/21 and full 18-suite verification green.
2026-07-25T19:51:13Z  E15-F02 in-review → approve (round 3) — Reviewer found no findings after independent managed-destination replacement and unlink fault injection, full R1–R14 audit, focused 21/21, and all 18 configured suites. Proceeding to PR + pr-loop before done.
2026-07-25T20:27:15Z  E15-F02 in-review → done — PR #51 merged at 4a8bdd6 after Codex rounds 1–3 resolved two P1 blockers (initializer stdout purity; unrelated normal-teardown prune); five P2 suggestions recorded as nonblocking; VERSION 0.33.0 tagged.
2026-07-25T20:42:37Z  E15-F03 pending → spec-ready — Architect authored R1–R20 with portable native fan-out, provision-before-claim sequencing, static conflict guards, and full lifecycle traceability; three Doc-critic findings applied; autonomous gate bypass engaged.
2026-07-25T20:42:57Z  E15-F03 spec-ready → in-progress — autonomous:true bypassed the human pause; Builder round 1 started from the approved R1–R20 contract.
2026-07-25T21:00:32Z  E15-F03 in-progress → in-review (round 1) — Builder implemented portable parallel dispatch, targeted worker lifecycle, config/installer/docs wiring, VERSION 0.34.0, and permanent R1–R20 coverage; focused + exact 19-suite verification green.
2026-07-25T21:13:21Z  E15-F03 in-review → reject (round 1) — Native-host concurrency scenarios passed, but Reviewer found duplicate F02 creation, unreachable clean-primary teardown after board writes, delegate-backend PR timing conflict, and missing lock-time stale-dependency race coverage; feedback: progress/E15-F03-review-round1/review.md.
2026-07-25T21:34:39Z  E15-F03 in-progress → in-review (round 2) — Builder repaired all four blockers with single provisioning, coordinator bookkeeping reconciliation, delegate fail-stop, and real stale-dependency/lifecycle regressions; focused + exact 19-suite verification green.
2026-07-25T21:42:57Z  E15-F03 in-review → approve (round 2) — Reviewer independently passed B1–B4, native fan-out/exclusivity/failure isolation, real bookkeeping/ff-only/exact-teardown lifecycle, R1–R20 traceability, and all 19 configured suites. Proceeding to PR + pr-loop before done.
2026-07-25T22:07:30Z  E15-F03 in-review → done — PR #53 merged at 181aa34 after Codex rounds 1–2 resolved one P1 installed-layout manifest-ordering blocker; one P2 suggestion recorded as nonblocking; VERSION 0.34.0 tagged.
2026-07-25T22:07:30Z  E15 done rollup — all E15 features are done; epic E15 → done (derived + persisted). Drift check triggered before next-task selection.
2026-07-25T22:09:45Z  E15 drift check (Scout, read-only) — E13, E14, and E16 remain still-valid under S1/S2/S3; no demotions. Reconciled E15 epic status/table and corrected E15-F03 TaskStore spec_path flagged by Scout. Findings: progress/E15-drift/scout-drift-E15.md.
2026-07-25T22:23:37Z  E16-F01 pending → spec-ready — Architect authored R1–R14 cycle/diagnostic contract aligned to ADR-0001, applied four Doc-critic findings, and Orchestrator corrected the pre-existing TaskStore spec_path; standing autonomous approval engaged.
2026-07-25T22:24:14Z  E16-F01 spec-ready → in-progress — user’s standing autonomous instruction approved the gate; Builder round 1 started from the R1–R14 contract.
2026-07-25T22:42:12Z  E16-F01 in-progress → in-review (round 1) — Builder implemented deterministic feature/slice cycle diagnostics, warn-only init integration, stable seven-code selection vocabulary, installer/docs wiring, VERSION 0.35.0, and R1–R14 tests; exact full verification green.
2026-07-25T22:50:39Z  E16-F01 in-review → approve (round 1) — Reviewer found no findings after all seven exact no-selection codes, unchanged board hashes, independent SCC audits, R1–R14 traceability, and all 20 configured suites. Proceeding to PR + pr-loop before done.
2026-07-25T23:15:20Z  E16-F01 in-review → done — PR #55 merged at b86663f after Codex rounds 1–2 resolved one P1 sliced-parent diagnostic gap; three P2 suggestions recorded as nonblocking; VERSION 0.35.0 tagged.
2026-07-25T23:26:30Z  E16-F02 pending → spec-ready — Architect authored R1–R15 human-first rationale plus exact C1–C8/D1–D13 ledger contract, applied three Doc-critic findings, and Orchestrator corrected the pre-existing TaskStore spec_path; standing autonomous approval engaged.
2026-07-25T23:26:59Z  E16-F02 spec-ready → in-progress — user’s standing autonomous instruction approved the gate; documentation Builder round 1 started from the R1–R15 contract.
2026-07-25T23:44:14Z  E16-F02 in-progress → in-review (round 1) — Builder delivered the cold-reader rationale, exact C1–C8/D1–D13 ledger, primary-source notes, installed navigation, VERSION 0.36.0, and documentation-only R1–R15 coverage; exact 21-suite verification green.
2026-07-25T23:50:45Z  E16-F02 in-review → approve (round 1) — Reviewer found no findings after installed-only cold read, byte-identical install, exact 21-row/source audit, R1–R15 traceability, documentation-only scope, and all 21 configured suites. Proceeding to PR + pr-loop before done.
2026-07-26T00:01:05Z  E16-F02 in-review → done — PR #57 merged at 3c0619d after Codex round 1 found no P0/P1 blockers; two P2 suggestions recorded as nonblocking; VERSION 0.36.0 tagged.
2026-07-26T00:16:46Z  E16-F03 pending → spec-ready — Architect authored R1–R17 deterministic Node selector contract aligned to ADR-0001 and current Node docs, applied four Doc-critic findings, and Orchestrator corrected the pre-existing TaskStore spec_path; standing autonomous approval engaged.
2026-07-26T00:17:48Z  E16-F03 spec-ready → in-progress — user’s standing autonomous instruction approved the final gate; Builder round 1 started from the R1–R17 selector contract.
2026-07-26T00:39:12Z  E16-F03 in-progress → in-review (round 1) — Builder implemented the zero-dependency deterministic selector, authoritative schema-v1 integration with preserved prose fallback, installer/docs wiring, VERSION 0.37.0, and exhaustive R1–R17 differential coverage; exact 22-suite verification green.
2026-07-26T00:48:02Z  E16-F03 in-review → reject (round 1) — Reviewer confirmed two R13 fail-closed parser gaps (duplicate top-level repos and malformed inline workflow) plus missing genuine R13/R16 traceability labels/cases; feedback: progress/E16-F03-review-round1/review.md.
2026-07-26T00:59:32Z  E16-F03 in-progress → in-review (round 2) — Builder made relevant config/manifest parents fail closed, added exact and nearby malformed-input regressions, and replaced fabricated aggregate mappings with stable R1–R17 labels; exact 22-suite verification green.
2026-07-26T01:07:44Z  E16-F03 in-review → reject (round 2) — Reviewer confirmed prior fixes but found malformed non-comment child lines inside workflow/umbrella mappings were silently skipped, violating R13/R16 fail-closed syntax; feedback: progress/E16-F03-review-round2/review.md.
2026-07-26T01:17:10Z  E16-F03 in-progress → in-review (round 3) — Builder made malformed non-comment descendants of workflow/umbrella fail closed, preserved valid nested mappings, added exact and nearby regressions, and passed the exact 22-suite verification.
2026-07-26T01:24:27Z  E16-F03 in-review → reject (round 3) — Reviewer confirmed the round-2 syntax cases but found inconsistent relevant-mapping indentation and scalar descendants were accepted despite invalid YAML, violating R13/R16; feedback: progress/E16-F03-review-round3/review.md.
2026-07-26T01:32:51Z  E16-F03 in-progress → in-review (round 4) — Builder enforced per-mapping child indentation, rejected scalar descendants and partial nested dedents under workflow/umbrella, added six permanent negatives, and passed the exact 22-suite verification.
2026-07-26T01:40:55Z  E16-F03 in-review → approve (round 4) — Reviewer found no findings after replaying all prior blockers, a 26-shape config boundary matrix, R1–R17 traceability, independent routing/performance checks, installer/fallback audits, and the exact 22-suite verification. Proceeding to PR + pr-loop before done.
2026-07-26T01:49:21Z  E16-F03 in-review → done — PR #59 merged at b118408 after Codex round 1 found zero P0/P1 blockers; one P2 nested-sequence suggestion was recorded as nonblocking; VERSION 0.37.0 tagged.
2026-07-26T01:50:41Z  E16 pending → done — all three robustness features are merged and Reviewer-verified; epic metadata and feature rollup synchronized before drift review.
2026-07-26T01:53:13Z  E16 drift-check complete — Scout judged draft epics E13 and E14 still-valid; no E16-caused S1 contradiction, S2 removal/rename, or S3 supersede signal fired. A pre-existing E10-F03 brief reference was noted for future drilling but did not qualify for demotion.
2026-07-27T04:48:32Z  E17-F01 pending → spec-ready — Architect wrote 25 R-ids across config schema, per-front-end resolution, backward compatibility, stamping, deselection and release; Scout recon + verified vendor research fed the plan. Split recommended (R15–R17 seam) but full five-front-end coverage retained per the human intake decision.
2026-07-27T04:48:41Z  E17-F01 spec-ready → in-progress — human gate cleared by explicit "continue autonomously" authorization; one-shot build chosen over the Architect split recommendation, honoring the intake decision for full five-front-end coverage.
2026-07-27T05:22:08Z  E17-F01 in-progress → in-review (round 1) — Builder implemented T1–T25 across all five front-ends: models: block in config + migrate_config, resolver (model_tier/resolve_model/models_any), stamping for claude/antigravity/opencode, new .gemini/agents and .codex/agents emitters, deselect symmetry, docs/manifest, VERSION 0.38.0. Reports full 23-suite chain EXIT=0 and a byte-identity check against main.
2026-07-27T05:43:27Z  E17-F01 in-review → reject (round 1) — Reviewer reproduced the full chain (green under LC_ALL=C; the only failure is a pre-existing UTF-8 collation bug in tools/fix-worktree.sh, confirmed on clean main) and mutation-tested the suite 7 ways. Two blocking findings: opencode.json fresh-install mode regressed 0644 → 0600 via cp from mktemp (breaks BR1/R11 and diverges fresh vs upgrade), and R5 is violated when a pin is literally "inherit" (written verbatim to claude/codex artifacts).
2026-07-27T06:12:26Z  E17-F01 in-progress → in-review (round 2) — Builder fixed both blockers: opencode.json write switched cp → cat so fresh installs keep umask mode (with a umask-independent mode assertion), and a pin of "inherit" now compiles to key omission on all five front-ends before the OpenCode format guard. R11 diff exclusion narrowed back to harness.config.yaml. Both fixes mutation-proved; full chain exit 0 under LC_ALL=C, 478 assertions.
2026-07-27T06:33:17Z  E17-F01 in-review → approve (round 2) — Reviewer re-ran init.sh + the full 23-suite chain (LC_ALL=C exit 0, 478 assertions; UTF-8 red only at the pre-existing tools/fix-worktree.sh collation bug, reproduced on clean main). All three round-1 findings fixed and mutation-proved: opencode.json now written with cat > at both §6 sites (fresh/upgrade both -rw-r--r--, umask-independent assertion verified at umask 002/027/077), a pin of "inherit" compiles to omission on all five front-ends before the OpenCode guard, and the manifest.txt exclusion is gone from the R11 diff. R11 re-verified vs a main worktree with identical modes and paths across all 111 entries; R21/R22 shared-emitter determinism re-mutated 4 ways and still bites. Recommend done.
2026-07-27T06:34:07Z  E17-F01 in-review → approve (round 2) — Reviewer re-ran the full chain (exit 0 under LC_ALL=C, 478 assertions), re-mutation-tested the shared-emitter determinism (4 fresh mutations all caught), verified R11 byte+mode identity vs main across 111 paths, and confirmed both blockers fixed. Judged the umask-independent mode assertion better than its own round-1 suggestion. All R1–R25 mutation-sensitive. Proceeding to PR + pr-loop before done.
2026-07-27T14:47:47Z  E17-F01 in-review → done — PR #61 merged at b55a464 after 4 Codex rounds (2 P1s fixed, 1 P1 adjudicated by the human into spec amendment A1 + R26, final round zero blocking). VERSION 0.38.0. Epic E17 deliberately NOT rolled: F02 (dual Builder + escalation) and F03 (worker roster) are anticipated in epic.md but not yet seeded, so an all-features-done rollup would publish a false completion.
2026-07-27T20:04:21Z  E99-F03 pending → in-review — Fixer seeded a locale-safe validate_key fix under the reserved maintenance epic E99 (reused by id; epic rolled done → in-progress to carry pending work). Builder pinned tools/fix-worktree.sh to LC_ALL=C, closing the collation hole where en_US.UTF-8 accepted uppercase slugs (the pre-existing bug E17-F01's round-1 review recorded on clean main). Reviewer approved: full 23-suite chain green under both en_US.UTF-8 and C, new dual-locale regression test proved non-vacuous against the pre-fix body, LC_ALL=C confirmed not to ASCII-cripple init.sh's Python validators (PEP 538 coercion). VERSION 0.38.1. Proceeding to PR + pr-loop before done.
2026-07-27T21:05:00Z  E99-F03 in-review → done — PR #65 merged at d385d49 after 4 Codex rounds. Round 2 raised the only blocking finding (P1): the exported LC_ALL=C reached the project init gate, which was REPRODUCED destroying an otherwise valid worktree (a consumer whose .harness/init.project.sh needs UTF-8 fails its own gate, and do_create rolls back). Human adjudicated the fix scope, narrowing the blast radius the intake brief had accepted: the caller's locale is now restored at each foreign-code escape while every glob and parsed git call stays under C. Rounds 1 and 3 fixed two P2s (glibc en_US.utf8 locale-alias skip that silently voided the regression leg on Linux CI; post-checkout hook via git worktree add). Round 4's P2 — reference-transaction hook on teardown/rollback ref updates — was DEFERRED, not patched: its remedy converges on the validate_key-scoped LC_COLLATE=C that intake explicitly rejected, making it a decomposition decision at the round cap. Seeded as E99-F04 (pending, autonomous:false). All three foreign-code legs are independently mutation-proved; full 23-suite chain exit 0 under C and the fix-worktree suite green under en_US.UTF-8. VERSION 0.38.1. Epic E99 deliberately NOT rolled: E99-F04 is pending, so an all-features-done rollup would publish a false completion.
2026-07-28T11:46:44Z  E20-F01 in-review → approve (round 1) — Reviewer re-ran ./init.sh and the full 26-suite verification chain (exit 0), then mutation-tested independently: 19 mutations, 15 killed, 4 survivors all proved semantically equivalent for the real config shape or explicitly delegated to the by-hand checklist. Targeted the SECOND `elif [ -t 0 ]` (harness-install.sh:1470) by line number after asserting builder_backend_prompt in context, avoiding the vacuous resolve_agents hit at :1371. Config writer verified byte-level: 1-line diff with trailing comment + indentation + all 153 comments preserved, idempotent (find -newer proves the writer is SKIPPED, not re-run), append-only migration confirmed by head-prefix cmp, and three unnamed edges (empty value, empty value + trailing comment, quoted value) all handled and convergent. All eight by-hand interactive items executed over a real pty (expect), including Ctrl-C (rc=130, stty -g identical, nothing written) and the non-raw toggle_select rung. Blast radius clean: 11 protected function bodies byte-identical to HEAD, AGENT_KEYS unchanged, --print-agents still exactly 2 stdout lines, only one line removed from the installer (a usage comment). MINOR bump 0.41.0 → 0.42.0 judged correct per AGENTS.md Versioning. Five non-blocking findings recorded in progress/review-E20-F01.md (fresh-vs-migrated execution: block text divergence and its discoverability half, a silent no-op when execution: exists without backend:, and two test-strength gaps that become load-bearing at E20-F02). Proceeding to PR + pr-loop before done.

- E21-F01/F02/F03 + E99-F06 — change-size discipline (PR #78, merged 2026-07-28). Codex: 7 rounds total, 19 findings resolved; E21-F04 withdrawn mid-review (premise invalid), 4 non-blocking findings deferred to E99-F07.
2026-07-29T05:12:40Z  E99-F05 pending → in-review (round 1) — Orchestrator routed the autonomous sdd:false fix straight to the Builder. wfc_bot_seen now takes the trigger anchor and filters both bot streams (issue-comments by created_at, pr.json .reviews[] by submittedAt) so an earlier round's Codex activity no longer disarms the first-response probe. Reaction check left alone: reactions.json is fetched per-round from the current trigger comment id, already round-scoped by construction; rationale pinned with an assertion. R57 regression added to tests/test_pr_loop.sh, mutation-proved against the pre-fix body (reproduced the PR #68 symptom: expected 5, got 2). Full 27-suite chain exit 0, 655 assertions. VERSION 0.46.1.
2026-07-29T06:05:00Z  E99-F05 in-review → approve (round 1) — Reviewer re-ran ./init.sh and the full 27-suite chain with CODEX_HOME sandboxed (exit 0, 655 assertions, 0 failures, 0 skips) and mutation-tested R57 in BOTH directions: reverting wfc_bot_seen to its unscoped pre-fix shape reproduced the exact PR #68 symptom ("expected 5, got 2"), and four over-correction mutations were each killed — .createdAt on the REST comment stream, .created_at on the GraphQL review stream, timestamp-filtering the reactions stream, and neutering wfc_bot_seen to always return 1. The per-stream field split was verified against the LIVE GitHub API rather than the fixture (gh pr view --json reviews exposes submittedAt and no created_at; REST issue comments expose created_at; the anchor is REST created_at — all fixed-width RFC3339 UTC, so lexical >= is exactly chronological). The false-exit-5 edge was settled by replaying 14 real recorded rounds in .pr-loop/{68,70,72,74,76}: #68/round-1 is a control with zero stale activity (so byte-identical pre/post fix), zero bot issue comments ever, its only bot review at t≈291s, and mtimes proving the watcher ran to t≈316s without firing at 180s — which can only be the transient 👀 reaction, confirming the reaction stream is load-bearing and that this fix makes rounds 2+ behave exactly like the empirically-safe round 1. Constraints all hold: sh/dash/bash -n clean, the bare return 1 intact at :213, all six bot comparisons exact literals with no prefix match (R27), no exit statement changed, no VERSION literal or default-branch diff in the suite. PATCH 0.46.0 → 0.46.1 confirmed warranted (harness-install.sh:1868 copy tools; R21 asserts install parity). change-size tier: ok (35 production lines / 3 files). Four non-blocking observations recorded in progress/E99-F05/review.md, incl. the now over-stated "no Codex activity of any kind" diagnostic and a measured 178–528s first-response latency that makes the 180s default tighter than it looks. Recommend done after merge.
2026-07-29T12:20:40Z  E99-F07 in-review → reject (round 1) — Reviewer re-derived all four brief defects as genuinely fixed, then rejected on a single narrow finding: the -z rename fold is asserted but not tested. The R7e fixture is a 0-line rename between two PRODUCTION paths, so the unfolded header record (NF==3, $3=="") classifies as production and contributes 0 lines while the two bare path records are eaten by the pre-existing NF<3 skip — every number R7e checks is identical with and without the fold. Two Reviewer-invented mutations (delete the awk state machine; key the fold on the source) both survive the full suite, and a rename-with-modification onto a test path is charged entirely to production — the exact defect item 1 removes, through the door the fix opened. Reviewer built and verified the replacement fixture. Also verified and standing: -h before [ -f ] is correct not scope creep (broken links and links-to-dirs are both mode 120000 and 1 0 in --numstat); the docs/WORKFLOW.md edit is allowed (no repo-global DO-NOT-TOUCH list — it is a per-.plan.md construct); both replaced assertions bite; all nine pre-existing guarantees non-vacuous; VERSION 0.46.3 PATCH sound.
2026-07-29T12:27:03Z  E99-F04 in-review → reject (round 1) — Reviewer independently re-derived the whole locale audit (29 git invocations across 19 kinds; bracket ranges exist ONLY at lines 79/80/85, all inside validate_key's C-pinned region; no sort, no </>) and agreed with all 19 verdicts: zero pins needed on git. Probed the residual portability hole across 5 shells x 2 locales with both a valid multibyte path and an invalid UTF-8 byte — all prefix globs and strips correct — and closed it structurally: bash 3.2 is the only shell where ranges are collation-sensitive AND it re-runs setlocale on assignment, while shells that ignore mid-script LC_ALL= match byte-wise anyway, so the two failure modes are mutually exclusive. Seven of its own mutants; confirmed the reduced mutant independence is an EQUIVALENT mutant, not a blind spot. Rejected on one blocking finding: the comment at tools/fix-worktree.sh:269-273 states a git constraint that does not exist for the call it annotates — only `git branch -d` refuses to delete a branch checked out in a worktree; `git update-ref -d` succeeds (exit 0, git 2.55.0). The Builder generalised a branch -d transcript to update-ref -d without testing it, and the false claim propagated into the public CHANGELOG. The conclusion (do not swap the ordering) is right for a STRONGER reason the Reviewer verified: deleting the ref first leaves the worktree on an unborn branch, non-forced worktree remove then refuses, and the residual is registration-present + branch-absent — the one state do_teardown hard-refuses, i.e. NOT reconcilable by re-run.
2026-07-29T12:36:01Z  E99-F07 in-progress → in-review (round 2) — Builder accepted B1 in full. New R7e fixture satisfies three conditions at once (60-line baseline on main so git calls it a rename; 6 lines appended so the folded record carries lines; destination classification differs from source: src/moved.js production → src/moved.spec.js test). N1 and N2 both now FAIL where they previously passed the whole suite, plus a self-invented N5 attacking the fold TRIGGER (the NF==3 && $3=="" header rule) rather than its body. All four non-blocking items dispositioned: three fixed, trailing-tab stripping declined as a behaviour change and documented under Known limits instead. tools/change-size.sh changed by comments only this round, verified mechanically. Both false round-1 claims retracted in place in build.md rather than quietly rewritten.
2026-07-29T12:42:42Z  E99-F04 in-progress → in-review (round 2) — Builder re-ran every step itself rather than trusting the Reviewer transcript and confirmed the Reviewer was right: the checked-out guard is `branch -d`/`-D`-specific; `update-ref -d refs/heads/X $BASE` exits 0 and the ref IS gone. It then performed the swap to verify the REAL reason: the worktree keeps the branch name but HEAD is unborn, every tracked file reads staged-new, non-forced `worktree remove` refuses, and the residual is registration-present + branch-absent — the state do_teardown hard-refuses. Also verified the other half rather than asserting it: `merge-base --is-ancestor X X` exits 0, so the retained order's residual passes branch_merged by construction and its exact shape is already asserted reconcilable by an existing teardown-idempotency case. Corrected all three sites, with build.md leading on an explicit retraction naming the error rather than quietly overwriting it. All five non-blocking items dispositioned; N2 mutation-proved that guard leg 4 was vacuous under ambient LC_ALL=C and now bites. VERSION stays 0.46.2 (round 2 is comments, CHANGELOG prose and test hygiene only).
2026-07-29T13:06:53Z  E99-F04 in-review → approve (round 2) — Reviewer re-derived every clause of the corrected rationale itself on git 2.55.0 rather than trusting either transcript, and confirmed the mechanical comments-only claim (catching that the single non-comment hunk at tools/fix-worktree.sh:290 is exactly the authorized N1 diagnostic change). Verified the E99-F126-lone citation is load-bearing and real. Re-ran the N2 mutation: the round-1 test file was VACUOUS under ambient LC_ALL=C (ok, exit 0) and the round-2 file bites (FAIL) — the round's most valuable change. Ruled the do_teardown clause in-scope, not scope creep. Settled the leg-5 audit by evidence: tests/test_fix_worktree.sh:278 IS a second instance of the reachable-by-another-path construct (vacuous under ambient C, bites under UTF-8), but test_slug_rejection_is_locale_independent carries a genuinely non-vacuous proof in the locale CI actually runs in — so nothing is left unproven and it is a nit (N6). An earlier suite failure (test_cascade.sh R2) was proved to be ENOSPC from an unrelated project filling TMPDIR, not a defect: the clean re-run is 656 ok / 0 FAIL. 24/24 under all three locales with NO skips, so the UTF-8-dependent legs really executed. sh/dash/bash --posix/zsh -n all clean. Change-size tier ok (102 production lines / 3 files).
2026-07-29T13:21:21Z  E99-F07 in-review → reject (round 2) — Round 1 B1 confirmed closed: N1/N2/N5 all fail now, plus four Reviewer-invented mutations on untried axes (fold fires but drops counts; fold zeroes them; fold double-emits source AND destination; header rule loosened to bare NF==3) all caught, and the one survivor proved an EQUIVALENT mutant via byte-identical JSON on all seven fixtures rather than assumed. Over-fit check: all three fixture conditions load-bearing, each weakened independently. Comments-only claim verified by md5 of the comment-stripped file at both commits, not by reading the diff. The 651/660/659 discrepancy reconciled: 660 counts a non-assertion `ok` line from test_umbrella.sh, 659 is `^ok - `, and replaying the suite at the round-1 commit in the same environment also gives 659/660 — so no assertion was gained or lost, which is the only thing the count was evidence for. REJECTED on B2, the FOURTH instance of the reachable-by-another-path pattern in this feature: R9b greps for "PR body" across the whole of orchestrator.md minus the fenced parallel-fix section, so it is satisfied by any occurrence anywhere else — and this feature's own item-3 edit put one at the umbrella sub-step. Executed mutation M11 removed ONLY the PR-body instruction from the main-path section and the suite stayed green. The test already knows the remedy: it applies section-scoped extraction to reviewer.md with a comment explaining why a whole-file grep is worthless, and simply never applied it to orchestrator.md.
2026-07-29T13:43:26Z  E99-F07 in-progress → in-review (round 3) — B2 accepted; the second path to the expected value was created by this feature's own item-3 edit. R9b's "PR body" and "never blocks|advisory" greps now run against a heading-extracted main-path section (index(), no regex, with a non-empty guard so a renamed heading fails loudly instead of making everything under it vacuous). Proven both ways including a self-invented M12 (move the advisory sentence into the umbrella step), which exists because the single-occurrence case would fail under either scope — the scoping only earns its keep once a second occurrence appears. NB-2 accepted: destination renamed so the rename record sorts last in the raw -z stream, and a new N11 (lazy flush that never emits at END) now bites. Sweep found two more instances: R7e's total_files message claimed to detect a MISSING fold, which it cannot (5 either way) though it does detect a double-emit; and R7f was entirely jq-gated so the tab escape had NO coverage on a jq-less host. The Builder's own first attempt at the --repo tightening had the exact defect it was meant to close (HARNESS_DIR appears 4× in that section), caught only by executing the mutation. Harness tightening taken: agents/builder.md now carries the reachable-another-way lens, asserted by R9c so it is not an untested doc claim.
2026-07-29T14:02:09Z  E99-F07 in-review → reject (round 3) — B2 confirmed fixed both directions (M11c fails scoped and PASSES under round-2 $_outside, reproducing the lie independently; the non-empty heading guard fires first; a moved invocation proves the section-level check non-vacuous). All three sweep fixes verified by measurement: R7e total_files is undetectable for a missing fold (5) and detects a double-emit (6); R7f's new assertions were run on a CONSTRUCTED jq-free PATH since this host ships /usr/bin/jq; the abandoned HARNESS_DIR attempt would indeed have passed (4x in section, 3x after the caveat is deleted). NB-2 confirmed by od -c. The Builder's correction to round-2 NB-1 verified and RETRACTED by the Reviewer: a jq-less pr-round-trend.sh exits 4 with 0 bytes stdout, so moving the gate down would turn a skip into a spurious failure. Harness edit ruled IN SCOPE — round 2 recommended it, agents/reviewer.md authorizes self-improvement, and .claude/agents/builder.md is a pointer not a mirror so no drift is possible. REJECTED on B3, the FIFTH instance and located inside the fix meant to stop it: R9c greps "reachable\|other than the one" across the whole of builder.md, so replacing the entire lens bullet with an unrelated bullet about dead code leaves the suite green — the lens gone from installed body, the assertion guarding it reporting pass. Reviewer validated the two-clause remedy both ways.
2026-07-29T14:16:32Z  E99-F07 in-progress → in-review (round 4) — B3 accepted. R9c now uses three -qF greps on distinctive fixed substrings, one per operative clause: the lens, the question that operationalises it, and a THIRD the Builder added beyond the validated remedy — the mutation-proof requirement — on the grounds that a question without the proof step invites reasoning about the answer instead of running it, which is precisely how its own abandoned HARNESS_DIR tightening got written in round 3. Substrings chosen line-local because grep cannot match across a line wrap. Proven both ways including a contrast run showing the round-3 form still passes the B3 mutant (which contains the word `reachable` in its dead-code bullet) — demonstrating the mechanism rather than asserting it. NB-4 and NB-6 taken; NB-3 and NB-5 declined with reasons encoded as comments, NB-5 on a stronger ground than fails-safe: each extractor stops at the first heading that could not be part of its own section, so the asymmetry is correct rather than merely harmless.
2026-07-29T14:33:01Z  E99-F07 in-review → approve (round 4) — Reviewer verified tools/ and agents/ mechanically unchanged (git diff f3159f7 77d0971 empty), then closed B3 by executed contrast: the round-3 dead-code mutation of agents/builder.md:79-84 is now FAIL against the round-4 assertions and still PASS against the round-3 test file on the same tree. The three -qF clauses are independently load-bearing (three surgical mutations, three distinct messages, other two substrings verified present in each mutant) and each substring occurs exactly once on a single line. Ruled the third clause JUSTIFIED, not scope creep: the two-grep remedy round 3 prescribed lets the mutation-proof sentence be deleted silently (executed and confirmed green), so the Reviewer's own prescription was the sixth instance of the defect class. NB-4 verified via cat -A on fail()'s echo path plus a tests/-wide backslash sweep; NB-6's count of five corroborated against rounds 2 and 3; NB-3's declination encodes the within-section-paraphrase distinction faithfully; NB-5's declination measured against the real file structure (^## cannot match a ### heading at all -> _main_cs is only correct with /^#+ /, and a ### sibling would be swallowed otherwise) and recorded as the Builder improving on the review, with one non-blocking qualification that the comment's closing generalization overstates for a #### subsection (fails loud, not silent). ./init.sh exit 0, full 27-suite chain exit 0 with 660 ok lines, CODEX_HOME sandboxed, no ENOSPC, sh/dash/bash -n clean, change-size tier ok. VERSION 0.46.3 / PATCH unchanged. Recommend done after merge.
2026-07-29T14:55:36Z  E99-F04 / E99-F05 / E99-F07 in-review → done — PRs #82, #80 and #83 all merged. Codex returned CLEAN on round 1 for F05 (191s) and F04 (128s); F07 drew a single P2 on round 1 (192s), deferred under the STATED stopping rule (blocking_severities P0,P1, fixed before the round) and seeded as E99-F08 rather than starting a fifth cycle. VERSION 0.46.3. Local review did the work Codex did not have to: F04 and F07 were both rejected on round 1, and F07 took four rounds. Every F07 rejection was the same defect class in the TESTS, not the fix — an assertion whose expected value is reachable by more than one code path, five instances in one feature, the fifth inside the guard written against the other four. All five were caught by EXECUTING a mutation and none by inspection, including two self-sweep attempts that carried the defect they were written to close. agents/builder.md now carries that lens, asserted in three parts by R9c. F04's round-1 rejection was the reasoning analogue: a right conclusion (do not swap the teardown ordering) resting on a false mechanism (the checked-out guard is branch -d-specific; update-ref -d has no such check), which had already reached the public CHANGELOG. Epic E99 deliberately NOT rolled: E99-F08 is pending, so an all-features-done rollup would publish a false completion.
2026-07-29T16:27:58Z - E21-F04 pending -> spec-ready (Architect completed specs)
2026-07-29T17:03:16Z - E21-F04 spec-ready -> in-progress (Human approved)
2026-07-29T18:45:00Z - OpenCode /sdd-fix-parallel opt-in + model helper — installer skips /sdd-fix-parallel for OpenCode by default; adds /sdd-test-concurrency probe and --with-opencode-parallel flag; fixes remove_owned set -e regression on macOS sh; adds tools/opencode-model-helper.sh with heuristic tier pins; updates docs/manifest; VERSION 0.47.0. Relevant suites green.
2026-07-29T21:00:26Z | orchestrator | E21-F04 | pending → architect (re-spec after withdrawal in PR #78): spawning Architect with inbox brief + existing spec + epic context
2026-07-29T21:06:28Z | architect | E21-F04 | pending → spec-ready (re-spec after withdrawal: 9 R-ids, stacking framed as incremental review only, ADRs touched: none, Doc-critic clean) — PAUSED at human gate
2026-07-29T21:21:33Z | orchestrator | E21-F04 | spec-ready → in-progress (human approved, gate latency 905s) — spawning Builder (round 1)
2026-07-29T21:37:34Z | builder | E21-F04 | in-progress → in-review (Builder round 1: T1–T7, 6 files changed, init.sh + all tests green) — spawning Reviewer
2026-07-29T21:42:45Z | reviewer | E21-F04 | in-review → approve (round 1) — all 83 pr-loop tests green, R1–R9 traced, cross-file consistent, DO NOT TOUCH honored, ADRs correct, change-size ok (225 lines/5 files) → done
2026-07-29T21:42:45Z | orchestrator | E21-F04 | done (Reviewer verdict persisted). E21 epic rolled up → done.
2026-07-29T21:42:45Z | orchestrator | E21 drift check (trigger: E21 done): NO-OP — no specs/architecture.md to re-validate against (graceful degradation); no epic demoted
2026-07-30T00:28:39Z | reviewer | E23-F01 | in-review → approve (round 7) — Independent spec and quality/security reviewers approved all R1-R10 at f0a3fc4 after seven file-backed rounds closed ownership, partial-unit, invocation, and symlink boundary defects. The exact configured 27-suite chain passed; change-size tier ok (433 production lines / 3 files). Proceeding to PR + sdd-pr-loop before merge cleanup.
2026-07-30T01:44:22Z | builder | E99-F08 | in-progress → in-review (Builder round 1: _json_escape handles the full C0 class; R8d regression test; VERSION 0.48.0 → 0.48.1) — spawning Reviewer
2026-07-30T01:44:22Z | reviewer | E99-F08 | in-review → approve (round 1) — full test_command (27 suites) green; exhaustive C0 class check byte-exact under sh/dash/bash-3.2; mutation re-run: R8d fails on pre-fix mutant with the real symptom, fix restored byte-identical; POSIX syntax clean; R7f/R8c untouched; change-size ok (41 lines/3 files) → done
2026-07-30T01:45:30Z | orchestrator | E99-F08 | done (Reviewer verdict persisted). E99 epic rolled up → done.
2026-07-30T01:50:00Z | scout | E99 drift check (trigger: E99 done): E13/E14/E17 NOT-STALE, no demotions (findings: progress/E99-drift/scout-drift-E99.md)

2026-08-02T03:46:23Z | orchestrator | board | /sdd-next selector returned complete (no-candidates: all seeded features done). Closed the outstanding epic-rollup gap: **E23 rolled up → done** (sole feature E23-F01 done, PR #88 merged 2026-07-30; epic.md frontmatter synced). **E17 deliberately NOT rolled** — all its *seeded* features are done, but its own epic.md lists anticipated-but-unseeded features and unmet success criteria (escalation path, worker roster), so an all-features-done rollup would publish a false completion; it needs `/sdd-drill E17`.
2026-08-02T03:46:23Z | scout | E23 drift check (trigger: E23 done): E13/E14/E17 all **NOT-STALE**, no demotions (findings: progress/E23-drift/scout-drift-E23.md). E23 produced zero ADRs and there is no specs/architecture.md, so the ADR signal could not fire; verdicts grounded in E23-F01's actual diff (merge 2b63977, 28 files, exactly one production file — harness-install.sh §5d/§5f). E23 touched E17's neighbourhood but only *through* E17-F01's own helpers behind an explicit scope fence, so the 2026-07-30 E99 judgment stands.
2026-08-02T06:10:27Z | driller | E17 | /sdd-drill E17 (amend — E17 was already pending/planned, so ids appended above max with no renumber, no re-flip): seeded **F02** (builder-heavy role), **F03** (deterministic escalation, depends_on F02), **F04** (worker roster). THREE features, not the two anticipated: "dual Builder + escalation" specced to ~17 R-ids against max_requirements 12, so it was split at decomposition per E21-F01. **ADR-0002** records the decision — builder-heavy is the same body at a heavier tier, never a second prompt, so escalation is routing not behavior; a per-spawn override was rejected because only Claude Code and Antigravity can override per spawn (it would silently no-op on 3 of 5 front-ends). Epic-level decision: **keep gated** (all autonomous:false). Briefs: progress/inbox/E17-F0{2,3,4}.md. Merged as PR #94.
2026-08-02T06:10:27Z | orchestrator | E99-F09 | PR #87 closed **unmergeable** and re-specced. Its branch and the merged E23-F01 both write .agents/skills/<name>/SKILL.md over the same command list (Antigravity vs Codex); each side's reclaim deletes what it believes it owns, so deselecting either front-end can remove the other's live glue while the pristine-only guard reports success. Merge attempted (7 files, 19 hunks) and **backed out rather than forced** — resolving the text yields an installer that passes its own tests and clobbers a selected front-end. E99-F09 seeded pending/gated with progress/inbox/E99-F09.md; branch preserved at 16994a5; E99 re-opened from done.
2026-08-02T06:10:27Z | orchestrator | board | Backfilled the missing **E22** record (E22-F01 shipped in PR #85 with no board entry for four days) and salvaged **E21-F05** from the abandoned PR #81 before closing it; E21 re-opened from done. Merged as PR #93. Also **v0.50.1** (PR #92): the `pin.` guidance said an unpinned codex/opencode tier "stamps nothing", which post-E23-F01 reads as "no role definition at all" — corrected in all four places it appeared, verified by executing both directions rather than reading the code.
2026-08-11T00:00:00Z | orchestrator | E99-F15 | pending → in-progress → in-review → done (sdd:false fix lane). One choke point for every spec/epic filesystem read: `_read_contained(root_real, path)` in tools/validate-board.py is now the only function that opens a spec or epic document, and root_real is its first REQUIRED argument — so a new read site cannot be written without naming what should contain it. Closes the fourth containment site (Codex #3741652119: epic.md via a symlinked specs/epics/<id>-* directory), which was open and NOT theoretical — an external epic.md's status changed the verdict. tasks-lock.py dropped its own copy of the check and its own io.open; it reads through the same entry point and re-establishes containment before writing (`_write_contained`, a deliberate write-side twin: the read helper answers "may I open this", which is the wrong question a moment before a write). All 28 existing R-ids passed UNCHANGED — no test edited — plus R29 (the epic.md escape, with an in-repo-symlink control) and R30 (replaces the entry point and requires the verdict to change for BOTH documents, so a read site keeping its own open() fails the suite). Mutation-checked against main: R29 fails, R30's probe cannot run. PR #126 — Codex clean on round 1, 0 findings; 34/34 suites; v0.59.1. Board rolled via chore PR #127.
2026-08-11T00:00:00Z | orchestrator | E17-F04 | pending → architect: spawning spec generation with progress/inbox/E17-F04.md as primary input (sdd:true, autonomous:false ⇒ human gate ahead)
2026-08-11T00:00:00Z | architect | E17-F04 | pending → spec-ready (12 R-ids, exactly on max_requirements: 12; complexity: **complex** on a stated basis — landed on the budget, and the capability table is a second table describing the same five front-ends as AGENT_KEYS/HOST_MARKERS, the body-vs-copy defect class E17-F05 paid two rounds for). **Corrected the brief's premise**: it claims "the installer already probes the environment to pick front-ends" — it does not. `command -v` appears four times in harness-install.sh and never for a front-end CLI; what exists is detect_host (session env markers: who launched me) and a human-toggled picker (intent, not presence). Presence-detection is new code and a DIFFERENT evidence class — HOST_MARKERS R6 forbids ambient env vars precisely because they cannot prove *this run* was launched from that CLI, whereas "can this machine invoke it" is exactly what a PATH lookup answers. Recon: progress/E17-F04-scout.md. All three drill open questions answered in the spec (.harness/workers.json gitignored per the telemetry.jsonl precedent; regenerated every run, installer-owned; capability vocabulary = a CLOSED set of three tags each backed by a fact the harness already holds, rather than a taxonomy of what each CLI is good at). ADRs touched: none (specs/architecture.md absent — graceful degradation recorded, not silently omitted). — PAUSED at human gate
2026-08-12T00:00:00Z | doc-critic | E17-F04 | feature-spec checkpoint (agents/architect.md R12) run RETROACTIVELY — it was skipped at spec-ready because this repo's own .claude/agents/ carries no doc-critic shim, even though tests/test_install.sh asserts every INSTALLED target gets one (the harness does not dogfood its own checkpoint; seed an E99 fix). 14 findings, ALL legitimate — the four load-bearing mechanism claims were re-verified against harness-install.sh/tests before applying, per "verify the cause, not just the conclusion". The serious ones: (C1) R2's "byte-identical to a pre-feature target" was unachievable given the plan's own config key, and the tests.md case written to enforce it would have FAILED a correct implementation, pushing the Builder to either weaken the test or delete the key; (C3) write_escalation_arming — the precedent the plan tells the Builder to copy — opens with `agent_selected || continue`, and the roster must NOT filter by selection, a bug that is byte-identical to correct on any machine where everything is selected; (C4) `harness-frontend` was true of every row by construction (R5 already restricts entries to AGENT_KEYS), so it discriminated nothing and the vocabulary's defence rested on it — re-derived from SELECTED and renamed `harness-stamped`; (C5) T2's provenance comment placed above HOST_MARKERS would be absorbed by tests/test_agents_host.sh's POSITIONAL `sed -n '/^# PROVENANCE/,/^HOST_MARKERS="$/p'` range, silently disarming the R8 guard the spec keeps citing as its discipline model; (P2) R7 was a closure property only, satisfiable by an empty vocabulary and empty capability lists — the derivation rules the plan spends the most words on were required by no R-id; (P1) no seeded-vs-migrated byte-convergence rule for the new `workers:` block, the exact defect class this spec cites to justify complexity: complex. Also corrected two of my own factual errors: R6 is HOST_MARKERS' forbidden-by-NAME blacklist, not the session-markers-only bullet I quoted, and AGENT_KEYS is a fixed constant the human never toggles (the picker resolves SELECTED) — the latter is the confusion that produces C3. Still 12 R-ids, budget untouched: P2/P3 folded into R5/R7 rather than adding a 13th. Findings note: progress/E17-F04-doc-critic-feature-spec.md. Spec remains PAUSED at the human gate (PR #128).
2026-08-13T00:00:00Z | driller | E21 | /sdd-drill E21 (**amend** — E21 is `in-progress`, not `draft`, so a default run would have STOPPED; the human directed the amend, ids appended above max with no renumber and no re-flip). Re-drilled **E21-F05** after Codex round 1 on PR #129 returned three P1 findings — all verified against the cited files, none disputed — and the honest count to close them came to **13 R-ids against max_requirements: 12**, so the Architect stopped and reported the seams. SECOND time this epic's own budget rule has fired on this epic. All three P1s and every role-file contradiction lived in the **increment contract**, none in the doctrine: the spec told the BUILDER to open an increment's PR (agents/reviewer.md:34-38 uses that exact contradiction as its worked example and says to hard reject); it left no path for the Reviewer to approve a non-final increment (traceability rejects until every R-id has a passing test; the approve verdict sets the whole feature done); and it named `depends_on` as the seam rule, which sequences SIBLING board features and cannot delimit increments inside one feature. Cut follows the findings — same as F04 with the halves reversed (there the mechanism was sound and the doctrine burned). **F05 keeps its id**, narrowed to the doctrine document (~6 R-ids, docs/WORKFLOW.md only) — the human gate chose re-spec-in-place over seeding F06+F07, because the status vocabulary has no `withdrawn` value and a superseded `pending` feature would keep being selected by tools/next-task.mjs. **F06 seeded** for the increment contract (~7 R-ids, four role files, depends_on E21-F05). F06's brief opens with the question that decides whether it is built at all: the sibling-feature model already delivers budget-sized reviews with zero new machinery and stacking buys only wall-clock overlap — but under siblings no PR ever targets a non-default base, so E21-F04's merged pr-stack-guard.sh has no caller, the state it was withdrawn for once already. Board decision #3 (no `increments[]`) under pressure and deliberately NOT reversed — reversing it is a re-drill, not a spec edit (ADR-0001's protected surface). **ADR deltas: none** — the decomposition forces no new recorded decision, and the one live architectural question (does the lane earn its keep) is unsettled, so writing an ADR now would record a decision nobody has taken. Epic-level decision: **keep gated** — both features stay autonomous:false, consistent with every F03/F04/F05 precedent in this epic and because F06 edits four role contracts. Epic stays `in-progress` (already past `planned`; nothing to flip). Briefs: progress/inbox/E21-F0{5,6}.md. Doc-critic record carried forward from the superseded spec branch as progress/E21-F05-doc-critic-feature-spec.md.
2026-08-13T00:00:00Z | orchestrator | E99-F16 | seeded pending/autonomous (sdd:false fix lane, PR #130). The harness cannot spawn its OWN mandatory doc-critic checkpoint: agents/architect.md R12 requires it before spec-ready and tests/test_install.sh:580 asserts every INSTALLED target gets a .claude/agents/doc-critic.md shim, but this repo has none — so R12 is silently skipped on every spec the harness writes for itself while the installer test stays green. The usual installer-wiring gap runs source→consumer; this one is INVERTED, which is why it went unnoticed. Not hypothetical: run by hand afterwards the checkpoint returned 14 findings on E17-F04 (including a requirement whose test as written would have FAILED a correct implementation) and a not-gate-ready verdict on E21-F05 with two blocking findings reproduced by executing them. Brief: progress/inbox/E99-F16.md.
2026-08-14T00:00:00Z | orchestrator | E21 | re-drill merged (PR #131) after Codex round 1 on PR #129 returned three P1s, all verified, none disputed, and the honest count to close them came to 13 R-ids against a budget of 12. **E21-F05** kept its id and narrowed to the doctrine document (~6 R-ids, docs/WORKFLOW.md only); **E21-F06** seeded for the increment contract (~7 R-ids, four role files). PR #129 CLOSED as superseded — not reviewed, because a clean verdict under auto_merge would have authorized merging a spec carrying a ⛔ NOT BUILDABLE banner; its doc-critic record was carried forward first and the branch preserved. The re-drill itself took 4 review rounds (1 1 1 1, no repeated ids) and **two of them were spent on my own error**: Codex round 2 said the entry "has no `parked` blocker", which PRESUPPOSES the mechanism; I read it as "no park mechanism exists", concluded the board lacked the vocabulary, and un-seeded F06 without opening store/tasks.schema.json — where `parked` is defined, with featureBlockers() skipping parked features while preserving their route. Round 3 caught that; round 4 caught the stale epic-table row the un-seed/re-seed churn left behind. **E21-F06 is now seeded + PARKED** with reason and unblocked_by; verified `next-task.mjs --feature E21-F06` reports the park with `[route when unparked: architect]` and never selects it. Residual recorded in epic.md: a park means *not yet workable*, not *declined* — there is still no terminal disposition for a feature the project decides against, the same gap that made re-spec-in-place right for F05.
2026-08-14T00:00:00Z | builder+reviewer | E99-F17 | pending → done (PR #132). **main was RED and I made it red**: merging #131 parked E21-F06, and tests/test_feature_park.sh R7 required the repo's OWN LIVE BOARD to carry zero parked features — so the first legitimate park in this repo's history broke the suite that tests parking. Permanent suite coupled to mutable live state; same family as freezing a VERSION string or diffing DO-NOT-TOUCH files against main, both of which this repo has already paid for. Found by the E99-F16 Builder running the full suite, not by CI. R7 now DERIVES a parkless board from the live one (`--tasks "$T/noparks.json"`), keeping what the rule was for — the real selector over the real board's content — while surviving any number of real parks. Two corrections the mutation check forced: my first control was WRONG and failed on its first run (board-wide selection returns the first ACTIONABLE feature with an empty blocked list and never mentions a park that is not in its way, so the control now targets the parked feature); and the derivation was NOT load-bearing until R7 was strengthened — swapping the derived board back to the live one SURVIVED, because the assertion could not tell its two inputs apart. R7 now pairs them and that mutation dies.
2026-08-16T00:00:00Z | builder+reviewer+doc-critic | E99-F16 | in-progress → in-review → done (PR #134, rolled #135). The harness can now spawn its OWN mandatory doc-critic checkpoint — **proven by executing it**, not by asserting the shim exists. Three preconditions, any one alone leaving R12 unrunnable: the .claude/agents/doc-critic.md shim, `Task` on the source .claude/agents/architect.md (the caller could not spawn anything), and a doc-critic registration in the source opencode.json. Reviewer approved after 15 mutations in a throwaway worktree, but flagged it could not run the one check that mattered — its own shim has no Task — so the Doc-critic was spawned through the new shim for the FIRST EXECUTED R12 run in this repo, which returned 3 further findings, all applied. **Rounds 1–3 each found the SAME defect class in a different branch of one function** — an assertion naming something stronger than it checks: the claude branch (existence-only), the opencode-JSON branch (key-membership-only, plus a comment of mine falsely claiming that branch checked mode and prompt), and the file-based .opencode/agent fallback (a ZERO-BYTE file would have satisfied the whole suite once the pr-fixer xfail retired). Three in one function is this repo's threshold for rewriting the rule, so round 3 was escalated to a Builder sub-agent: register_ok's `case` is gone, replaced by one NORMALIZED RECORD per mechanism + one shared validator, with a coverage assertion that exits 2 (suite misconfigured) if a record field has no check. The Builder found a FOURTH instance in a failure MESSAGE ("a caller cannot spawn what is not a subagent", printed even for orchestrator where `primary` is correct). Round 4 clean, 0 findings — meaningful after three, not a quiet sample. Trend: 1 1 1 0, converging. **No VERSION bump**, measured not argued after Codex raised it on two separate PRs: installs from main and from the branch are BYTE-IDENTICAL (`diff -r -x ch tprev thead` → no output), so the same stamp is accurate and a bump would tell every downstream .harness/ it is stale when nothing differs. The underlying ambiguity is in AGENTS.md ("the `.claude/` glue" does not distinguish the glue the installer STAMPS from this repo's dogfooding config) and is UNFIXED — with the wrinkle that AGENTS.md is itself in HARNESS_BODY_PROSE, so clarifying when to bump would legitimately require a bump.
2026-08-16T23:05:00Z  E17-F04 spec-ready → in-progress — human gate cleared by explicit approval; the spec's Recorded decision 4 (no `path` field) carried into the build rather than re-derived.
2026-08-16T23:05:00Z  E17-F04 in-progress → in-review → in-progress (local review round 1 reject) — two unpinned guarantees, both TEST gaps with `harness-install.sh` correct as written: `generated_by` as a fixed literal (the invariant that justified shipping no JSON escaper) and R2's "nothing else added" measured as a gate-off vs gate-ON `comm`, blind to an artifact written on both paths. Each demonstrated by a mutation that left all 38 suites green.
2026-08-16T23:05:00Z  E17-F04 local review round 2 → APPROVED — both mutations killed by the case that names the rule; five round-1 kills replayed to confirm the `sandbox_run` refactor made nothing vacuous.
2026-08-16T23:05:00Z  E17-F04 PR #140 — Codex loop ran 6 rounds (1,2,1,1,1,0). Round 1 `#` inside a quoted scalar; round 2 nested `workers.options.roster` + a doubled-quote escape, at which point the decode-then-compare gate parse was INVERTED to a fail-closed whitelist rather than patched a third time; round 3 a read-only roster defeating R10's overwrite; round 4 the round-3 fixture's own `[ ! -w ]` guard under root — the CAP, handed to the human with needs-human; rounds 5-6 ran on the human's explicit authorization, closing a tab-indentation hole and coming back clean.
2026-08-16T23:05:00Z  E17-F04 round-3 premise check — Codex's stated mechanism ("`set -e` aborts the next install") holds under dash and zsh but NOT under bash, which is macOS `sh` and the shell this suite runs under: POSIX exempts a redirection error on a compound command from exiting, so the real local failure was SILENT — stale roster retained while `info()` reported a write. Tests were written to assert the OUTCOME rather than either shell's reaction.
2026-08-16T23:05:00Z  E17-F04 post-cap local review round 3 — caught what the loop had not: the round-2 whitelist rewrite (82 production lines) had never had local review, and its "a `#` opens a comment only when whitespace precedes it" rule was unpinned (relaxing `[[:space:]]+#` to `[[:space:]]*#` left 39 suites green while `roster: true#disabled` enabled the gate). Also overturned a disclosed residual: tools/run-tests.sh discards a green suite's stderr, so the uid-0 skip's announcement was invisible and the skip was a silent pass until the driver pinned it.
2026-08-16T23:05:00Z  E17-F04 process note — a tab-indent guard hand-written in the Orchestrator's own context was fail-OPEN (it let a tab-polluted section enable the gate) and its mutation survived; delegating to pr-fixer found both that and the mirror case (tab pollution AFTER the key, since the helper exited on first match). Recorded because the harness caught a defect introduced by not using the harness.
2026-08-16T23:05:00Z  E17-F04 trend verdict DISPUTED on the record — pr-round-trend.sh read NON-CONVERGING and recommended splitting; concentration showed the findings in ONE function (the YAML gate parse cascade) against a 311-line/4-file diff at tier `ok`. Judgement recorded against an advisory tool; the cascade was ended structurally, not by splitting.
2026-08-16T23:05:00Z  E17-F04 in-review → done — PR #140 merged at c85174b after a clean Codex round 6; VERSION 0.63.0 (bumped from 0.62.0 after PR #139/E99-F77 took 0.62.0 mid-loop — two distinct bodies at one stamped version is exactly what the contract forbids, and git could not see it).
2026-08-16T23:05:00Z  E17 pending → done — all five model-routing features are merged and Reviewer-verified.
2026-08-17T07:00:00Z  E21-F05 pending → spec-ready → in-progress — Architect re-verified all four of the brief's documented defects against main at 189af66 before speccing: every line number in the brief had gone stale, every defect was still present. 9 R-ids against a budget of 12 (the brief estimated ~6; the extra three are the doctrine statements the brief puts in scope plus defect #2 splitting into two distinct failure modes). Human gate cleared by explicit prior approval.
2026-08-17T07:00:00Z  E21-F05 doc-critic checkpoint (architect R12) — 7 findings, all applied, none declined. The load-bearing one: the "stated exactly once" assertions used `grep -c`, which counts LINES not occurrences, so two copies of a doctrine statement on one wrapped line would have satisfied every one of them. A spec about correcting false statements would have shipped a test unable to detect duplication.
2026-08-17T07:00:00Z  E21-F05 build — two mutations SURVIVED earlier forms of R3 and both were real defects. Asserting the outcome sentence as two independent presence checks passed even after the sentence was rewritten back to `main`, because a NEIGHBOURING sentence supplies the token; paragraph scope survived for the same reason. Only a per-SENTENCE conjunction kills it. This is the "expected value reachable by another path" failure, now the fourth occurrence in this repo.
2026-08-17T07:00:00Z  E21-F05 in-review → APPROVED (local round 1) — Reviewer ran each of the nine cases in ISOLATION against main's files: all nine fail, each with the symptom its own R-id names, so no test is decoration. It independently measured all three forms of R3's assertion and confirmed only the shipped one kills. It also corrected the Orchestrator's paraphrase of the T10 subheading rationale: the assertions are presence-based, so a truncated extraction fails SAFE (R9 goes red) rather than passing vacuously.
2026-08-17T07:00:00Z  E21-F05 PR #144 — Codex round 1 found one P2 with a TEST GAP behind it: the decision table's lane rows restated only the SAFETY half of the entry condition, dropping `verification.test_command`, three lines below prose claiming the subsection is the document's only statement of it. R1's assertion missed it because it anchors on literal tokens and the row PARAPHRASED ("safely shippable"). Fixed both rows to point rather than restate — pr-fixer extended past the row Codex named, since the refusal row paraphrased identically and would have left the new assertion trivially satisfiable — and proved the pointer half against a second, different paraphrase that dodges the banned phrase. Round 2 clean; merged at 9ffc3f2.
2026-08-17T07:00:00Z  E21-F05 in-review → done — VERSION 0.63.1 (PATCH: docs/ is in HARNESS_BODY_PROSE so the installed body changed and a bump is owed, but an upgrading target gains no key, command or callable behavior; the MINOR counter-argument was recorded in the plan and rejected by the Reviewer). E21 epic stays in-progress — F06 is parked behind a product decision.
2026-08-17T07:00:00Z  Session process note — the primary checkout moved branches under the Architect mid-run (a concurrent session drives it; two new worktrees appeared). Remaining E21-F05 work moved into its own worktree with every board write scoped by HARNESS_DIR, verified by the primary's tasks.json still reading `pending` while the worktree read `in-progress`.
2026-08-17T08:00:00Z  E21-F06 DECIDED NO (gate) — the stacked-PR lane does not earn its keep. Evidence: the sibling-feature model already delivers budget-sized reviews with zero new machinery; stacking adds only wall-clock overlap; and 0 of the last 100 PRs targeted a non-default base, so E21-F04's merged pr-stack-guard.sh has never fired despite having three call sites. The cost — a delimiter convention plus four role contracts that must agree — lands on the harness's most consistency-fragile surface. F06 is NOT to be built; its park now records a terminal decision rather than a wait. The board has no `declined` status, which is why a park is carrying it.
2026-08-17T08:00:00Z  E21-F07 seeded — deprecate the stacked-PR lane and reclaim its machinery: docs/WORKFLOW.md becomes a deprecation notice naming the sibling model, and tools/pr-stack-guard.sh plus its /sdd-pr-loop glue are REMOVED rather than left inert (a dead gate in a merge path is indistinguishable from a live one to the next reader; git preserves it if the decision is reversed).
2026-08-18T21:08:26Z  E24-F04 PR #149 repair checkpoint — audited four live Codex threads; two were already fixed, and `be8d160` closed read-only cleanup plus uid-0-safe rollback verification with attributed mutation kills. Reproduced and repaired the pre-existing inception fallback red baseline (`aa9eaaa`). Integrated PRs #148/#154/#153 without modifying their work, restamped E24-F04 as 0.69.0, and passed all 43 suites under dash; awaiting independent review and hosted gates before merge.
2026-08-18T21:25:00Z  E24-F04 independent review — one P2 reproduced red: delimiter-bearing absolute parent paths made the human-readable `diff -q` parser collapse exact blockers to tier roots. Added `thin_blocker_paths_ignore_parent_delimiters`; anchored both message forms on the exact comparison roots under `LC_ALL=C`; the new case and full umbrella suite pass. Corrected the stale `stub_tree` changelog name to `thin_prose_tier`.
2026-08-18T22:05:00Z  E24-F04 focused re-review repair — exact roots were still insufficient for a nested one-sided filename containing `: `. Replaced all `Only in` prose parsing with an independently derived, quoted/dotfile-aware path walk while retaining `diff` as the byte-identity authority. A silent-walk mutation first exposed a new fail-open coupling and converted the child; added a permanent doctored-installer control requiring tier-root fallback. Exact-path and fail-closed cases plus the complete umbrella suite pass.
2026-08-18T22:25:00Z  E24-F04 focused re-review follow-up — the independent walk now names nested file-vs-directory collisions before recursion (`docs/type-clash` pinned exactly). Touching test_umbrella.sh activated main's new shared-fence gate and exposed its inherited bare-backtick section slicer; migrated it to tests/lib/fence.awk. Full umbrella and change-size suites pass; final aggregate gate pending.
