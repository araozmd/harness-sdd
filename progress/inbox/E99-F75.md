---
feature: E99-F75 (carrier; absorbs E99-F76)
seeded_by: fixer
date: 2026-09-05
---
# Intent — assertion-hygiene batch: vacuous prose assertions + R11 corrected-line pins

## Problem
1) (F75) Bare single-word `grep -qi '<word>'` prose assertions pass on STALE files —
the word predates the feature (e.g. 'bare', 'never'). Two proven instances in the
finding; the class must be audited out of tests/.
2) (F76) test_adr_citation.sh R11 pins the presence of EXPLANATION, not the CORRECTED
LINE, and never covers scout.md / doc-critic.md at all: `agents/scout.md:48` Inputs
line still reads single-namespace `specs/adr/NNNN-*.md`; `agents/doc-critic.md:19`
plan-output table row likewise. Mutations reverting those lines stay green today.
Also: (a) the redundant post-loop `grep -qi 'bare' "$REV"` in test_adr_citation.sh
duplicates the in-loop per-file check — remove it; (b) the space-separated qualified
citation form `<ns> ADR-NNNN` (taught by reviewer.md/SPEC-FORMAT.md/architect.md's
`- platform ADR-0008` example) must appear in the two corrected files.

## Intended fix
- Correct scout.md's Inputs line and doc-critic.md's plan-output row to acknowledge
  BOTH ADR namespaces (`specs/adr/` and `specs/<product>/adr/`), including the
  space-separated `<ns> ADR-NNNN` mention.
- Extend R11's loop to include agents/scout.md and agents/doc-critic.md, pinning each
  file's CORRECTED LINE with the anchored two-token folded-newline form:
  `tr '\n' ' ' < "$f" | grep -qiE 'tokenA[^.]{0,60}tokenB'` — verified to FAIL on the
  pre-change text.
- Audit tests/*.sh for bare single-word `grep -qi '<word>'` PROSE-CONTRACT assertions
  (assertions pinning agent/doc prose, not tool output/fixtures); replace each with an
  anchored two-token form. Report any you judged out of class and why.
- Remove the dead post-loop assertion.
- Add the convention to agents/builder.md's Principles: a prose assertion anchors TWO
  co-occurring tokens across folded newlines and must be verified to fail on the
  pre-change blob.

## How to verify
Per corrected line: revert it in place, confirm the suite goes red naming that file,
restore (cp <file> <file>.mutbak / mv back). Full: sh tools/run-tests.sh --jobs 8.
