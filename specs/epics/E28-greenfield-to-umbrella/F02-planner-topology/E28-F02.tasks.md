# Planner topology output — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom, one at a
> time. Each task names the `R-id`(s) it satisfies. Check off when done.
> Pinned anchor strings live in `E28-F02.plan.md` §"Docs/platform content contract"; do not
> invent anchors the tests contract does not name.

- [ ] **T1** (R1, R2, R3, R4, R5, R6, R7, R10) — In `agents/planner.md`, add a
  `## Repo topology output` section after the architecture/ADR section. It states the
  trigger (`more than one deployable`), the two artifacts (one `repo-topology ADR` at
  `specs/adr/NNNN-<slug>.md`, with `specs/adr/` + `ADR-` and the allocation phrase
  `above the max existing ADR number` shipped verbatim, and a draft at
  `umbrella.manifest.draft.yaml` in the harness dir with one `repos:` entry per deployable
  carrying `path`/`init`/`test_command`/`delegate_cmd` and optional `scaffold_cmd`), the
  `exactly one deployable` no-op **naming both artifacts as not-written**, the inert rule
  (`never set` `umbrella.manifest`, never write `umbrella.manifest.yaml`; does not engage
  umbrella mode), the `DRAFT` + `inert` header and the `path:`/`own directory` base rule,
  `scaffold_cmd` as optional and `opaque` (runner attribution `F03`), and the single-writer
  rule naming `/sdd-drill` and `amend`. Do not reword existing sections.
- [ ] **T2** (R1, R2, R3, R4, R5, R6, R8) — In `harness-install.sh`, add the same step to the
  `cat > "$CMDDIR/sdd-plan.md" <<'EOF'` heredoc (`~:5279-5339`) using installed-mode
  `.harness/`-prefixed paths. Change nothing else in the installer.
- [ ] **T3** (R8) — Reconcile the source-mode `/sdd-plan` artifacts from that heredoc:
  run `./harness-install.sh --self --agents=claude,codex` (which strips `.harness/` via
  `self_transform`), or hand-edit `.claude/commands/sdd-plan.md` and
  `.agents/skills/sdd-plan/SKILL.md` to the same anchored step. **The selection must be
  claimant-preserving:** the shared skill unit is gated on `codex`/`opencode` (ADR-0003) and
  `self_reconcile` DELETES manifest-owned paths the run does not regenerate, so
  `--agents=claude` alone strips the committed Codex/OpenCode glue and R8 can never pass.
  All three surfaces must carry the pinned anchors; a divergence is the defect R8 exists to
  catch.
- [ ] **T4** (R6, R9) — In `umbrella.manifest.example.yaml`, add `scaffold_cmd: ""` to each
  entry with a comment naming it optional and `opaque`. Keep the four existing keys, every
  entry, and the file's existing comments otherwise unchanged (the `tests/test_umbrella.sh`
  R5 parser must stay green).
- [ ] **T5** (R9) — In `docs/UMBRELLA.md`'s `## Manifest reference`, add one sentence
  documenting `scaffold_cmd` as optional and opaque. Touch nothing else.
- [ ] **T6** (R1–R10) — Create `tests/test_planner_topology.sh` per `E28-F02.tests.md` and
  the plan's "Suite design" (POSIX `sh`, `chmod +x`, structurally-bounded spans with
  non-empty + line-count guards, positive controls before negatives/ordering, the
  install-free fixture plan run via `next-task.mjs --tasks/--config`, no `VERSION` literal).
- [ ] **T7** (release hygiene, no R-id) — Bump `VERSION` (**MINOR** — this is a capability:
  a new Planner output and a new manifest key, not a body fix; AGENTS.md) and add the
  `CHANGELOG.md` entry. Sync the `VERSION` literal in
  `tests/test_codex_native.sh` (`~:222`) to the new value (2026-09-16 lesson: a bare
  VERSION-file comparison is the permanent-suite anti-pattern).
- [ ] **T8** — Run `sh tools/run-tests.sh tests/test_planner_topology.sh` alone, then the
  full `sh tools/run-tests.sh`, then `./init.sh` from the repo root; ensure green before
  hand-off.
- [ ] **T9** — Run `sh tools/change-size.sh` before the PR hand-off (advisory; record the
  tier).
