# Greenfield single-repo path — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom, one at a
> time. Each task names the `R-id`(s) it satisfies. Check off when done.
> Pinned anchor strings live in `E28-F01.plan.md` §"Docs content contract"; do not invent
> anchors the tests contract does not name.

- [x] **T1** (R1) — In `docs/INSTALL.md`, add the section `## Starting from nothing (new product)`
  immediately before `## Bootstrap (first run)`. It documents, in order: create an empty
  directory → run `harness-install.sh` → `git init` → `/sdd-plan` → `/sdd-drill` → `/sdd-next`.
  Keep first-occurrence order of the five anchors within the section.
- [x] **T2** (R2) — In that section, state that the target may be an empty, non-git directory;
  that `git init` plus a commit is the human's step; the drift-guard consequence (`init.sh`
  skips on a non-git tree and warns on an untracked body — never fails); and ship the pinned
  sentence verbatim: `The installer does not create a git repository.`
- [x] **T3** (R3, R4) — Reconcile `docs/INSTALL.md`'s `## Bootstrap (first run)` (keep the
  heading text exactly): `/sdd-plan` is the greenfield front door and `/sdd-next` follows it;
  the seeded `E00-F01` task is what `/sdd-next` routes afterwards (test/lint/typecheck
  detection). Remove the framing that `/sdd-next` drafts the project's epics. Do not touch
  other INSTALL.md sections.
- [x] **T4** (R5, R6) — In `harness-install.sh`'s fresh-install `Next steps` block
  (`~:7186-7196`), name `/sdd-plan` before `/sdd-next`. Change only that block's text, and do
  **not** add any `git init` call (R6 pins that none exists). The upgrade branch is untouched.
- [x] **T5** (R5, R6, R7, R8, R9, R10) — Create `tests/test_greenfield.sh` per `E28-F01.tests.md`
  and the plan's "Suite design" (POSIX `sh`, `chmod +x`, empty/non-git fixture asserted first,
  install via `sh "$SRC/harness-install.sh"` with `--agents=claude --builder-backend=in-session
  --pr-loop=false`, sandboxed `HOME`/`CODEX_HOME`, layout + banner + `init.sh` + version
  assertions).
- [x] **T6** — Bump `VERSION` (PATCH — installed body banner text changed) and add the
  `CHANGELOG.md` entry. No R-id (release hygiene, AGENTS.md).
- [x] **T7** — Run `sh tools/run-tests.sh tests/test_greenfield.sh` alone, then the full
  `sh tools/run-tests.sh`, then `./init.sh` from the repo root; ensure green before hand-off.
- [x] **T8** — Run `sh tools/change-size.sh` before the PR hand-off (advisory; record the tier).
