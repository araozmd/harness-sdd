# Installer promotion — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom, one at a
> time. Each task names the `R-id`(s) it satisfies. Check off when done.
> Pinned anchor strings live in `E28-F03.plan.md` §"Content contract"; do not invent
> anchors the tests contract does not name. Reuse the existing cascade — never fork
> `install_one`, `manifest_upsert`, discovery, or `audit_one`.

- [x] **T1** (R1) — In `harness-install.sh`, add `FROM_MANIFEST=""` and the
  `--from-manifest <file>` / `--from-manifest=<file>` cases to the arg parser, next to
  `--umbrella`; an empty value (`--from-manifest=` or a missing argument) dies before any
  write. Add the usage-header line naming `--from-manifest <file>`, that it is
  umbrella-mode-only, and that it promotes a single install from the Planner's draft.
- [x] **T2** (R1) — Add the combination guards **before any write**: reject
  `--from-manifest` with `--self` (inside the existing `--self` block, beside its
  `[ "$DRY_RUN" = 0 ] || die` line), with `--standalone`, with `--thin`, and in single-target
  mode (the existing `if [ -z "$UMBRELLA" ]` block). In the `--umbrella` branch, refuse
  non-zero **only when `--from-manifest` is given** and
  `<umbrella>/.harness/.harness-version` is absent, naming the target — a plain `--umbrella`
  into a fresh dir must still work. (The existing umbrella-mode `--print-agents` rejection
  already covers that combination.)
- [x] **T3** (R2, R3, R4, R5) — Add the promotion helpers near `manifest_upsert`:
  `promotion_read_manifest` (parse the bounded draft shape; fail on missing/unreadable
  file, missing top-level `repos:`, a `repos:` with zero entries, or an entry with no
  `path`), `promotion_rebase_path` (resolve against the draft dir; require the parent to be
  the umbrella root and the basename to equal the key and match `^[a-z0-9-]+$`; print
  `./<key>` or fail), `promotion_existing_child` (an existing child path must be a directory
  with a `.git` entry, else fail naming the path, the reason, and the remedy), and
  `promotion_seed_manifest` (non-clobber insert of the entry under `repos:`, mirroring
  `manifest_upsert`'s scoping; a pre-existing key is preserved verbatim).
- [x] **T4** (R2, R3, R4, R8) — Add the promotion **pre-pass** in the `--umbrella` branch,
  after `UMB` is resolved and before the `DRY_RUN` block: when `FROM_MANIFEST` is set,
  validate the manifest and every entry — including a declared child that already exists but
  is not a git work tree — (no writes). Inside the existing `DRY_RUN` block, print the
  `promotion preview` (`would create child:` / `would git init` / `would run scaffold_cmd:`
  / `would re-base path:`) and still write nothing.
- [x] **T5** (R5, R6, R9) — Add the **apply step** after the `DRY_RUN` block and before
  `install_one "$UMB"`: for each entry, when the child directory is absent, `mkdir` it,
  `git init -q` it (local only; fail closed if `git` is unavailable), run its non-empty
  `scaffold_cmd` best-effort (warning naming the child and command on stderr, then
  continue), and seed the live root manifest. Then fall through to the unchanged cascade,
  so discovery installs the created children and the landing audit runs.
- [x] **T6** (R1) — Document promotion: add `## Promoting a single install to a
  coordinator` to `docs/UMBRELLA.md` (invocation, the re-base, the key==directory rule,
  local git only naming the remote work owed, `scaffold_cmd` opacity, the landing audit and
  exit `3`, the resolved dirty-tree behavior, and the promote-before-a-plain-cascade order)
  **and qualify** the existing Manifest-reference sentence that a key which is not a
  directory name "both work" — dispatch supports the split, promotion requires the aligned
  shape. Add a promotion subsection to `docs/INSTALL.md`'s `## Umbrella mode`. Pin every
  anchor in the plan's content contract.
- [x] **T7** (R1, R2, R3, R4, R5, R6, R7, R8, R9, R11) — Create `tests/test_promote.sh` per
  `E28-F03.tests.md` and the plan's "Suite design": POSIX `sh`, `chmod +x`, the env
  sandbox, positive controls before negatives/ordering, structural spans with line-count
  floors, and no `VERSION` literal.
- [x] **T8** (R1, R10) — Extend `tests/test_install.sh` with the wiring/single-target
  assertions: `--from-manifest` without `--umbrella` (and in single-target mode) is
  rejected non-zero writing nothing, as is an empty `--from-manifest=` value; a single
  install creates no child and no live manifest; a plain `--umbrella` cascade creates no
  child from a draft (it still writes its normal root manifest).
- [x] **T9** (release hygiene, no R-id) — Bump `VERSION` (**MINOR** — a new installer
  capability, not a body fix; AGENTS.md), add the `CHANGELOG.md` entry, and sync the
  `VERSION` literal in `tests/test_codex_native.sh` (`~:230`) to the new value (the
  2026-09-16 permanent-suite lesson).
- [x] **T10** — Run `sh tools/run-tests.sh tests/test_promote.sh` alone, then
  `sh tools/run-tests.sh` and `./init.sh` from the repo root; ensure green before hand-off.
- [x] **T11** — Run `sh tools/change-size.sh` before the PR hand-off (advisory; record the
  tier).
