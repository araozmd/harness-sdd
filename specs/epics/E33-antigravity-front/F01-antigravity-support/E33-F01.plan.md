# Native Antigravity invocation adapter, dynamic subagent dispatch, and documentation — Technical Plan

> Translates the .spec.md intent into design. Every decision cites the R-id(s) it serves.

## Stack & dependencies
- Antigravity Customization System (Progressive Skills, `define_subagent`, `invoke_subagent`).
- Shell scripts for test verification.
- No new external dependencies.

## Files to change (serves: R#)
| File | Change | R-id |
|---|---|---|
| `.agents/skills/sdd-*/SKILL.md` | Update `## Invocation adapter` to explicitly mention Antigravity `/sdd-*` alongside Codex and OpenCode | R1 |
| `agents/orchestrator.md` | Add Antigravity section explaining dynamic `define_subagent` recipe and role delegation | R2, R3 |
| `.agents/skills/sdd-next/SKILL.md` | Add note on Antigravity dynamic role dispatch | R2, R3 |
| `AGENTS.md` | Document Antigravity as a supported front end alongside Claude, Codex, and OpenCode | R4 |
| `README.md` | Update front-end support list to include Antigravity | R4 |
| `docs/WORKFLOW.md` | Add Antigravity slash-command and subagent details | R4 |
| `tests/test_antigravity_front.sh` | Add automated regression test asserting all skills document Antigravity and check syntax | R5 |

## DO NOT TOUCH
- `harness-install.sh` (do not reintroduce retired complex emitters)
- `.codex/` and `.claude/` files (must remain untouched and fully functioning)
- `store/tasks.schema.json`

## Approach notes
- Keep the skill updates clean and concise.
- For Antigravity, subagents are defined at runtime with `define_subagent(name="...", description="...", system_prompt="<agents/role.md content>", enable_write_tools=True)` and invoked with `invoke_subagent`.
