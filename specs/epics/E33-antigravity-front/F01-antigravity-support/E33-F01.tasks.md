# Native Antigravity invocation adapter, dynamic subagent dispatch, and documentation — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom, one at a time.

- [x] **T1** (R1) — Update `## Invocation adapter` across all `.agents/skills/sdd-*/SKILL.md` files to document Antigravity `/sdd-*` slash commands.
- [x] **T2** (R2, R3) — Update `agents/orchestrator.md` and `.agents/skills/sdd-next/SKILL.md` to document Antigravity dynamic subagent definition and dispatch pattern.
- [x] **T3** (R4) — Update `AGENTS.md`, `README.md`, and `docs/WORKFLOW.md` to document Antigravity support.
- [x] **T4** (R5) — Create `tests/test_antigravity_front.sh` to verify Antigravity skill definitions and syntax.
- [x] **T5** — Run `./init.sh` and `./tools/run-tests.sh` to ensure all tests pass.
