# Source `--self` OpenCode glue — Tasks

> Atomic, sequential steps. The Builder works these top to bottom, one at a time.
> Each task names the R-id(s) it satisfies. Check off when done.

- [ ] **T1** (R1) — In `self_install` (`harness-install.sh:7661`) change the default selection
      to `claude,codex,opencode` (and `--agents=all` ⇒ all three), and admit `opencode` in the
      selection guard at `:7670-7672` while keeping `gemini`/`antigravity` refused. Update the
      three Claude/Codex-only Builder-facing strings to name `opencode`: the `:7662` header
      comment, the `:7667` undetected-host `die`, and the `:7671` unsupported-key `die`. Keep
      the `--self` arg guards, `--umbrella`/`--print-agents` refusals, and `--agents=claude`,
      `--agents=codex`, `--agents=opencode` narrowing unchanged.
- [ ] **T2** (R4) — Before `install_one "$_si_tmp"` (`:7694`), set
      `OPENCODE_PARALLEL_OVERRIDE=false` beside the existing `BUILDER_BACKEND_OVERRIDE` /
      `PR_LOOP_OVERRIDE` forcing, so the temp install can never emit
      `.opencode/command/sdd-fix-parallel.md` or a marker. Confirm the gated command is absent
      from both the temp output and the source.
- [ ] **T3** (R1, R7, R8) — In the temp-install → source-copy loop (`:7696-7701`), include
      `opencode.json`, `.opencode/command/*.md`, and `.opencode/agent/*.md`, applying
      `self_transform` to each. Do not add a second transform path.
- [ ] **T4** (R3) — Verify the `pr-fixer` direction both ways: with the repo's
      `pr_loop.enabled: true`, `--self` writes `.opencode/agent/pr-fixer.md`; flipping the gate
      to false reclaims a pristine file and leaves a foreign/edited/symlinked one untouched
      with a warning.
- [ ] **T5** (R5) — Extend `self_reconcile`'s `allowed()` (`:7597-7603`) to admit
      `opencode.json`, `.opencode/command/*.md`, and `.opencode/agent/pr-fixer.md` as per-file
      ownership units; never own `.opencode-parallel`. In the `self_install` arming loop
      (`:7714-7725`) skip front-ends without a per-role file (`*) continue`) so `opencode` is
      not published as `unstamped`; its verdict comes from `write_escalation_arming`.
- [ ] **T6** (R1, R2, R7) — Bring the committed `opencode.json` under generation: write the
      emitter's source-transformed bytes into it (simplest: remove the stale hand-maintained
      file, run `--self`, commit the created file) and confirm it defines the six standard
      roles **and** `builder-heavy`, with `"instructions": ["AGENTS.md"]` and
      `{file:./agents/<role>.md}` prompts. Do not weaken the foreign guard to force adoption.
- [ ] **T7** (R4, R8) — Confirm the generated `.opencode/command/` set is exactly the shared
      command set plus `sdd-test-concurrency.md` (plus `sdd-pr-loop.md` while the gate is on),
      that each file's bytes equal the source `.claude/commands/<cmd>.md` body, and that
      `sdd-fix-parallel.md` is absent.
- [ ] **T8** (R5, R10) — Confirm the reconciler preserves a foreign/edited/symlinked OpenCode
      file with a warning and leaves it unclaimed; confirm the `.agents/skills/<cmd>/SKILL.md`
      units still carry F01's host-neutral adapter (both `$sdd-*` and `/sdd-*`, no `/skills`)
      with `{codex, opencode}` claiming — F02 forks no body.
- [ ] **T9** (R1, R2, R3, R4, R5, R7, R8) — Extend `tests/test_self_mode.sh`: the fixture
      does **not** copy `.opencode` (absent by construction) and inherits `opencode.json` via
      the `"$SRC"/*` glob, so **delete the inherited `opencode.json`** before the generation
      assertions (a copied fixture inherits the artifact); assert `builder-heavy` present,
      commands present, gated command absent when `--with-opencode-parallel=true` is passed
      and byte-identical when it is not, pr-fixer follows the gate, body equality with
      `.claude/commands/` (with a length floor), no `.harness/` prefix, OpenCode ownership
      guards, the `opencode=neither`/`blocked` arming verdict, and
      `.opencode`/`opencode.json` idempotence.
- [ ] **T10** (R6) — Extend `tests/test_self_drift.sh`: `glue_diff` compares `opencode.json`
      and `.opencode/`. **The green fixture must delete the inherited `opencode.json` and any
      copied `.opencode/` before `--self` — this is non-negotiable, it is the falsifiability
      condition** (else the diff can go green on a stale committed artifact). Add an
      OpenCode-specific gate-red arm (mutate `gen_opencode_json`, re-run `--self`, assert the
      diff surfaces it).
- [ ] **T11** (R1, R9, R11) — In `tests/test_codex_native.sh`: remove `opencode` from the
      `ok=False` self-selection/host cases at `:51-53` and add positive
      `--self --agents=opencode` / `HARNESS_HOST_AGENT=opencode` cases (`gemini`/`antigravity`
      stay negative); pass explicit `--agents=claude,codex` to the arming runs at `:57`, `:61`,
      `:101` and `:113` so the Codex model/arming assertions keep their meaning (bare `--self`
      now adds `opencode=neither` and forces `blocked`); add the source-layout host resolution
      assertion (static always, live `opencode` probe gated on `command -v opencode`); sync the
      `VERSION` literal at `:230`.
- [ ] **T12** (R11) — Update `docs/INSTALL.md` (source/`--self` section — name OpenCode glue,
      delete the "does not emit OpenCode glue" claim), bump `VERSION` by a MINOR, and add the
      `CHANGELOG.md` entry.
- [ ] **T13** (R2, R9) — In `tests/test_source_shims.sh`: drop `.opencode` from the absence
      loop (`:90-104`), delete both `KNOWN_GAPS` lines (`:76-77` — `builder-heavy:opencode` and
      `pr-fixer:opencode`) so the general registration rule asserts both are registered
      (`builder-heavy` via `opencode-json`, `pr-fixer` via `opencode-file`), and refresh the
      stale comments at `:9`, `:82-84`, `:105` and `:306-307` that describe OpenCode
      registration as `opencode.json`-only or `.opencode/` as absent.
- [ ] **T14** — Write/adjust tests per `E31-F02.tests.md`.
- [ ] **T15** — Run `./init.sh` + the project test command; ensure green before hand-off.
