# Source `--self` OpenCode glue — Tasks

> Atomic, sequential steps. The Builder works these top to bottom, one at a time.
> Each task names the R-id(s) it satisfies. Check off when done.

- [ ] **T1** (R1) — In `self_install` (`harness-install.sh:7282`), admit `opencode` in the
      selection guard (remove the `claude|codex`-only `die`; accept the normal claimant set).
      Keep the default selection (currently `claude,codex`) unless the operator passes
      `--agents`.
- [ ] **T2** (R1, R4) — In the temp-install → source-copy loop (`~:7340`), include
      `opencode.json`, `.opencode/command/*.md`, and `.opencode/agent/*.md`, applying
      `self_transform` to each.
- [ ] **T3** (R4) — Before `install_one "$_si_tmp"`, force the OpenCode capability gate off
      for the temp install so no `.opencode-parallel` marker and no
      `.opencode/command/sdd-fix-parallel.md` can be emitted. Confirm the gated command is
      absent in both source and temp output.
- [ ] **T4** (R5) — Extend `self_reconcile`'s `allowed()` (and the unit grouping if needed) to
      admit and guard `opencode.json`, `.opencode/command/*.md`, and
      `.opencode/agent/pr-fixer.md`; explicitly never own `.opencode-parallel`.
- [ ] **T5** (R5) — Confirm the reconciler preserves a foreign/edited/symlinked OpenCode file
      with a warning and leaves it unclaimed; do not let it publish the temp install's
      optimistic arming verdict for OpenCode.
- [ ] **T6** (R2) — Regenerate and commit the source `opencode.json`; assert it defines the
      six standard agents **and** `builder-heavy`, with `"instructions": ["AGENTS.md"]` and
      `{file:./agents/<role>.md}` prompts.
- [ ] **T7** (R3) — Verify the `pr-fixer` direction both ways: with the repo's
      `pr_loop.enabled: true`, `--self` writes `.opencode/agent/pr-fixer.md`; flipping the
      gate to false reclaims a pristine file and leaves an edited one.
- [ ] **T8** (R4) — Update `.gitignore` to ignore `.opencode-parallel` (and any OpenCode
      scratch) while never ignoring `.opencode/command/` or `.opencode/agent/`.
- [ ] **T9** (R1, R2, R3, R4, R7) — Extend `tests/test_self_mode.sh`: fixture copies
      `.opencode`; assert `builder-heavy` present, commands present, gated command and marker
      absent, pr-fixer follows the gate, and no `.harness/` prefix survives.
- [ ] **T10** (R6) — Extend `tests/test_self_drift.sh::glue_diff` to compare
      `opencode.json` and `.opencode/`.
- [ ] **T11** (R8) — Add the OpenCode leg to `tests/test_agents_host.sh` asserting `/sdd-next`
      and the agents (including `builder-heavy`) resolve from the source repo.
- [ ] **T12** (R1, R8) — Update `docs/INSTALL.md` (source/`--self` section), bump `VERSION`,
      and add the `CHANGELOG.md` entry.
- [ ] **T13** — Write/adjust tests per `E31-F02.tests.md`.
- [ ] **T14** — Run `./init.sh` + the project test command; ensure green before hand-off.
