# OpenCode claims the shared `.agents/skills` units — Tasks

> Atomic, sequential steps. The Builder works these top to bottom, one at a time.
> Each task names the R-id(s) it satisfies. Check off when done.

- [ ] **T1** (R1, R2, R3) — In `harness-install.sh`, widen `skill_unit_claimed()` (`~:6519`)
      to `agent_selected codex || agent_selected opencode`. Do not touch either caller.
- [ ] **T2** (R1, R2, R3) — Trace the §7 reclaim path (`~:7084-7090`) and confirm the
      deselect-last transition calls it with the new predicate; if reclaim is gated on a
      prior-selection check that excludes opencode, extend that check to the shared-claimant
      set (not a second per-call `agent_selected`).
- [ ] **T3** (R4) — Replace the Codex-only adapter in `gen_skill_body()` (`~:6535`) with the
      host-neutral wording: Codex `$sdd-<name>` with trailing args, OpenCode `/sdd-<name>`,
      `$ARGUMENTS` mapping, no `/skills` instruction for OpenCode.
- [ ] **T4** (R5) — In `gen_skill_body()`, after the adapter, append the OpenCode capability
      precondition to the `sdd-fix-parallel` unit only: under OpenCode, read
      `.harness/.opencode-parallel`; if it is not `supported`, stop before spawning workers
      and report `/sdd-test-concurrency` then `--with-opencode-parallel=true`.
- [ ] **T5** (R1) — Update `specs/adr/0003-one-shared-skill-unit-per-command.md`: claiming set
      `{codex, opencode}`, Antigravity retired, OpenCode the new reader; keep the decision text
      and the ledger-name note intact.
- [ ] **T6** (R4, R5) — Update `docs/INSTALL.md`, `docs/HARNESS.md`, and the OpenCode row in
      `README.md` to describe the shared surface, the host-neutral adapter, and the gate. Also
      update the installer's own user-facing strings: the §5d `ok` success line (`~:6786`) and
      the install-manifest text (`~:4089-4102`) that say `.agents/skills/` is "read by Codex".
- [ ] **T7** (R1, R2, R3, R6) — Add the mixed-selection cases to `tests/test_install.sh`:
      `--agents=opencode` installs units; `--agents=codex,opencode` then deselection of
      `opencode` leaves units; deselection of the last claimant reclaims pristine units;
      `--agents=codex`-only bytes/behavior unchanged.
- [ ] **T8** (R4, R6) — Add the adapter assertion to `tests/test_codex_native.sh` **and** update
      its frozen golden fixture (`:189`) that byte-compares generated `.opencode/command/*` and
      `.agents/skills`, which the adapter change legitimately alters. A green run on the stale
      golden would mean the comparison is no longer covering the bytes it names.
- [ ] **T9** (R5) — Add the `sdd-fix-parallel` precondition assertion to
      `tests/test_installer_toggles.sh`.
- [ ] **T10** (R6) — Prove byte-equality: generate the shared units under `--agents=codex` and
      `--agents=codex,opencode` and assert each `SKILL.md` is identical.
- [ ] **T11** — Write/adjust tests per `E31-F01.tests.md`.
- [ ] **T12** — Run `./init.sh` + the project test command; ensure green before hand-off.
