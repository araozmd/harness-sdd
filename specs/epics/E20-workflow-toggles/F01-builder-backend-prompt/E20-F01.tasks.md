# `execution.builder.backend` prompt in the installer picker — Tasks

> Atomic, sequential, independent steps. Work top to bottom, one at a time. Each task
> names the R-id(s) it satisfies. Check off when done.
>
> **Before T1:** run `./init.sh` and confirm it exits 0.

## Read path (config values in)

- [x] **T1** (R7, R8, R11, R12) — In `harness-install.sh`, add
      `_cfg_execution_builder_value <file> <key>`: print the `execution:` → `builder:` →
      `<key>` scalar (unquoted, comment-stripped), empty when the file, the section or the
      key is absent. Copy the section-scoped `awk` shape of `_cfg_pr_loop_value`, one nesting
      level deeper. Place it beside the other `_cfg_*` readers.

## Decision path (pure, unit-testable)

- [x] **T2** (R2) — Add `builder_backend_answer <answer> <current>`: prints the resolved value
      and nothing else. Empty answer ⇒ `<current>`; `1` or `in-session` ⇒ `in-session`; `2` or
      `delegate` ⇒ `delegate`; anything else ⇒ `<current>`. No `read`, no writes, no globals.
      **Write it so `sed -n '/^builder_backend_answer() {$/,/^}$/p'` yields a complete,
      sourceable definition** — the opening line must be exactly `builder_backend_answer() {`
      and no line inside the body may be exactly `}`.
- [x] **T3** (R1, R3) — Add `builder_backend_prompt <current>`: print a two-option menu **to
      stderr** naming `<current>` as the default and stating that `delegate` requires
      `execution.builder.delegate_cmd`; `read -r` one line; print
      `builder_backend_answer "$line" "$current"` on stdout. No loop, no re-ask, no other
      logic.
- [x] **T4** (R1, R4, R6, R12) — Add `resolve_builder_backend <target>`: set the global
      `BUILDER_BACKEND` from, in order — a non-empty `$BUILDER_BACKEND_OVERRIDE`; else an
      interactive TTY (`[ -t 0 ]`) ⇒ `builder_backend_prompt "$current_effective"`; else the
      current effective value. `current_effective` = `_cfg_execution_builder_value
      "$H/harness.config.yaml" backend` when that file exists, else `in-session`. Also record
      how it resolved (override / prompt / unchanged) for T9's report line.

## Write path

- [x] **T5** (R7, R8) — Add `set_builder_backend <config-file> <value>`: `awk` scoped to the
      top-level `execution:` section's `builder:` mapping, rewriting **only the value token**
      on the `backend:` line and preserving indentation plus any trailing comment; write via
      `"$f.tmp"` + `mv`, like `seed_pr_loop_optin`. Leave every other line byte-for-byte
      intact.
- [x] **T6** (R9) — Add one entry to `migrate_config`'s table for the `execution:` block:
      when no line matches `^execution:[[:space:]]*(#.*)?$`, append at EOF a comment header
      plus `execution:` / `  builder:` / `    backend: in-session` / `    delegate_cmd: ""`.
      When the header is present, do nothing. Do **not** insert a nested key into an existing
      `execution:` block. Do not modify any existing entry.

## Argument surface

- [x] **T7** (R4, R5) — In the arg-parsing section, seed
      `BUILDER_BACKEND_OVERRIDE="${HARNESS_BUILDER_BACKEND:-}"` beside `AGENTS_OVERRIDE`, and
      parse `--builder-backend=*` and `--builder-backend <value>` (an empty value ⇒ no
      override). Immediately after the parse loop — before target resolution and before any
      `install_one` — `die` non-zero when a non-empty override is neither `in-session` nor
      `delegate`, naming the offending value and both legal values.

## Wiring into `install_one`

- [x] **T8** (R1, R6) — Call `resolve_builder_backend "$TARGET"` immediately after
      `resolve_agents "$TARGET"`, so both questions are asked back to back before the body
      copy.
- [x] **T9** (R7, R8, R10, R11, R12) — Immediately after the existing config
      seed/preserve + `migrate_config` block, add the apply step: skip the writer entirely
      when the resolved value equals the config's current value; otherwise call
      `set_builder_backend`. Then, when the resolved value is `delegate` and
      `_cfg_execution_builder_value … delegate_cmd` is empty, print a warning naming
      `execution.builder.delegate_cmd` and the config's path (never abort). Finally print
      exactly one report line naming the resolved backend and how it resolved.

## Documentation

- [x] **T10** (R13) — Update the `harness-install.sh` header comment and the summary heredoc
      to document the second question, `--builder-backend=` / `HARNESS_BUILDER_BACKEND`, both
      legal values, and the `delegate_cmd` warn-and-proceed ruling. **Do not write the word
      "drift" anywhere in `harness-install.sh`** — say "diverge".
- [x] **T11** (R13) — Add a `docs/INSTALL.md` subsection covering the same, plus the fact that
      re-running the installer is how the value is changed later.

## Tests + release

- [x] **T12** (R14) — Create `tests/test_installer_toggles.sh` per `E20-F01.tests.md`: one
      `hrun`-style gateway that runs the installer under `env -i` with sandboxed `HOME` and
      `CODEX_HOME`, a self-cleaning `mktemp -d` + `trap`, and every check in the matrix.
      **No frozen `VERSION` literal, no hard-coded previous version, no `git diff` against
      `main` anywhere in the suite** — these are permanent-suite anti-patterns this repo has
      hit three times. Do not touch `tests/test_install.sh`.
- [x] **T13** (R14) — Append `&& sh tests/test_installer_toggles.sh` to
      `verification.test_command` in `harness.config.yaml` (and extend its trailing comment).
- [x] **T14** (R15) — Bump `VERSION` `0.41.0 → 0.42.0` and add the matching `## [0.42.0]`
      entry to `CHANGELOG.md`, naming `--builder-backend` / `execution.builder.backend` so
      the suite's marker check has something durable to find.
- [x] **T15** — Run `./init.sh` and the full `verification.test_command`; ensure green before
      hand-off. Then walk the Reviewer's by-hand interactive checklist in
      `E20-F01.tests.md`.
