# Three supported front ends and native Codex workflow — Tasks

Work only after baseline completion and human spec approval, on a new feature branch. The Orchestrator owns store transitions; the Builder owns this checklist. Each step is bounded to the plan's paths.

- [ ] **T1** (R1–R4, R11) — Capture a portable v0.78.1 emitted fixture before changing the installer, with generator commit/version and checksums; add fresh, retired-only, mixed-selection, legacy-no-selection, orphan-metadata, and edited/symlink upgrade cases to `tests/test_frontend_retirement.sh`.
- [ ] **T2** (R1, R2) — Split active selection from prior cleanup authority in `harness-install.sh`; implement three-host registry, explicit-selector errors, persisted-selection migration, and host/worker/model table removal.
- [ ] **T3** (R3, R4) — Implement bounded pre-upgrade pristine cleanup and corresponding active drift ownership; verify Codex skill units, companions, custom GEMINI text, and user edits survive.
- [ ] **T4** (R5, R6) — Extend `--self` selection, shared-emitter path transform, and separate Codex model harvesting while preserving Claude models and consumer seeds.
- [ ] **T5** (R7) — Extend source manifest generation, gated/deselected pristine cleanup, and existing drift tests; prove clean regeneration from a fixture with copied outputs removed first.
- [ ] **T6** (R8) — Add narrow Codex invocation adaptation and selected-host completion advice, with argument and literal-shell preservation assertions in `tests/test_codex_native.sh`.
- [ ] **T7** (R9, R10) — Emit/stamp/reclaim the gated Codex pr-fixer; replace the Codex in-session exception and add narrow fresh-role handoff guidance, preserving PR-loop policy.
- [ ] **T8** (R1–R12) — Update affected existing suite assumptions and retained-host golden comparisons; run focused retirement, native Codex, self/drift, model, and PR-loop tests.
- [ ] **T9** (R12) — Replace canonical root AGENTS.md with a fresh concise entrypoint preserving the plan’s required rules, update current README/docs/config comments, remove pristine source GEMINI.md, append migration/release notes, and set VERSION to 0.79.0 after confirming baseline 0.78.1 exists.
- [ ] **T10** (R5–R12) — Regenerate source glue through `--self`, run it again, and record idempotence and unchanged seed/Claude-model evidence.
- [ ] **T11** (R5, R8–R10) — Run the local CLI config/discovery probe and bounded fresh-context native workflow smoke described in `.tests.md`; record version, command, outcomes, and any explicit limitation under `progress/<run>/`.
- [ ] **T12** (R1–R12) — Run `./init.sh` and `sh tools/run-tests.sh`; hand the independent Reviewer this contract, changed-path list, test output, smoke evidence, and any preserved-file diagnostics. Do not mark the feature done yourself.
