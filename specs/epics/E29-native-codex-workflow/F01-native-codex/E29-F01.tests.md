# Three supported front ends and native Codex workflow — Test Contract

## Traceability

Case names below label shell assertions; all are required unless explicitly classified as optional environment-dependent evidence. Final statuses below reflect independent Reviewer verification of checkpoint `6645d5d`.

| R-id | Concrete test | Type | Status |
|---|---|---|---|
| R1 | `tests/test_frontend_retirement.sh::active_registry_and_explicit_rejection`; existing host/roster/model suites | Integration | Passed — Reviewer verified |
| R2 | `tests/test_frontend_retirement.sh::recorded_selection_migration` | Upgrade | Passed — Reviewer verified |
| R3 | `tests/test_frontend_retirement.sh::legacy_owned_cleanup_and_preservation` | Upgrade | Passed — Reviewer verified |
| R4 | `tests/test_frontend_retirement.sh::post_retirement_drift_ownership`; existing init drift suite | Integration | Passed — Reviewer verified |
| R5 | `tests/test_codex_native.sh::self_selection_and_paths`; existing self-mode suite | Integration | Passed — Reviewer verified |
| R6 | `tests/test_codex_native.sh::source_model_and_collision_preservation`; model-routing suite | Integration | Passed — Reviewer verified |
| R7 | `tests/test_self_drift.sh::codex_manifest_and_idempotence`; `test_self_mode.sh` | Integration | Passed — Reviewer verified |
| R8 | `tests/test_codex_native.sh::invocation_arguments_and_completion` | Behavioral output | Passed — Reviewer verified |
| R9 | `tests/test_codex_native.sh::pr_fixer_gate_and_ownership`; existing PR-loop suite | Integration | Passed — Reviewer verified |
| R10 | `tests/test_codex_native.sh::native_handoff_contract`; bounded CLI smoke below | Contract + local execution | Passed — Reviewer verified |
| R11 | `tests/test_codex_native.sh::retained_host_and_seed_regression`; affected existing suites + configured full run | Regression | Passed — Reviewer verified |
| R12 | `tests/test_codex_native.sh::release_and_current_usage_contract`; Reviewer checks exact current support/invocation sections against emitted artifacts | Documentation contract | Passed — Reviewer verified |

## Required hermetic cases

### Frozen upgrade evidence (R1–R4)

Before implementation changes, create `tests/fixtures/frontend-v0.78.1/` from the recorded baseline installer. Store only fixture-owned emitted artifacts and required prior body/command/config evidence, with a README identifying baseline commit/version, generation command, and checksums. No credentials, real project data, home directories, or mutable `/tmp` references. CI must not fetch a Git tag or regenerate old output using the new installer.

Exercise v0.78.1 `gemini,antigravity,codex`, retired-only, each retired host individually, all-five, a version-stamped pre-selection target, and orphan `.agents` without version stamp. Include pr-loop on/off shapes and resolved Gemini model roles. Check that explicit retired CSV/env/host requests fail before tree checksums change; `all` yields exactly three supported keys; retired ambient host markers produce existing undetected fallback behavior. Retired-only recorded selection stops with recovery guidance and no writes; explicit `--agents=codex` then upgrades it successfully. Mixed implicit upgrade persists surviving keys and is idempotent. Legacy no-selection grants no Codex/global-prompt removal authority.

For each relevant owned path, include pristine, user-edited, foreign same-name, and symlink variants (file and parent directory). Assert both content and link targets survive protected cases. Assert removal of pristine Antigravity rules/personas/workflows (including prior PR-loop workflow), stamped Gemini model roles, and only the GEMINI pointer block. Preserve adjacent GEMINI user text, unrelated siblings, Codex skills and policy companions when Codex remains selected, and unrelated global prompt sentinels. Explicitly exercise `antigravity,codex → claude`: pristine formerly shared skill units are reclaimed because no active claimant remains, while edited units and their policy companions survive. Corrupt/missing/symlinked stamps must not authorize deletion. Check warnings name preserved paths. After migration, enumerate `harness-owned-paths.sh` output: preserved retired files are absent, active proven Codex roles/skills remain, and installed init does not claim retired custom files as body drift.

### Source regeneration (R5–R7)

Use copied source fixtures with generated outputs and manifests removed **before** the first run when proving creation. Cover default both, explicit Claude-only, Codex-only, both, `all`, valid/unsupported `host`, and rejected OpenCode/retired self selections; error paths must leave the tree unchanged. Verify seven standard Codex role names plus the gated eighth, required TOML keys, complete skill+policy units, source-root references, and installed/umbrella references remaining `.harness/`-rooted.

Set distinct existing Claude and Codex model choices; omit one Codex model; verify each survives unchanged with no cross-host aliases. Preserve seed config byte-for-byte except separately approved documentation comment edits. Test model-only source override versus arbitrary edited role, edited skill, foreign same-name file, and symlink collision. Prove the first-time emitter does not clobber or claim protected artifacts.

Compare a second unchanged self run byte-for-byte, including manifest. Mutate or remove each class of owned Codex output and confirm existing source drift diagnostics detect it. Remove/prune a pristine gated role and skill on gate-off and previously managed host outputs on deselection; preserve edited leftovers with diagnostics and exclude them from current ownership. Confirm unrelated files under `.codex` and `.agents` remain untouched.

### Invocation, PR gate, and retained hosts (R8–R12)

Verify selected-host completion text for fresh Claude-only, Codex-only, OpenCode-only and mixed installs. Inspect actual continuation examples in sdd-new, sdd-plan, sdd-drill, sdd-next, sdd-fix, sdd-fix-parallel and gated sdd-pr-loop. Codex uses `$sdd-*` for invocation; argument text reaches existing `$ARGUMENTS` guidance; executable shell paths/expressions remain byte-identical to their canonical counterparts. Portable spelling may remain only where clearly labeled as a canonical name, never as the next command the user is told to enter.

PR-loop gate matrix: off → on → off, on → Codex deselected, source on/off, edited/foreign/symlinked pr-fixer. Assert required native TOML shape, stamp creation, fresh-role skill dispatch, pristine reclaim and preservation warnings. Do not count a retained customized role as installer-generated active glue. Assert Codex's prior in-session exception is absent from the active dispatch span; contract assertions must read that span rather than match a negative example elsewhere.

Compare Claude and OpenCode artifact bodies and resolved models against prechange fixtures, allowing only explicitly required active-support/PR-fixer shared prose and advice changes. Preserve default backend, all-inherit consumer model settings, fresh PR-loop off, supported host detection and escalation routing/arming policy. Explicitly assert that combined source generation with inherited Codex is UNARMED and Claude-only generation retains its previous verdict; do not require the combined verdict to equal the old Claude-only one. Current documentation checks target current tables/usage sections, excluding historical records; verify VERSION 0.79.0, baseline preservation, and a release note explicitly describing removed selectors and combined-source arming. Assert the regenerated canonical AGENTS.md preserves each required entrypoint rule from the plan, the installer copies those canonical bytes to consumers, root pointers resolve to that body, self-generation leaves canonical bytes intact, and source GEMINI.md is removed only after baseline preservation.

## Local Codex configuration and discovery probe

Environment-dependent supplemental evidence; hermetic tests above must pass without credentials, network, or inference.

Use a temporary project and isolated `CODEX_HOME`, never change real home config. Record `codex --version`. For a compatible installed CLI, trust only the fixture, run `codex -C <fixture> doctor --json`, and inspect **config-load status**, not overall exit (missing auth/connectivity can make the latter nonzero). Negative control: replace `developer_instructions` with an invalid key in a fixture-only role; require the config diagnostic to identify that role, then restore it. Check actual discoverable skill names through the CLI's available discovery surface. If the CLI lacks the needed diagnostic/discovery interface, record that specific limitation rather than claim native loading from a generic TOML parse.

## Bounded native end-to-end smoke

Run only in a disposable local git fixture, with the available authenticated Codex host. No pushes, remote PR creation, merges, or PR-loop polling. Limit the exercise to **one toy feature**, **one Architect→Builder→Reviewer cycle**, **one corrective Builder round maximum**, and **10 minutes wall time**; stop and report if the boundary is reached. Use the actual installed host's delegation controls; do not hardcode this application's tool schema into generated files.

1. Discover/invoke `$sdd-next` with a pending non-autonomous toy feature and an intent that requests one harmless text artifact. Confirm init runs and Architect returns spec paths through a fresh context. Confirm the host stops at `spec-ready`, with no implementation artifact written.
2. In this disposable fixture only, the test driver records explicit approval and resumes. Confirm a separate Builder writes the artifact, then a fresh Reviewer runs fixture init/tests and records its verdict. Capture role identity/session evidence and file handoff paths, not just a host's claim that it changed roles.
3. Confirm the independent verdict precedes `done`. Separately run a failing fixture init and confirm no downstream role or implementation work starts.
4. Exercise native pr-fixer loading/dispatch with a synthetic local handoff that authorizes no external action; actual GitHub-loop behavior remains covered by existing stubbed PR-loop tests.

Record commands, CLI version, elapsed bound, progress paths, observed gate pause, role separation, and outcome. If authentication, delegation, or execution is unavailable, record **not executed** and the precise reason; parser success is not an end-to-end pass. A behavioral failure is a defect to resolve before acceptance; environmental unavailability is an explicit Reviewer limitation and must appear in the delivery summary.

## Reviewer gate

Run `./init.sh` and configured `sh tools/run-tests.sh`. Lint/typecheck commands are empty in current config. Review any preserved-file warning against the fixture intent. One passing executable/contract case is required for every R-id; report environment-dependent evidence separately. Only the independent Reviewer may accept the feature.


## Final validation receipt

Independent Reviewer **APPROVE**, checkpoint `6645d5d`: `./init.sh`, configured
`sh tools/run-tests.sh` (**47/47 suites**), and `git diff --check` passed. All
R1–R12 rows have final executable coverage, including the corrected source-arming
identity check and an independent mutation that restores the exact false
`armed` / `codex=raise` failure when that check is omitted. Full evidence:
`progress/2026-09-14-native-codex/review.md`; native execution evidence:
`progress/2026-09-14-native-codex/smoke.md`.

The bounded Codex CLI 0.154.0 smoke completed in 582.96 seconds, preserving the
approval pause, separate Builder/Reviewer/pr-fixer contexts, and failing-init halt.
It reached Reviewer APPROVE while remaining `in-review`; no merge or `done`
transition occurred. Local Git writes were test-driver owned. Remote PR-loop
execution, model-pair performance, and dash portability were not verified. The
verified shell was GNU Bash 5.3.15 acting as `/usr/bin/sh`; inherited source Codex
intentionally leaves combined escalation DISARMED. TaskStore remains `in-review`
pending the Orchestrator's authorized PR/merge lifecycle.
