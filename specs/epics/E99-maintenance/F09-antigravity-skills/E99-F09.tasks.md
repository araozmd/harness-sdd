# Antigravity claims the shared skill unit — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom, one at a
> time. Each task names the R-id(s) it satisfies. Check off when done.

- [ ] **T1** (R1) — Add `skill_unit_claimed()` to `harness-install.sh` next to the skill-unit
      helpers: `agent_selected codex || agent_selected antigravity`, with the ADR-0003 comment
      warning that a front-end missing from this set has its units reclaimed silently.
- [ ] **T2** (R1) — Mechanical rename per the plan's table: `install_codex_skill` →
      `install_skill_unit`, `reclaim_codex_skills` → `reclaim_skill_units`, `gen_codex_skill`
      → `gen_skill_body`, `codex_skill_destination_is_symlinked` →
      `skill_unit_destination_is_symlinked`. Leave `gen_codex_skill_policy` and every
      `*_stamp_*` helper alone. Verify with `grep -n 'codex_skill\|codex_skills'` — no stale
      call sites — and `sh -n harness-install.sh`.
- [ ] **T3** (R1, R3) — §5d: widen the install gate to `skill_unit_claimed`, and update the
      §5d header comment to name both discoverers and cite ADR-0003. The policy companion
      stays unconditional inside the unit writer — no new parameter.
- [ ] **T4** (R1) — Restate the §5d `ok` summary line and the HARNESS-OWNED manifest prose
      (`:2234-2236`) so neither claims the unit is Codex's.
- [ ] **T5** (R4, R5) — §7 `codex)`: reclaim the units only when `! agent_selected antigravity`.
- [ ] **T6** (R4, R5) — §7 `antigravity)`: reclaim `$HARNESS_OWNED_CMDS` units when
      `! agent_selected codex`, placed so it cannot disturb the existing rules/personas/
      workflows reclaim or the `GEMINI.md` branch below it.
- [ ] **T7** (R6) — §7b: widen the gate-off reclaim from `agent_selected codex` to
      `skill_unit_claimed`, still above the deferred `rm -rf "$CMDDIR"`.
- [ ] **T8** (R4) — Rewrite `tests/test_install.sh`'s codex-deselect block (`:1079-1102`):
      same `antigravity,codex` fixture, inverted verdict — assert the units and their stamps
      survive, keep the edited-file, user-sibling and warning assertions. Add a line comment
      naming E99-F09 R4 and ADR-0003 as the reason the old verdict changed, so the next reader
      does not "restore" it.
- [ ] **T9** (R1, R2, R3) — Extend the antigravity-only case in `tests/test_install.sh`: the
      unit exists with `name:`/`description:` frontmatter and a policy companion, and is
      byte-identical to the same command's unit from a codex-only install.
- [ ] **T10** (R5) — New case: `antigravity,codex` → `claude` reclaims the whole unit, empties
      `.harness/.codex-skills/`, and prunes the emptied directories.
- [ ] **T11** (R7) — New case: a foreign `SKILL.md` survives an **antigravity-triggered**
      install and an antigravity-triggered last-claimant reclaim, with the warning emitted.
- [ ] **T12** (R6) — New case in `tests/test_pr_loop.sh`: gate-off on an antigravity-only
      target reclaims the `sdd-pr-loop` unit and leaves the ungated units in place.
- [ ] **T13** — Update `docs/INSTALL.md`, `docs/HARNESS.md` and `README.md` per the plan's
      file table: the unit is shared by Codex and Antigravity, and the stamp directory keeps
      its historical name on purpose (cite ADR-0003).
- [ ] **T14** — Bump `VERSION` (MINOR) and add the `CHANGELOG.md` entry, calling out the one
      **behavior change**: deselecting `codex` from a target that still selects `antigravity`
      no longer removes the skill units.
- [ ] **T15** — Mutation-test each new/rewritten case: commit first, then break exactly the
      gate that case owns and confirm that case fails while the others stay green. Record the
      pairs in `progress/`.
- [ ] **T16** — Run `sh tools/run-tests.sh` (all suites) and `sh tools/change-size.sh`; re-run
      the touched suites under `dash`; `./init.sh` green before hand-off.
