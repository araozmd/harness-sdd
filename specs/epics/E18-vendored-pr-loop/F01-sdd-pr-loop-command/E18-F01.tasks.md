# `/sdd-pr-loop` command + vendored Codex watcher — Tasks

> Atomic, sequential steps. Work top to bottom, one at a time. Each names the R-id(s) it
> satisfies. The four blocks (A config+watcher · B command+gate · C pr-fixer · D de-MCO +
> docs) are contiguous, so a later split of this feature would be a clean cut.

## Block A — config block + vendored watcher

- [x] **T1** (R15) — Add the top-level `pr_loop:` block (`enabled: true`,
  `auto_merge: true`, `max_rounds: 4`, `blocking_severities: "P0,P1"`,
  `merge_strategy: "merge"`) with explanatory comments to `harness.config.yaml`.
- [x] **T2** (R16, R17, R19) — Add `_cfg_pr_loop_value <file> <key>` to
  `harness-install.sh`, modeled on `_cfg_models_value` (section-scoped `awk`), plus a
  `pr_loop_enabled` helper that returns exit 0 unless the resolved value is literally
  `false`. Add the `pr_loop:` arm to `migrate_config`, with the block text byte-identical
  to the T1 tail.
- [x] **T3** (R18) — Verify by inspection + test that every consumer of the new keys falls
  back to the documented default when the block or the key is absent.
- [x] **T4** (R21) — Create `tools/wait-for-codex.sh` from
  `multi-cli-orchestrator/skills/pr-loop/scripts/wait-for-codex.sh`, converted to POSIX
  `sh`: `#!/bin/sh`, no `[[ ]]`, no process substitution, no `${var:0:7}`, no `(( ))`.
  Rename every `MCO_*` env read to `HARNESS_*`.
- [x] **T5** (R23, R25, R26, R27) — Preserve the four source files
  (`pr.json`, `review-comments.json`, `issue-comments.json`, `reactions.json`), the
  `trigger-ts.txt` anchor, the `--paginate --slurp | jq 'add // []'` fetches, and all four
  evaluation conditions with their jq predicates unchanged in shape.
- [x] **T6** (R24, R28, R22) — Keep the wait-mode exit contract exactly: `0` findings,
  `2` timeout (never reported as clean), `3` clean, `4` usage / unresolvable trigger
  timestamp.
- [x] **T7** (R29) — Add the `evaluate <round-dir>` sub-mode: pure, offline, invokes no
  `gh`, reads `trigger-ts.txt` for the anchor, exits `0`/`1`/`3`. Factor the evaluation so
  wait mode and `evaluate` share one routine.
- [x] **T8** (R30, R31) — Add the `preflight <pr>` sub-mode: `gh` on PATH, `gh auth status`
  ok, `jq` on PATH, repo slug resolvable, PR exists and is open. Exit `0` on success, `5`
  with a single-line diagnostic naming the failed check and its remedy otherwise. Post
  nothing to GitHub in this mode.
- [x] **T9** (R32, R33, R34) — Add the first-response probe to wait mode: exit `5` with a
  Codex-GitHub-App diagnostic when no bot activity of any kind appears within
  `HARNESS_FIRST_RESPONSE` (default 180). Disarm permanently on first sighting; skip when
  the value is `0` or is not below `HARNESS_POLL_CEILING`. Honor `HARNESS_POLL_INTERVAL`
  (60) and `HARNESS_POLL_CEILING` (900); read no `MCO_*`.
- [x] **T10** (R21, R35) — Add `chmod +x "$H/tools/wait-for-codex.sh"` beside the other
  helper chmods in `harness-install.sh`; add `.pr-loop/` to the seeded `.harness/.gitignore`
  `_ignores` list and `/.pr-loop/` to the repo-root `.gitignore` (removing the dead
  `.mco-cache/` entry).

## Block B — the `/sdd-pr-loop` command body, mirroring and the gate

- [x] **T11** (R1) — Add the `sdd-pr-loop.md` heredoc to the CMDDIR section of
  `harness-install.sh`, written **unconditionally** on every run (the reclamation
  pristine-compare reference).
- [x] **T12** (R36, R37, R38) — Write the body's steps 0–2: run
  `.harness/tools/wait-for-codex.sh preflight <pr>` before posting anything and STOP on
  non-zero; post `@codex review` and derive the trigger id from the printed
  `…#issuecomment-<id>` URL with the explicit anti-pattern note about a separate list call;
  launch the watcher in the background at its harness-relative path and branch on
  `0`/`2`/`3`/`4`/`5`, never hand-poll.
- [x] **T13** (R39, R40) — Write the classification + stall-detection steps: freshness
  filter, case-insensitive `P0|P1|P2|nit` match (badge form included), first match wins,
  default `P2`, filter to `blocking_severities`, compare `blocking.json` against round
  `n-1` and escalate immediately on any repeat.
- [x] **T14** (R41, R46) — Write the round branching against `max_rounds`, the
  `worker`/`role` bookkeeping files, and the handover summary construction.
- [x] **T15** (R42, R43, R44, R45) — Write the gate re-check + terminal states: non-Codex
  unresolved thread ⇒ `needs-human` and no merge; Codex-only ⇒ resolve then merge under
  `auto_merge` with the configured strategy and `--delete-branch`; `auto_merge: false` ⇒
  stop after the green summary; local cleanup only when the merge command itself succeeded.
- [x] **T16** (R47, R35) — Add the `HARNESS_DRY_RUN=1` path and pin the cache root to
  `<HARNESS_DIR>/.pr-loop/<pr>/round-<n>/` throughout the body.
- [x] **T17** (R2, R3) — Introduce `HARNESS_PR_LOOP_CMDS="sdd-pr-loop"` and
  `HARNESS_OWNED_CMDS="$HARNESS_SDD_CMDS $HARNESS_PR_LOOP_CMDS"`; mirror the gated command
  into `.claude/commands/`, `.opencode/command/`, `.agents/workflows/` and the global
  Codex prompts dir **only when the gate is on**, reusing each front-end's existing copy
  loop and its existing edited-file backup/warn behavior.
- [x] **T18** (R4, R6, R7) — Widen every §7 deselect reclamation loop from
  `$HARNESS_SDD_CMDS` to `$HARNESS_OWNED_CMDS`, keeping by-name removal inside
  harness-owned workspace dirs and `remove_if_pristine` inside the global Codex prompts dir
  and `.agents/`.
- [x] **T19** (R5, R8) — Add the §7b gate-off reclamation pass: while the gate is off, for
  each **still selected** front-end, reclaim its `sdd-pr-loop` command and `pr-fixer`
  artifact under the T18 ownership rules and prune empty dirs. Place it after the deselect
  loop and **before** `rm -rf "$CMDDIR"`.
- [x] **T20** (R2) — Update `manifest.txt` with a line describing the gated pr_loop glue.

## Block C — the `pr-fixer` role and its per-front-end shims

- [x] **T21** (R9) — Create `agents/pr-fixer.md`: canonical, front-end-neutral, carrying
  the vendored runbook (one comment, one fix, one commit, one return; inputs
  `pr_number`/`comment_id`/`path`/`line`/`body`/`round_dir`; writes
  `$round_dir/fix-<comment_id>.md`; out of scope: push, thread resolution, unrelated files,
  the full suite).
- [x] **T22** (R10) — Emit `.claude/agents/pr-fixer.md` via a gated `emit_agent pr-fixer`
  call, and add `pr-fixer` to `HARNESS_CLAUDE_SHIMS` (removal ledger only).
- [x] **T23** (R11, R12) — Add a hoisted `gen_oc_agent` emitter writing
  `.opencode/agent/pr-fixer.md` (frontmatter `description` + `mode: subagent`, body points
  at `.harness/agents/pr-fixer.md`), called only when the gate is on. Leave
  `gen_opencode_json` and the `.opencode.stamp` logic untouched.
- [x] **T24** (R13, R14) — Emit `.agents/agents/pr-fixer.md` via a gated `gen_ag_persona`
  call placed **outside** the `ag_personas` loop; add no row to `ag_personas` or
  `MODEL_ROLES`, and create no codex/gemini `pr-fixer` artifact.
- [x] **T25** (R14) — State in the command body that a front-end without a `pr-fixer`
  sub-agent (codex, gemini) applies fixes in-session under the same one-fix-one-commit
  discipline.
- [x] **T26** (R4, R5, R6, R7) — Extend the §7 deselect arms and the §7b gate-off pass to
  reclaim each front-end's `pr-fixer` artifact, pristine-compare in `.agents/`, by name
  elsewhere, pruning `.opencode/agent/` and its parent only when empty.

## Block D — de-MCO pass, docs, tests, version

- [x] **T27** (R48) — Rewrite the three `/pr-loop` references
  (`agents/orchestrator.md:102`, `CLAUDE.md:20`, `docs/WORKFLOW.md:325`) to
  availability-phrased `/sdd-pr-loop`, each with an explicit alternative when the command
  is not installed. Introduce no prose-templating mechanism.
- [x] **T28** (R20) — Sweep the harness body for `MCO_`, `.mco-cache`, `~/.agents/skills`,
  `route-task`, `start-feature` and remove every remaining occurrence (excluding
  `progress/history.md` and shipped feature specs, which are historical records).
- [x] **T29** (R49) — Add the source-layout copies `.claude/commands/sdd-pr-loop.md` and
  `.claude/agents/pr-fixer.md`, resolving paths from the repository root in the existing
  source-layout dialect.
- [x] **T30** (R50) — Update `README.md`, `docs/INSTALL.md`, `docs/WORKFLOW.md`, and
  `docs/HARNESS.md`: list `/sdd-pr-loop`, document the `pr_loop:` block and the `HARNESS_*`
  env knobs, and state the Codex-GitHub-App + authed-`gh` + `jq` precondition and the
  fail-fast behavior.
- [x] **T31** (R51) — Write `tests/test_pr_loop.sh` per `E18-F01.tests.md`, sandboxing
  `CODEX_HOME` for every installer-invoking run, freezing no exact `VERSION` string, and
  diffing no DO-NOT-TOUCH file against `main`. Gate only the jq-fixture assertions behind a
  `jq` presence check that prints a `skip` line.
- [x] **T32** (R51) — Add `sh tests/test_pr_loop.sh` to `verification.test_command` in
  `harness.config.yaml` and extend the trailing comment listing the suites.
- [x] **T33** (R52) — Extend `tests/test_install.sh`: add `sdd-pr-loop` to the existing
  per-front-end command loops and to the deselect-removal assertions.
- [x] **T34** (R53) — Confirm `init.sh` is unmodified and that a `PATH` without `gh` and
  `jq` still yields `./init.sh` exit 0.
- [x] **T35** (R54) — Bump `VERSION` to `0.39.0` and add the matching `CHANGELOG.md` entry.
- [x] **T36** — Run `./init.sh` and the full `verification.test_command` chain; ensure green
  before hand-off.
