# E26-F02 — Plan

1. **Manifest write (R2)** — last step of `self_install`: `cksum` over the sorted
   glob of `$SRC/.claude/{agents,commands}/*.md` + `.escalation-arming` (existence-
   guarded), repo-relative paths, into `.claude/.glue-manifest`. Deterministic ⇒
   `test_self_mode.sh` idempotence covers it unchanged (R4).
2. **init.sh warn line (R3)** — beside the escalation-arming advisory: gated on
   `[ -f "$HARNESS_DIR/harness-install.sh" ] && [ -f ".claude/.glue-manifest" ]`
   (targets carry neither — `HARNESS_BODY_LOCAL` never ships the installer), re-run
   `cksum` per manifest line, one ⚠️ naming `sh harness-install.sh --self` on any
   mismatch/missing file. Warn-only: no exit-code change on staleness.
3. **Gate suite (R1)** — `tests/test_self_drift.sh`: fixture copy of the tree,
   `--self` in the copy, `diff -r` copy/.claude vs `$SRC/.claude` and the two arming
   files. Any divergence fails with the remedy named. Also asserts the init.sh warn
   fires on a hand-edited fixture and stays silent on a clean one.
4. **VERSION 0.77.0 + CHANGELOG.**

## Risks

- The gate makes "edit an emitter" a two-step change (edit + `--self`) — that is the
  feature, and the failure message says exactly that.
- `cksum` is POSIX and stable across platforms for the same bytes.

## Out of scope

Gating target installs (their glue is regenerated on every installer run anyway).
