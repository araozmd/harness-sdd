# E26-F02 — Tests (tests/test_self_drift.sh)

Fixture: full copy of the source tree (same helper as `test_self_mode.sh`), sandboxed
`CODEX_HOME`.

- **R1 / gate-green** — on the pristine copy, `--self` then `diff -r` against the
  committed `.claude/` (and `.escalation-arming`) shows zero divergence — proves the
  REPO's emitters and committed glue are in sync right now.
- **R1 / gate-red** — edit one emitter phrase in the copy's `harness-install.sh`
  (e.g. a shim body word), re-run `--self` in a second pristine copy carrying that
  edit: the diff against its committed glue is non-empty — the exact "emitter changed
  without regeneration" failure the gate exists for (asserted by driving the suite's
  own comparison helper, not by re-implementing it).
- **R2 / manifest** — after `--self`, `.claude/.glue-manifest` exists, lists every
  regenerated file + `.escalation-arming`, never itself; entries verify with `cksum`.
- **R3 / warn-fires** — hand-edit one generated file in the copy → `./init.sh` exits
  0 AND prints the ⚠️ staleness line naming `--self`.
- **R3 / warn-silent** — pristine copy: `./init.sh` prints no staleness line; a copy
  with the manifest removed (target-layout proxy) also prints none.

Mutation kills (recorded in the PR): drop the manifest write → R2 fails; make the
init.sh check fatal → R3 warn-fires' exit-0 arm fails; skip a file class from the
manifest glob → R2's completeness listing fails.
