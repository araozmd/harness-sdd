# E26-F01 — Tasks

- [ ] T1 — Reconcile the 7 command bodies per the plan's base-choice table:
  upstream committed-copy content into the heredocs, drop repo-asides, verify each
  reconciled body reads correctly under BOTH layouts. One commit per file. (R2, R7)
- [ ] T2 — Replace `emit_agent`'s generic body with per-role prose reconciled from
  the committed shims (frontmatter key order unchanged). (R2, R7)
- [ ] T3 — `--self` flag: arg parsing + guards; self pass runs shims, commands
  (+ prefix transform), arming only. (R1, R6)
- [ ] T4 — `self_model` harvest + the `resolve_model` self-mode choke point; arming
  destination parametrized. (R3, R4)
- [ ] T5 — pr-loop gating flows from the repo's own config in self mode. (R5)
- [ ] T6 — Run `--self`, commit the regenerated 15 files, confirm idempotence. (R2, R6)
- [ ] T7 — `tests/test_self_mode.sh`; VERSION MINOR + CHANGELOG; full suite. (R8)
