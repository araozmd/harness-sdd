# E26-F01 — Tests (tests/test_self_mode.sh)

Fixture: copy the SOURCE repo's glue-relevant tree (`harness-install.sh`, `agents/`,
`.claude/`, `harness.config.yaml`, `VERSION`, `tools/` as needed) into a temp dir so
`--self` runs never touch the real checkout. Sandbox `CODEX_HOME`.

- **R1 / self-regenerates** — after `--self`, `.claude/agents/builder.md` and
  `.claude/commands/sdd-next.md` exist and reference `agents/builder.md` /
  `init.sh` with NO `.harness/` prefix anywhere in any regenerated file.
- **R1 / arg-guards** — `--self <dir>` and `--self --umbrella <dir>` exit non-zero
  with no filesystem writes.
- **R3 / model-preserved** — seed the fixture's `builder.md` with `model: sonnet`,
  `builder-heavy.md` with `model: opus` → regenerated shims carry the same lines;
  a shim seeded WITHOUT `model:` stays model-less after `--self`.
- **R4 / arming-real** — with those models, `./.escalation-arming` reads `armed` +
  `claude=raise`; with both shims model-less it reads the disarmed verdict.
- **R5 / pr-loop-gated** — `pr_loop.enabled: true` in the fixture config ⇒
  `sdd-pr-loop.md` + `pr-fixer.md` regenerate; flipped to `false` ⇒ both are absent
  from the regeneration set (and `--self` does not delete... asserts per the gate's
  documented removal semantics).
- **R6 / idempotent** — a second `--self` run changes zero bytes (`diff -rq` over
  `.claude/` + `.escalation-arming`).
- **R6 / no-collateral** — `--self` leaves `VERSION`, `harness.config.yaml`, and a
  planted sentinel file outside the glue dirs byte-identical; writes no
  `.harness-version`, no `.agents`.
- **R2 / reconciliation-sentinels** — the regenerated `sdd-next.md` contains the
  inception-brief routing phrase (upstreamed content survived); the regenerated
  `sdd-pr-loop.md` contains `baseRefOid` (emitter-side content survived) and does
  NOT contain "as this one does" (repo-aside dropped); `.pr-loop/` cache tokens
  survive the prefix transform.
- **R7 / target-unaffected-mechanism** — a normal install into a temp target still
  produces `.harness/`-prefixed glue containing the same reconciled prose.

Mutation kills (recorded in the PR): skip the harvest → R3 fails; hardcode `armed` →
R4's disarmed arm fails; transform `s|harness/||` (unanchored) → R2's `.pr-loop/`
sentinel fails; let `--self` fall through to §1 → R6 no-collateral fails.
