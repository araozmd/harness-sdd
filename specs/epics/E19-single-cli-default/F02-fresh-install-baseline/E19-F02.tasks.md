# Fresh-install pre-check baseline = the detected host — Tasks

> Atomic, sequential, independent steps. Work top to bottom, one at a time. Each task names
> the R-id(s) it satisfies. Check off when done.
> **Prerequisite:** E19-F01 is merged. If `precheck_baseline` does not exist in
> `harness-install.sh`, stop — this feature has nothing to attach to.
> **Before T1:** re-read the "DO NOT TOUCH" list in `E19-F02.plan.md`. The marker table and
> `detect_host` belong to E19-F01 and are not edited here.

## The change

- [ ] **T1** (R4, R9) — Write the failing test first: an existing install
      (`.harness/.harness-version` present, `.harness/.agents` deleted) with a host marker set
      must still report `baseline=` all five keys. This is the "upgrades must not silently
      narrow" guard and it is the only branch that is easy to get wrong.
- [ ] **T2** (R1, R2, R3, R4, R6) — Add the two new branches to `precheck_baseline`: persisted
      `.harness/.agents` wins; else an existing install (`.harness-version` present) ⇒ ALL;
      else `detect_host`'s key when it resolves, ALL when it does not. Change nothing else in
      the function and nothing in `resolve_agents`.
- [ ] **T3** (R4, R9) — Add a comment on the middle branch stating that the `.harness-version`
      test — not the absence of `.agents` — is what stops an upgrade from silently narrowing.
- [ ] **T4** (R5, R7) — Verify by reading the diff that the no-TTY/no-override branch still
      assigns ALL directly without consulting the helper, and that the `PRIOR_AGENTS` block
      including its `grep -vx codex` exclusion is untouched.

## Tests

- [ ] **T5** (R12) — Extend `tests/test_agents_host.sh` under E19-F01's environment
      discipline: `env -i PATH="$PATH" HOME="$X/home" CODEX_HOME="$X/ch" [MARKERS…]`, a
      sandboxed `CODEX_HOME` on every installer-invoking run, no frozen exact `VERSION`, no
      DO-NOT-TOUCH file diffed against `main`.
- [ ] **T6** (R1, R2) — Assert the fresh cases via `--print-agents`: empty dir + `CLAUDECODE=1`
      ⇒ `baseline=claude` exactly (not a union, not all five); empty dir + no marker ⇒
      `baseline=` all five sorted keys.
- [ ] **T7** (R3) — Assert the upgrade case: install with `--agents=gemini`, then ask
      `--print-agents` with `CLAUDECODE=1` set ⇒ `baseline=gemini`.
- [ ] **T8** (R5) — Assert the no-TTY default is unchanged: `CLAUDECODE=1`, no override,
      non-TTY stdin ⇒ all five front-ends stamped.
- [ ] **T9** (R8) — Assert the fresh narrowed install removes nothing: pre-seed the sandboxed
      `$CODEX_HOME/prompts` with a foreign file and a `.sdd-pr-loop.owners` ledger naming
      another target, install into an empty dir with `CLAUDECODE=1` and `--agents=host`
      (the non-interactive proxy for confirming the pre-checked baseline), and confirm both
      survive untouched.
- [ ] **T10** (R9) — Assert no existing install narrows from detection alone: install with
      `--agents=claude,gemini`, then re-run non-interactively with `CLAUDECODE=1` and no
      override; `.harness/.agents` and `GEMINI.md` are unchanged.
- [ ] **T11** (R6) — Assert by source inspection that the picker branch and the
      `--print-agents` path still both call `precheck_baseline`, with no duplicated baseline
      logic.

## Docs, version, hand-off

- [ ] **T12** (R10) — `docs/INSTALL.md`: document the new fresh-install default — what gets
      pre-checked, that it applies only to an interactive first install, that an upgrade keeps
      its persisted selection, and that an undetected host still pre-checks everything.
- [ ] **T13** (R10) — Update the `harness-install.sh` header comment line that still says the
      interactive path pre-checks "the saved `.harness/.agents` set, or ALL on a fresh
      install" — that is now inaccurate.
- [ ] **T14** (R11) — `docs/INSTALL.md`: document that re-running the installer is the
      supported way to change the selection later (re-prompts pre-checked from
      `.harness/.agents`, applies both adds and removes), with a copy-pasteable command.
- [ ] **T15** (R13) — Bump `VERSION` by a MINOR step and add the matching `CHANGELOG.md`
      entry.
- [ ] **T16** — Run `./init.sh` and the full `verification.test_command`; ensure green before
      hand-off.
