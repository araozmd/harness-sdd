# Host detection + explicit `--agents=host` — Tasks

> Atomic, sequential, independent steps. Work top to bottom, one at a time. Each task names
> the R-id(s) it satisfies. Check off when done.
> **Before T1:** re-read the "DO NOT TOUCH" list in `E19-F01.plan.md`. `AGENT_KEYS`, the
> `PRIOR_AGENTS` block, and the §7/§7b reclamation + ownership-ledger code are off limits.
> **The pre-check baseline behavior does not change in this feature** — that is E19-F02.

## Baseline

- [x] **T1** (R22) — Record the additivity baseline before touching anything: run
      `sh tests/test_install.sh` and confirm it is green. It runs non-TTY with no override,
      so it is the regression net for "an install that does not name `host` is unchanged".
      No existing assertion in it may need editing by the end of this feature.

## Detection

- [x] **T2** (R1, R6, R7, R8) — In `harness-install.sh`, immediately after the `AGENT_KEYS`
      block (~:426), add the `HOST_MARKERS` table with its rules header: only session
      markers; `CODEX_HOME`, `HOME`, `TERM_PROGRAM` and `*_API_KEY` forbidden by name; one
      row per detectable key; every non-`claude` row needs a verification comment. Seed it
      with the single verified row `claude CLAUDECODE CLAUDE_CODE_ENTRYPOINT`. Do **not**
      edit `AGENT_KEYS`.
- [x] **T3** (R2) — Add `marker_present <var>`: true iff the named variable is set and
      non-empty (`eval` + `[ -n … ]`, `${VAR:-}`-guarded so `set -u` cannot abort).
- [x] **T4** (R1, R3, R4, R5, R11) — Add `detect_host` (near `agent_known`, ~:592): walk
      `HOST_MARKERS`, collect matching keys, print the key on exactly one hit, print nothing
      on zero, and on two-or-more print nothing plus one stderr diagnostic naming the
      candidates. Verdict on stdout, diagnostics on stderr. It must create no file and never
      exit non-zero.
- [x] **T5** (R9, R10) — Give `detect_host` its `HARNESS_HOST_AGENT` fast path: exactly one
      known agent key wins outright; any other value (unknown token, several tokens, or the
      literal `host`) emits one warning naming the value and falls through to the marker
      walk. Never `die`.
- [x] **T6** (R8) — Run the plan's verification procedure for `codex`, `gemini`, `opencode`
      and `antigravity` on every CLI available to you. Add a row **only** for a marker you
      actually observed in the `comm -13` delta, with a comment recording CLI + version.
      Adding **no** further row is an acceptable, expected outcome — do not add a plausible
      guess, and do not use `GEMINI_CLI` unless you have confirmed `agy` does *not* also set
      it.

## The shared baseline helper (behavior-preserving)

- [x] **T7** (R22, R26) — Extract `precheck_baseline <target>` from `resolve_agents`'s
      interactive branch: persisted `.harness/.agents` if present, else ALL. This is a pure
      refactor — the resolved set for every existing code path must be identical. Replace
      the inline `if [ -f "$_persisted" ] … else … fi` with a call to it.

## The `host` mode

- [x] **T8** (R16) — In `resolve_agents`, before `validate_csv` runs, reject an override
      whose comma-split tokens include `host` alongside any other token: `die` with a message
      stating `host` is a resolution mode and must be the entire value. No target changes.
- [x] **T9** (R12, R13, R14, R15) — Add the whole-value `host` branch: when the trimmed
      override equals `host`, set `SELECTED` to `detect_host`'s key alone, or — when
      undetected — to `precheck_baseline "$_t"` (which yields ALL on a target with no
      existing install and the persisted set on one that has it). It runs before the TTY
      test, so it is honored on interactive and non-interactive runs alike.
- [x] **T10** (R25) — Emit exactly one `info` line from the host branch naming the detected
      key, or stating that the host was undetected and *which* fallback was applied.
- [x] **T11** (R17, R18, R19) — Verify by reading the diff that `AGENT_KEYS` is unchanged,
      that `validate_csv`'s token grammar is unchanged, and that the `PRIOR_AGENTS` block —
      including its `grep -vx codex` legacy exclusion — is untouched.

## Diagnostic flag

- [x] **T12** (R23) — Add `--print-agents` to the arg-parse loop beside `--agents`.
- [x] **T13** (R23, R24) — In single-target mode, after the existing target validation and
      before `install_one`, honor the flag: print `host=<key or empty>` then
      `baseline=<space-separated sorted keys>` and `exit 0`, writing nothing. `die` with a
      usage message when it is combined with `--umbrella`.

## Tests

- [x] **T14** (R28, R29) — Create `tests/test_agents_host.sh` following
      `tests/test_install.sh`'s structure (`fail`/`pass` helpers, per-case temp dirs). Every
      detection-sensitive invocation runs as
      `env -i PATH="$PATH" HOME="$X/home" CODEX_HOME="$X/ch" [MARKERS…] sh "$SRC/harness-install.sh" …`.
      Sandbox `CODEX_HOME` in every installer-invoking run. Freeze no exact `VERSION`; diff
      no DO-NOT-TOUCH file against `main`.
- [x] **T15** (R2, R3, R4, R5, R9, R10) — Write the detection matrix per `E19-F01.tests.md`:
      single marker, empty marker, no marker, two competing markers, `HARNESS_HOST_AGENT`
      win, `HARNESS_HOST_AGENT` garbage ⇒ warn-and-continue.
- [x] **T16** (R6) — Write the forbidden-marker assertions: with `CODEX_HOME` set (and
      nothing else) `host=` is empty; likewise for `GEMINI_API_KEY`, `OPENCODE_API_KEY`,
      `ANTHROPIC_API_KEY`, `TERM_PROGRAM`. Add a source-level check that no `*_API_KEY` name
      and no `CODEX_HOME` appears inside the `HOST_MARKERS` table.
- [x] **T17** (R12, R13, R14, R15, R18) — Write the host-mode end-to-end assertions: detected
      ⇒ only that front-end's glue; undetected + no existing install ⇒ all five; undetected +
      an existing `--agents=claude` install ⇒ still claude only (the anti-widening case);
      `HARNESS_AGENTS=host` behaves the same as `--agents=host`; `.harness/.agents` contains
      only concrete keys and never `host`.
- [x] **T18** (R16, R17, R24) — Write the rejection assertions: `--agents=host,gemini` exits
      non-zero and writes nothing; `--print-agents --umbrella` exits non-zero; every line of
      a persisted selection is one of the five known keys.
- [x] **T19** (R20, R21) — Write the removal-safety assertions: `--agents=host` into a target
      with no existing install leaves the sandboxed `$CODEX_HOME/prompts` untouched (no file
      created, none deleted, ledger intact); a detected `host` upgrade that drops a persisted
      front-end still honors the existing pristine-compare rule (a hand-edited file survives).
- [x] **T20** (R23) — Write the `--print-agents` contract assertions: exit 0, exactly the two
      lines on stdout, and the target's file listing byte-identical before and after.
- [x] **T21** (R22) — Write the additivity assertions: with no `host` token and no
      `HARNESS_HOST_AGENT`, a non-TTY no-override run still stamps all five even when
      `CLAUDECODE=1` is set, and `--agents=claude` behaves exactly as before.
- [x] **T22** (R28) — Extend the E08 block in `tests/test_install.sh` with the
      `--agents=host` end-to-end case and the "`host` never appears in `.harness/.agents`"
      assertion.
- [x] **T23** (R28) — Wire `&& sh tests/test_agents_host.sh` into
      `verification.test_command` in `harness.config.yaml` and extend its trailing suite
      comment.

## Docs, version, hand-off

- [x] **T24** (R27) — `docs/INSTALL.md`: new subsection covering the `host` mode, the marker
      table with an explicit statement of which markers are verified (and that a miss is
      normal), `HARNESS_HOST_AGENT`, `--print-agents`, and **both** undetected fallbacks —
      no existing install ⇒ ALL, existing install ⇒ persisted-else-ALL — with the reason the
      two differ, so the asymmetry reads as intentional.
- [x] **T25** (R27) — Name the `host` mode in `README.md`'s agent-selection paragraph, in the
      `harness-install.sh` header comment, and in the generated `manifest.txt` "AGENT
      SELECTION" block.
- [x] **T26** (R31) — Bump `VERSION` `0.39.0` → `0.40.0` and add the matching
      `CHANGELOG.md` entry.
- [x] **T27** (R30) — Confirm `init.sh` gained no dependency and that a target installed by
      this build still passes `./init.sh` with exit 0.
- [x] **T28** — Run `./init.sh` and the full `verification.test_command`; ensure green before
      hand-off.
