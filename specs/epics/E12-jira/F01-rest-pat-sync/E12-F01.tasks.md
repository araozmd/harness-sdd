# Jira sync via REST API + PAT (no MCP) — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom, one at a
> time. Each task names the R-id(s) it satisfies. Check off when done.
> Order: stub split + jira branch (tool) → config seed → docs pin → installer + gitignore →
> installer-test assertion → mirror-suite cases → VERSION bump → green gate.
>
> This feature **fills the recognized `jira` stub** in `tools/sync-board.mjs`. Do **not**
> create a second Jira tool/code path (R2); do **not** implement `owner→assignee` for jira
> (R12 — that is E10); do **not** touch the `github-projects` or `azure-boards` provider
> logic beyond splitting the shared stub.

- [x] **T1** (R2, R4) — In `tools/sync-board.mjs`, **split** the shared
  `if (provider === 'jira' || provider === 'azure-boards')` stub so `azure-boards` stays a
  recognized no-op stub (prints "not implemented", exits 0) and `jira` falls through to a
  new real branch. Preserve the inert-default early return (empty provider ⇒ no network
  call) and the unknown-provider error. Do **not** touch the `github-projects` branch.

- [x] **T2** (R1, R5, R9) — In the new `jira` branch, read `base_url` and `project_key`
  from `mirror.board`; if either is missing, exit non-zero naming the missing key(s)
  **before** any network call. Set up the Jira REST transport: HTTPS to `base_url` with an
  `Authorization: Bearer <PAT>` header, dependency-free (built-in `fetch` or `curl`
  shell-out — **no** new npm package, **no** MCP).

- [x] **T3** (R6, R7, R8) — Add the **PAT resolver + fail-closed preflight**: resolve the
  PAT from `JIRA_PAT` (env, precedence) else from `mirror.board.pat_file` (default
  `.harness/jira.pat`, trimmed). If neither resolves, exit non-zero naming `JIRA_PAT` and
  the pat_file path **before** any network call. Never write the PAT into `tasks.json`,
  config, or any file, and never print the PAT value in logs/errors.

- [x] **T4** (R3, R10, R11) — Implement the **REST reconcile**: for each epic (→ Jira
  Epic) and feature (→ Jira Story, both overridable via `mirror.board.issue_type_map`),
  find-or-create one issue matched by a stable feature-id key (label/summary prefix), and
  transition it to the workflow state mapped from feature status via the existing
  `mirror.board.status_map` (identity default). A re-run updates in place and creates **no**
  duplicate.

- [x] **T5** (R12, R13, R14, R15) — Finish the branch behaviors: leave `assignee` a
  **no-op for jira** (recognized, deferred to E10 — do not wire Jira's assignee); ensure the
  tool never writes `state/tasks.json` (one-way); implement `--dry-run` to print intended
  Jira changes and mutate nothing; and on a Jira REST **401/403**, exit non-zero with an
  actionable message that names the PAT/scope requirement and does **not** echo the PAT.

- [x] **T6** (R6, R10, R16) — In `harness.config.yaml`, add the Jira `mirror.board` keys
  with **inert defaults** + comments: `base_url: ""`, `project_key: ""`,
  `pat_file: ".harness/jira.pat"`, and a commented `issue_type_map` example (epic→Epic,
  feature→Story). Reuse existing `provider`/`status_map`/`assignee`. Keep `provider: ""`
  (inert). **No secret** goes in this file.

- [x] **T7** (R5, R6, R10, R11, R12, R13, R17) — In `store/board-mirror.md`, flip the
  `jira` providers-table row from "stub (no-op)" to implemented (mirror) and add a **`jira`
  contract** section: Server/DC REST + Bearer PAT (Cloud out of F01 scope); `JIRA_PAT` env
  / gitignored `pat_file` + precedence; `base_url`/`project_key`; `issue_type_map`
  (epic→Epic/feature→Story) + `status_map` workflow mapping; REST+PAT-only / no-MCP
  transport; one-way invariant; `assignee` deferred to E10. Keep `azure-boards` as a stub.

- [x] **T8** (R17) — In `store/jira.md`, add a prominent cross-reference clarifying this
  file is the **backend** (source-of-truth) stub, and that the one-way **mirror** `jira`
  provider (REST+PAT) is documented in `board-mirror.md` — so a reader choosing the mirror
  isn't misled by the backend stub. Do not convert this file into the mirror doc.

- [x] **T9** (R16) — In `harness-install.sh`, ensure the generated `harness.config.yaml`
  seed carries the new Jira `mirror.board` keys (inert), and append the default Jira PAT
  file path (`.harness/jira.pat` / its project-root equivalent) to the seeded `.gitignore`
  via the existing append-only logic. Keep `chmod +x tools/sync-board.mjs` (tool ships
  executable).

- [x] **T10** (R16) — In `tests/test_install.sh`, assert a fresh install ships an
  **executable** `.harness/tools/sync-board.mjs` (keep/reuse the existing assertion) and
  add an assertion that the seeded `.gitignore` ignores the default Jira PAT file path.
  Sandbox `CODEX_HOME` for the installer-invoking case (existing suite guard); reuse the
  suite's temp-dir pattern.

- [x] **T11** (R1, R3, R4, R5, R6, R7, R8, R9, R10, R11, R12, R13, R14, R15) — In
  `tests/test_mirror.sh`, add the `jira` behavioral cases per `E12-F01.tests.md` using a
  **stubbed Jira REST endpoint / injected transport** (record every call; no live Jira, no
  MCP). Cover: inert default no-op; configured run dispatches only to the stub REST endpoint
  (R1); reconcile idempotent (R3); Bearer PAT header used (R5); env-precedence + file PAT
  (R6); missing-PAT / missing-config fail closed before any network call (R7, R9); PAT never
  in tasks.json/config/output (R8); issue-type + status map honored (R10, R11); assignee
  no-op for jira (R12); tasks.json unchanged (R13); `--dry-run` mutates nothing (R14);
  401/403 → non-zero, actionable, PAT-free (R15). Keep the `azure-boards` stub-noop case.
  Assert **behavior**; never pin the exact VERSION; never diff `state/tasks.json` vs `main`.

- [x] **T12** (R18) — Bump `VERSION` one **MINOR** step (additive, backward-compatible;
  SemVer MINOR, not MAJOR) and add a matching `CHANGELOG.md` entry describing the
  REST+PAT Jira mirror: `jira` provider branch, config seed + gitignored PAT, docs pin,
  installer-test assertion.

- [x] **T13** — Write/finish the tests per `E12-F01.tests.md`; ensure every R-id maps to a
  passing test (`tests/test_mirror.sh` + `tests/test_install.sh`, both already in
  `verification.test_command`).

- [x] **T14** — Run `./init.sh` + the full `verification.test_command`; ensure green
  before hand-off.
