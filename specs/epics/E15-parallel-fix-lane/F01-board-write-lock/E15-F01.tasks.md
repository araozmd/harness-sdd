# Board write lock: flock-guarded read-modify-write on tasks.json — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom,
> one at a time. Each task names the R-id(s) it satisfies. Check off when done.

- [x] **T1** (R9, R3) — Create the lock helper `tools/tasks-lock.py`: acquire an
  advisory `fcntl.flock` on `state/tasks.json.lock` (resolve the lockfile with
  `cwd = HARNESS_DIR`) for the whole critical section, release on exit. Use
  stdlib `fcntl` only — no `flock(1)` binary, no third-party dep.
- [x] **T2** (R2, R1) — Inside the held lock, **re-read `state/tasks.json` from
  disk**, apply the caller's single status mutation to that fresh content, so a
  second concurrent writer that re-reads sees the first writer's committed result
  (no lost update). Do not read the board before acquiring the lock.
- [x] **T3** (R4) — Validate the mutated content before persisting: JSON parse
  **and** `store/tasks.schema.json` schema check. Write via write-temp-then
  `os.replace()` (atomic rename); on any validation failure, discard the temp file,
  leave the original `state/tasks.json` untouched, release the lock, and exit
  non-zero with a clear error (fail-stop, no torn file).
- [x] **T4** (R5) — Bound lock acquisition with a timeout (default ~10s, a single
  named constant). On timeout, exit non-zero with an actionable error naming the
  lockfile and the timeout — never block indefinitely, never proceed unlocked.
- [x] **T5** (R6, R7, R1, R2, R3, R4, R5, R8) — Amend `store/local.md`: update the
  `set_status` bullet (and the intro read-modify-write note) to mandate the lock
  protocol (acquire → re-read → mutate → validate → write → release), state that
  `on_write_command` fires **after** release, that the lock is a no-op for serial
  callers, single-host scope, and **no new status / no schema change**.
- [x] **T6** (R10) — Wire the installer: confirm `tools/tasks-lock.py` is copied
  into `.harness/tools/` and `chmod +x`'d beside `sync-board.mjs` /
  `telemetry-report.py` in `harness-install.sh`; append `state/tasks.json.lock` to
  the seeded `.gitignore` (append-only, never clobber).
- [x] **T7** (R10) — In `tests/test_install.sh`, add an assertion that a fresh
  install ships `.harness/tools/tasks-lock.py` as an **executable**.
- [x] **T8** (R8) — Verify no diff to `store/tasks.schema.json` and no new status
  value anywhere; the persisted write content is byte-unchanged for a serial caller.
- [x] **T9** (R11) — Bump `VERSION` one SemVer **MINOR** step and add the matching
  `CHANGELOG.md` entry (installed-body change).
- [x] **T10** — Write tests per `E15-F01.tests.md` (new `tests/test_board_lock.sh`),
  and append `&& sh tests/test_board_lock.sh` to `verification.test_command` in
  `harness.config.yaml`.
- [x] **T11** — Run `./init.sh` + the full `verification.test_command`; ensure green
  before hand-off.
