# Worktree-per-fix isolation helper for concurrent fix branches — Tasks

> Atomic, sequential steps. The Builder works these top to bottom. Each task names
> the R-id(s) it satisfies.

- [x] **T1** (R1, R2, R4) — Create executable `tools/fix-worktree.sh` with strict
  argument parsing, source/installed-layout self-location, canonical primary
  worktree resolution by comparing each porcelain path's absolute Git dir to the
  common dir, exact short-local-base validation, and captured base commit.
- [x] **T2** (R3, R4, R5) — Add read-only creation preflight for clean primary
  status, safe id/slug/base, absent exact branch/path/registration, and clear
  non-zero collision diagnostics before any mutation.
- [x] **T3** (R1, R6) — Implement `create` with `git worktree add -b` at the captured
  base commit and deterministic `.claude/worktrees/<id>-<slug>` placement; print
  only the absolute path on successful stdout.
- [x] **T4** (R7) — Add the four-path personal-layer absolute-symlink allowlist,
  missing-source behavior, unexpected-destination refusal, and explicit exclusion
  of `.claude/scheduled_tasks.lock` and arbitrary ignored files.
- [x] **T5** (R8, R10) — Run the correct source/installed initialization entrypoint
  inside the new worktree and add provisioning rollback limited to an unchanged
  branch at the captured base and a clean worktree after managed-link removal.
- [x] **T6** (R5, R9) — Implement `run`: verify the exact registered worktree and
  branch, change directory there, export the canonical main harness as
  `HARNESS_DIR`, execute the argument vector after `--` without re-parsing, and
  propagate its exit status.
- [x] **T7** (R11, R12) — Implement normal teardown identity, dirt, managed-link,
  merged-ancestry, and clean-primary-on-base checks; use only non-forced
  `git worktree remove`, prune, and `git branch -d`.
- [x] **T8** (R10, R13) — Make failure rollback and repeated/partial teardown
  idempotent and fail-safe, preserving and naming any resource that cannot be
  independently proven safe to remove.
- [x] **T9** (R1, R7, R14) — Update `.gitignore`, installer append-only ignore
  seeding/chmod wiring, and `docs/CONFIG-LAYERING.md` for the tracked worktree
  directory and explicit local-layer link policy.
- [x] **T10** (R14) — Extend `tests/test_install.sh` with fresh-install executable
  assertions, target-root `.claude/worktrees/` ignore
  preservation/idempotence checks, and a changelog-section SemVer policy test that
  does not depend on a tag, Git history, or a frozen current version.
- [x] **T11** (R14) — Bump `VERSION` one SemVer MINOR step and add the matching
  `CHANGELOG.md` entry without freezing that literal in a permanent test.
- [x] **T12** (R1–R13) — Add `tests/test_fix_worktree.sh` exactly as specified in
  `E15-F02.tests.md`, using only disposable local Git repositories, and append it to
  `verification.test_command` in `harness.config.yaml`.
- [x] **T13** (R1–R14) — Run `./init.sh`, `sh tests/test_fix_worktree.sh`,
  `sh tests/test_install.sh`, and the full configured verification command; resolve
  every failure before Builder handoff.
