---
id: E21-F07
---

# Plan — E21-F07

## Files to change

| File | Change |
|---|---|
| `tools/pr-stack-guard.sh` | DELETE |
| `harness-install.sh` | drop the tool's copy/chmod + manifest mention; delete the heredoc's base-change step (0b), merge-order guard block and `guard_deferred` branch; add upgrade reclamation of the orphaned installed tool |
| `.claude/commands/sdd-pr-loop.md` | mirror the same heredoc deletions (source-layout copy) |
| `docs/WORKFLOW.md` | lane section → short deprecation notice naming the sibling split |
| `specs/epics/E21-change-size-discipline/F04-stacked-pr-lane/E21-F04.spec.md` | append supersession note |
| `tests/test_pr_loop.sh` | delete the stacked suites; add the no-live-reference + default-lane-preserved assertions |
| `tests/test_stacked_doctrine.sh` | DELETE (its subject is gone); its fence-rule lesson already lives in `tests/lib/fence.awk` consumers |
| `VERSION`, `CHANGELOG.md` | 0.70.0 + entry |

## DO NOT TOUCH

`tools/pr-gate.sh` logic (only a prose/comment reference if any), `tools/pr-round-trend.sh`,
`wait-for-codex.sh`, the round cache contract, `agents/*.md` role bodies.

## Order

1. Delete tool + tests of the tool; 2. strip both command bodies; 3. WORKFLOW notice;
4. installer reclamation; 5. spec supersession note; 6. suite adjustments; 7. VERSION/CHANGELOG.
