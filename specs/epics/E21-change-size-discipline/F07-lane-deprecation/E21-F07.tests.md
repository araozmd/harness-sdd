---
id: E21-F07
---

# Tests — E21-F07

| R-id | Test | Where |
|---|---|---|
| R1 | fresh install ships no `tools/pr-stack-guard.sh`; an upgrade over a target that has the old file REMOVES it | `tests/test_install.sh` |
| R2 | neither command body (source copy nor a fresh install's `.claude/commands/sdd-pr-loop.md`) mentions `pr-stack-guard`, base-change restart, or a `guard_deferred` state | `tests/test_pr_loop.sh` |
| R3 | `docs/WORKFLOW.md` deprecation notice names the sibling split; the lane's how-to (restack procedure, delimiter convention) is gone — asserted fence-aware over the section | `tests/test_pr_loop.sh` |
| R4 | no live `pr-stack-guard` reference in the tree outside `CHANGELOG.md`, specs history and the deprecation notice; existing default-lane suites stay green | `tests/test_pr_loop.sh` + full suite |
| R5 | E21-F04 spec contains the supersession note (append-only: original prose retained) | `tests/test_pr_loop.sh` |
| R6 | CHANGELOG names the removal + reclamation; no test freezes the exact VERSION | `tests/test_install.sh` existing R10 pattern |
