# Per-round finding trend at the round cap — Tasks

- [x] **T1** (R1–R6) — Write `tools/pr-round-trend.sh`: series from `round-*/blocking.json`,
      verdict, concentration, text + json, exit 0 always / 4 on usage.
- [x] **T2** (R7) — Add step 4b to `.claude/commands/sdd-pr-loop.md`; update the cap row and
      the `needs-human` terminal state to carry the verdict.
- [x] **T3** (R7, R8) — Apply the same three edits to the installer's **generated** heredoc,
      with paths resolved against `.harness/`.
- [x] **T4** (R8) — `chmod +x tools/pr-round-trend.sh` in the installer.
- [x] **T5** (R1–R8) — Extend `tests/test_pr_loop.sh` with fixture round dirs.
- [x] **T6** — MINOR `VERSION` bump + `CHANGELOG.md` entry.
- [x] **T7** — Run `./init.sh` + the full `verification.test_command`; green before hand-off.
