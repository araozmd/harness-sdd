# Feature-size budget in the Driller and Architect — Tasks

- [x] **T1** (R1, R2) — Append the `change_size:` block with its five keys, defaults and
      rationale comments to the source `harness.config.yaml`.
- [x] **T2** (R3) — Seed the same block from `harness-install.sh` on the migration path,
      guarded by `^change_size:[[:space:]]*(#.*)?$`, byte-identical to T1's text.
- [x] **T3** (R3) — Name the block in the installer's `manifest.txt` PROJECT-OWNED section.
- [x] **T4** (R4, R5, R6, R10) — Add the "Feature-size budget" section to `agents/driller.md`:
      the `max_requirements` proxy, splitting along `depends_on`, the `epic.md` record, and
      the explicit never-`blocked`-on-size boundary.
- [x] **T5** (R7, R8, R10) — Add the over-budget stop-and-report rule and the override-line
      requirement to `agents/architect.md`.
- [x] **T6** (R9) — Document the two tiers in `docs/WORKFLOW.md`, stating that neither blocks.
- [x] **T7** (R1, R3) — Assert in `tests/test_install.sh`: block present after a fresh
      install, appended exactly once after an upgrade, user edits preserved.
- [x] **T8** (R4, R5, R6, R10) — Assert in `tests/test_sdd_drill.sh` that the INSTALLED
      `agents/driller.md` and `agents/architect.md` carry the rule and its boundaries.
- [x] **T9** — MINOR `VERSION` bump + `CHANGELOG.md` entry (installed body gains a capability).
- [x] **T10** — Run `./init.sh` + `verification.test_command`; ensure green before hand-off.
