# Pre-PR change-size check — Tasks

- [x] **T1** (R1–R8) — Write `tools/change-size.sh`: merge-base diff, classification,
      config-driven tiering, text + json output, exit-4 usage errors.
- [x] **T2** (R7) — Add `test_paths` / `generated_paths` to the `change_size:` block in
      `harness.config.yaml` **and** the installer heredoc, keeping the two byte-identical.
- [x] **T3** (R10) — `chmod +x` the tool in `harness-install.sh` (`copy tools` already ships it).
- [x] **T4** (R9) — Wire the check into `agents/reviewer.md` before the verdict.
- [x] **T5** (R9) — Wire it into `agents/orchestrator.md` before the PR is opened.
- [x] **T6** (R9) — Document the handoff check in `docs/WORKFLOW.md`.
- [x] **T7** (R1–R9) — Write `tests/test_change_size.sh` against a real throwaway git repo.
- [x] **T8** (R10) — Add installed-body assertions to `tests/test_install.sh`.
- [x] **T9** — Register `tests/test_change_size.sh` in `verification.test_command`.
- [x] **T10** — MINOR `VERSION` bump + `CHANGELOG.md` entry.
- [x] **T11** — Run `./init.sh` + the full `verification.test_command`; green before hand-off.
