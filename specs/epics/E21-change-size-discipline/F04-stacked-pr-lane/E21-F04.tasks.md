# Stacked-PR lane — Tasks

- [x] **T1** (R1–R5) — Write `tools/pr-stack-guard.sh` as an offline `evaluate` over
      `pr.json` + `open-prs.json`, with exit 0 / 6 / 4 and fail-closed on an unreadable base.
- [x] **T2** (R6) — Add the stack-guard subsection to step 6 of the source-layout
      `/sdd-pr-loop`.
- [x] **T3** (R6) — Apply the same subsection to the installer's generated heredoc, with
      paths resolved against `.harness/`.
- [x] **T4** (R6) — `chmod +x tools/pr-stack-guard.sh` in the installer.
- [x] **T5** (R7) — Document the lane in `docs/WORKFLOW.md`: wave-boundary seams, merge order,
      the retarget caveat, and that restacking is manual.
- [x] **T6** (R1–R7) — Extend `tests/test_pr_loop.sh` with fixture JSON.
- [x] **T7** — MINOR `VERSION` bump + `CHANGELOG.md` entry.
- [x] **T8** — Run `./init.sh` + the full `verification.test_command`; green before hand-off.
