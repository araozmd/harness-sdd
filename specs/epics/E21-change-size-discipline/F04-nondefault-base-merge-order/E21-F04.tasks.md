# Merge-order safety for a non-default PR base — Tasks

> Atomic, sequential steps. Work top to bottom, one at a time. Each names the R-id(s) it
> satisfies. T1 and T2–T4 are deliberately separate so the diff shows exactly what changed
> about the salvaged artifact.

## The guard

- [ ] **T1** — Restore `tools/pr-stack-guard.sh` **verbatim** from
      `git show 1873af9:tools/pr-stack-guard.sh`. No edits in this step.
- [ ] **T2** (R4) — Delete the `default_branch="main"` literal and rename the flag
      `--default-branch` → `--merge-target`. The flag becomes **required and non-empty**:
      missing **or empty** ⇒ exit `4` naming the argument. Add no replacement literal, no
      name list, and no `git`/`gh` lookup inside the guard.
- [ ] **T3** (R5) — Validate that `open-prs.json` parses as a **JSON array** before use; a
      zero-byte, truncated or object-shaped file ⇒ exit `4`, not a silent "nobody owns it".
- [ ] **T4** (R7, and the rename's tail) — Rewrite the guard's prose: strike "The feature
      stays atomic with respect to the default branch" and every neighbouring claim of
      atomic or simultaneous delivery; state instead that the guard enforces merge **order**,
      and that merging one increment publishes it to the merge target while the others are
      still open. Then move every remaining `--default-branch` / "default branch" reference
      onto the new vocabulary — **usage string, the escape-hatch comment, and both verdict
      `printf`s** (`base=%s (default branch) …` and `is not the default branch (%s)`), which
      are inaccurate the moment the flag carries an integration branch.
- [ ] **T5** (R1, R2, R3, R6) — Verify the verdict legs survived T1–T4 and extend R3's
      message: merge target ⇒ `0`; open PR's head ⇒ `6` naming the number as the PR to merge
      first; base nobody owns ⇒ `6` saying the PR has **not** been retargeted, that waiting
      will not clear it, and that the operator must retarget (`gh pr edit <pr> --base
      <branch>`) or set `HARNESS_MERGE_TARGET`. Confirm no `gh`, `git`, `curl` or network
      anywhere in the script.

## The loop body (apply to the source-layout copy first; **T11** mirrors all of it)

- [ ] **T6** (R8) — In `.claude/commands/sdd-pr-loop.md`, add the guard call at the **very
      top of the "Ready to merge" terminal state** — before the all-gates-green summary post,
      before the thread-resolution block, and before both `gh pr merge` calls. Resolve the
      merge target from `HARNESS_MERGE_TARGET`, else the repository default branch, and
      **fail closed on an empty result** rather than interpolating it. Capture `stack_rc`
      and dispatch `0` / `6` / `4`.
- [ ] **T7** (R8) — Document `HARNESS_MERGE_TARGET` in the command's env-only
      execution-knob line (`:34-35`) **and** in `docs/INSTALL.md:113-115`. The knob has
      **four** documented sites; the other two are T11 (the heredoc) and T12 (the seeded
      config header, outside the heredoc).
- [ ] **T8** (R9) — Apply the **nine** contract edits enumerated in `.plan.md`
      §"Terminal-states contract", in this order: 1 the `auto_merge: false` "one of two"
      sentence (`:461`); 2 "asked to land" → "attempted" (`:503`); 3 the needs-human
      "Reached by:" list gains exit `4` and **not** exit `6` (`:509`); 4 the three-successes
      closing sentence, whose trailing clause stops characterising the entries so the exit-`4`
      route is inside it (`:522`); 5 "either" → "any" (`:327`); 6 "both terminal states" →
      "every terminal state" (`:342`); 7 `docs/WORKFLOW.md:463`; 8 the new
      `### Merge order withheld (success)` subsection, which must distinguish both legs; 9
      the `tests/test_pr_loop.sh:1524` literal (see T15). Edits 1–6 and 8 exist twice — see
      T13. **The header's count, this task's count, and the numbered list must stay equal.**
- [ ] **T9** (R10) — Change the squash-message commit enumeration to the PR's own base ref;
      keep the empty-message fallback intact. Do not touch the post-merge cleanup block's
      `default_branch` lookup.
- [ ] **T10** (R11) — Document the retarget rule in the body: a base-ref change is not a
      review event — re-run the guard, post no `@codex review`, count no round.

## The installer, and the second copy

- [ ] **T11** (R8–R12) — Apply T6–T10 and T8's edits **identically** to the installer's
      generated heredoc (`harness-install.sh`, from `:2969`), with paths resolved against
      `.harness/`. The mirrored contract sites are `:3429`, `:3471`, `:3477`, `:3490`,
      `:3295`, `:3310`.
- [ ] **T12** (R8) — Add `HARNESS_MERGE_TARGET` to `harness-install.sh:326-328`, the seeded
      `harness.config.yaml` header comment. **This is outside the heredoc**, so T11 does not
      reach it — it is the site this repo's source-vs-installed drift keeps landing on.
- [ ] **T13** (R12) — Add `chmod +x "$H/tools/pr-stack-guard.sh"` beside the existing
      `wait-for-codex.sh` / `change-size.sh` / `pr-round-trend.sh` lines (`:1881-1883`).

## Tests

- [ ] **T14** (R1–R12) — Extend `tests/test_pr_loop.sh` per `.tests.md`, including every
      mutation check M1–M14.
- [ ] **T15** (R9) — Update the **existing** `test_body_handover_summary` (E18-F01 R46):
      the `'**both** terminal states'` literal at `:1524`, its failure message, and the
      `pass` line at `:1530`. Leave `specs/epics/E18-vendored-pr-loop/` alone — F04 R9
      supersedes R46's "both", it does not rewrite the shipped spec.
- [ ] **T16** (R8) — Add `HARNESS_MERGE_TARGET` to the R50 env-knob loop
      (`tests/test_pr_loop.sh:1589`) so `docs/INSTALL.md` stays policed for it like the
      other four knobs.

## Close-out

- [ ] **T17** — MINOR `VERSION` bump + `CHANGELOG.md` entry (the installed body gains a tool
      and a merge gate). Do not assert the version string in any test.
- [ ] **T18** — Run `./init.sh` + the full `verification.test_command`; green before
      hand-off. A red `test_body_handover_summary` means T15 was skipped.
