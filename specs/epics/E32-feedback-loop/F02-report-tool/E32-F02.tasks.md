# `tools/harness-report.sh` — Tasks

> Atomic, sequential steps. The Builder works them top to bottom and checks each one off
> when it is done. Each task names the R-id(s) it satisfies.

- [x] **T1** (R8) — Create `tools/harness-report.sh` (POSIX sh, `+x`, dash-clean) with the
  argument parser, `H` resolution (script grandparent), and the `redact` mode: read stdin,
  apply the R8 corpus (GitHub/generic tokens, emails, absolute `/home|/Users|/tmp|/private`
  paths) to fixed `[REDACTED-…]` tokens, write stdout, exit 0. Keep the redactor callable
  from the create path (used again in T7).
- [x] **T2** (R2, R3, R10) — Add a local `feedback` config reader for
  `$H/harness.config.yaml` (top-level `feedback:` section only, trailing comment stripped)
  and apply F01's resolution: `enabled` true-only ⇒ R2's silent exit 0; `repo` grammar
  `[HOST/]OWNER/REPO` with the shipped default and malformed ⇒ off; `max_per_session`
  `[0-9]+`, missing/invalid ⇒ `3`, `0` ⇒ no upstream. Derive the host and `gh` target.
  Do not source `harness-install.sh`.
- [x] **T3** (R4) — Implement the allow-list validator for `--trigger`, `--symptom`,
  `--role`, `--phase`, `--command`, `--exit-code` using the vocabularies/grammars in
  `E32-F02.plan.md`. `--command` is a **semantic** allow-list: a basename under `$H/tools/`,
  `init.sh`, `harness-install.sh`, or a shipped `sdd-*` unit present in the body. A missing
  `--trigger`/`--symptom` or any out-of-allow-list value = reject (no upstream, continue to
  T8's local copy, exit 0); there is no usage-error exit.
- [x] **T4** (R5) — Implement harness-owned file validation: normalise each `--file` to a
  harness-relative path (reject absolute and `..`), prefix `.harness/` when `H`'s basename
  is `.harness`, and accept it only when `harness-owned-paths.sh body "$H"` +
  `git ls-files --cached --others --exclude-standard` reports the project-relative path as
  part of the body. Drop everything else. No accepted file ⇒ the local-only branch (T8).
- [x] **T5** (R1, R6, R7) — Assemble the outbound artifacts from accepted fields only:
  the R6 title `[harness-feedback] <trigger> <symptom> in <first accepted file>`, the R1
  marker line (exact grammar from the plan), and the `Label: value` body lines. Assert-by-
  construction that free-form notes and dropped paths are never referenced.
- [x] **T6** (R9) — Add the pre-create duplicate search
  (`GH_HOST="$host" gh issue list --repo … --search "<title>" --json number --limit 1`).
  A result with ≥1 `"number"` ⇒ no create **and no comment**.
- [x] **T7** (R1, R8) — On the create path, run the T1 redactor over the assembled body's
  `Label: value` lines (the marker line stays byte-exact) and call
  `GH_HOST="$host" gh issue create --repo … --title "<title>" --body-file <tmp>`. Any
  `gh` failure or absence reaches T8.
- [x] **T8** (R10, R11) — Implement the session cap and the local copy. Ledger
  `$H/progress/feedback/.session-count` line `<session-id> <created>`; a different session
  token resets the count; missing/invalid token or `created >= N` ⇒ no upstream. Always
  write `$H/progress/feedback/<UTC-timestamp>.md` (collision suffix `-2`, `-3`, …) with the
  structured fields, the `--notes-file` text verbatim, and the outcome. Every operational
  outcome exits 0; `gh` missing/unauthenticated/failing is a `fallback` copy, never a
  non-zero exit.
- [x] **T9** (R12) — Add `chmod +x "$H/tools/harness-report.sh"` beside the tool `chmod`
  lines in `harness-install.sh`; bump `VERSION` to `0.86.0`; prepend a `## [0.86.0] — <date>`
  entry to `CHANGELOG.md`; replace the `docs/INSTALL.md` `## Harness feedback` sentence
  `**No reporter ships yet** — this block is accurate but inert until **E32-F02** … and
  **E32-F03** … land.` with one stating the report tool ships (E32-F02) and reporting stays
  inert until E32-F03's triggers land. Ensure `tools/harness-report.sh` is committed `+x`.
- [x] **T10** (R1–R12) — Create `tests/test_feedback_report.sh` (POSIX sh, `+x`, dash-clean)
  with one test per row of `E32-F02.tests.md`, the stubbed `gh`, and the installed-target
  fixture. Include the Discrimination requirements subsection's controls.
- [x] **T11** (R1–R12) — Grep `tests/` for a bare `0.85.0` current-version pin (R12) and sync
  it. Run `./init.sh`, then `./tools/run-tests.sh`, and confirm both green. Run
  `sh harness-install.sh --self` and confirm no generated-glue diff.
