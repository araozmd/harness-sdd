# Source-repo GitHub Action that labels marker-carrying issues — Tasks

> Atomic, sequential steps. The Builder works these top to bottom and checks each one off when
> it is done. Each task names the R-id(s) it satisfies.

- [x] **T1** (R1, R2, R3) — Create `.github/workflows/harness-feedback-labeler.yml` with the
  pinned `on:` block (`issues: types: [opened]` only) and `permissions:` block
  (`issues: write`, `contents: read`), the `label` job on `ubuntu-latest`, `actions/checkout`
  pinned by a full 40-hex SHA with the tag in a trailing comment, and the single step that runs
  `sh .github/scripts/feedback-labeler.sh label-issue "$ISSUE_NUMBER"` with `GH_TOKEN`,
  `GH_REPO`, and `ISSUE_NUMBER` supplied through `env:`. No issue-controlled text in any
  `${{ }}` or `run:` line.
- [x] **T2** (R4, R5, R6, R7, R11) — Create `.github/scripts/feedback-labeler.sh` (POSIX sh,
  `+x`, dash-clean) with the `parse <body-file>` mode: the strict R4 first-line anchored
  grammar (copied verbatim from `E32-F02.plan.md`, cited in a comment), the R5 "no
  `harness-feedback:` token after line 1" check, whole-string comparison (no line-oriented
  charset guards), and the R6 mapping. `parse` exits 0 always; empty output means "not a
  report". It must never source, `eval`, or `sh -c` the body.
- [x] **T3** (R2, R8, R9) — Add the `label-issue <issue-number>` mode: validate the number
  `^[0-9]+$`; fetch the body with
  `gh api "repos/$GH_REPO/issues/<n>" --jq .body > "$body_file"`; run the parse algorithm on
  the file; on an empty set exit 0 with no `gh` mutation; otherwise probe each label,
  **treating only a definitive absent (404 / empty list) as "needs creating" and aborting
  non-zero on any other probe error**, create only the missing ones with the fixed
  color/description, then add the set with one
  `gh issue edit "<n>" --repo "$GH_REPO" --add-label "<l1>,<l2>"`. No comment call anywhere.
- [x] **T4** (R10) — Confirm the change is source-only: no edit to `harness-install.sh`, the
  emitters, `VERSION`, or `CHANGELOG.md`, and no installer reference to the labeler script or
  workflow. If a stray edit was made, revert it; nothing here ships a bump.
- [x] **T5** (R11) — Append `.github/scripts/feedback-labeler.sh` to the `_sse_expected` list in
  `tests/test_install.sh`'s `test_source_scripts_are_executable` function (one token; precedent
  E32-F02 commit `04e1ae9`). That suite equality-checks the derived set, so without this
  declaration `test_install.sh` reds "undeclared script(s)". Touch no other line of that file.
- [x] **T6** (R1–R11) — Create `tests/test_feedback_labeler.sh` (POSIX sh, `+x`, dash-clean)
  with one test per row of `E32-F04.tests.md`, the stubbed `gh`, the fixture bodies (written
  under the suite's `mktemp` tree, never `tests/fixtures/`), and the Discrimination
  requirements subsection's controls. Cover valid markers for all four triggers, every
  rejection shape (missing/malformed/`v2`/second marker/not-first-line), the injection bodies
  with their canary controls, the label ensure/apply/no-comment path (including the probe
  present/absent/hard-error split and the fetch-failure exit), and the static workflow
  predicates each with a positive control.
- [x] **T7** (R1–R11) — Run `sh -n` on the new script and suite, assert
  `[ -x .github/scripts/feedback-labeler.sh ]` and `[ -x tests/test_feedback_labeler.sh ]`,
  then run `./init.sh` and `./tools/run-tests.sh` and confirm both green (including
  `test_install.sh`). Re-run any unrelated red suite alone before chasing it; report the strict
  shell the runner names.
