# Reporting rule + `/sdd-report` command — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom, one at a time.
> Each task names the R-id(s) it satisfies. Check off when done. Do not hand-write any
> `.claude/*`, `.opencode/*`, or `.agents/skills/*` file — `--self` generates them.

- [ ] **T1** (R1, R2, R3) — In the source root `AGENTS.md`, add a short
  `## Reporting harness defects` section naming the four trigger tokens
  (`harness-malfunction`, `contradictory-instruction`, `workaround`, `missing-capability`),
  the three non-trigger categories, and the file-at-end-of-task / early-stop /
  never-mid-flow wording. Keep it to a few lines.
- [ ] **T2** (R4) — In the same section, state that only the session-owning top-level role
  (Orchestrator, or Fixer/Inception/Planner/Driller owning the session) invokes `/sdd-report`,
  and that sub-agents append entries to `progress/feedback/notes.md` (format: a `## <trigger>`
  heading plus `symptom:` / `command:` / `phase:` and an optional `file:`) and never invoke
  the reporter.
- [ ] **T3** (R9) — In `AGENTS.md`'s Start-every-session step 1, extend the existing
  "STOP and report" sentence with the single exception: a non-zero `init.sh` exit that is a
  harness malfunction permits one F02 reporter call as the report (trigger
  `harness-malfunction`), then stop — no repair, no board write, no continuation, never a way
  around the gate.
- [ ] **T4** (R5) — Add the short filing-duty line to `agents/orchestrator.md`,
  `agents/fixer.md`, `agents/driller.md`, `agents/planner.md`, `agents/inception.md`.
- [ ] **T5** (R5) — Add the short note-duty line to `agents/architect.md`, `agents/builder.md`,
  `agents/reviewer.md`, `agents/scout.md`, `agents/doc-critic.md`, `agents/pr-fixer.md`.
  (`agents/builder-heavy.md` needs no edit — it delegates to `builder.md`.)
- [ ] **T6** (R7) — In `harness-install.sh`, append `sdd-report` to `HARNESS_SDD_CMDS`
  (~line 1776) and update the Claude, OpenCode and shared-skill `ok` messages to name
  `/sdd-report` (`\$sdd-report` in the skill-unit message; escape the `$`).
- [ ] **T7** (R8) — Add the `cat > "$CMDDIR/sdd-report.md"` body beside the other command
  bodies: it identifies a trigger (refusing non-triggers), picks a symptom from F02's
  vocabulary (`init.sh` failure → `init-failure`), and calls
  `.harness/tools/harness-report.sh` with the allow-listed upstream flags. It must pass **at
  least one harness-owned `--file`** (`init.sh`, else the failing harness tool/path), pass
  `--session-id` as `HARNESS_FEEDBACK_SESSION_ID` with the grammar-safe `date -u
  +%Y%m%dT%H%M%SZ` fallback when unset (never leaving the token unset — F02 fails closed),
  pass the free-form summary via the local-only `--notes-file`, state that free-form text is
  never sent upstream, and re-implement none of F02's mechanisms.
- [ ] **T7b** (R8) — In `AGENTS.md` (and the session-owning role lines), state that the role
  mints `HARNESS_FEEDBACK_SESSION_ID` once per session from the `session-start` `started_at`
  normalized to F02's grammar, or from `date -u +%Y%m%dT%H%M%SZ` on the `init.sh` hard-stop
  path where no marker exists.
- [ ] **T8** (R6, R7) — Run `sh harness-install.sh --self` and commit the regenerated glue:
  `.claude/commands/sdd-report.md`, `.opencode/command/sdd-report.md`,
  `.agents/skills/sdd-report/SKILL.md` + `agents/openai.yaml`, and `.claude/.glue-manifest`.
  Confirm `sh tests/test_self_drift.sh` is green before moving on.
- [ ] **T9** (R10) — Add `sdd-report` to `tools/harness-report.sh`'s `_shipped_sdd_cmds`, then
  run `sh tests/test_feedback_report.sh` to confirm `test_shipped_command_set_converges`
  stays green.
- [ ] **T10** (R11) — Bump `VERSION` to `0.87.0`; prepend a `## [0.87.0] — <date>`
  `CHANGELOG.md` entry naming reporting going live; rewrite the `docs/INSTALL.md` feedback
  section's "inert until E32-F03" sentence so it states reporting is live while keeping
  `E32-F02`/`E32-F03` references; add `/sdd-report` / `$sdd-report` to `README.md`'s
  Supported-CLIs table and its `$sdd-*` sentence.
- [ ] **T11** (R11) — Release sweep: sync the current-`VERSION` literals in
  `tests/test_codex_native.sh`, `tests/test_feedback_config.sh`, and
  `tests/test_feedback_report.sh`; re-anchor `test_feedback_report.sh`'s R12 pristine-bytes
  control on the F02 sentence this feature removes. Leave historical `## [0.85.0]`/`## [0.86.0]`
  CHANGELOG anchors untouched.
- [ ] **T12** (R1–R11) — Write `tests/test_reporting_rule.sh` exactly per
  `E32-F03.tests.md` (all heading slicers call `tests/lib/fence.awk`'s `fence_delim`).
- [ ] **T13** (R1–R11) — Run `sh tests/test_reporting_rule.sh` standalone, then
  `sh tests/test_change_size.sh` standalone (new-file convention check), then
  `./init.sh` and `sh tools/run-tests.sh`; ensure green before hand-off.
