# `feedback:` config block + opt-out notice — Tasks

> Atomic, sequential steps. The Builder works them top to bottom and checks each one off
> when it is done. Each task names the R-id(s) it satisfies.

- [ ] **T1** (R1) — Append the `feedback:` block as the final block of `harness.config.yaml`
  (after `workers:`): a blank separator line, then a comment header starting with
  `# Harness feedback (E32-F01)` (no interior blank lines), then `feedback:` with
  `enabled: true`, `repo: github.com/araozmd/harness-sdd`, `max_per_session: 3`. Do not
  edit any other line of the file.
- [ ] **T2** (R3, R4) — In `harness-install.sh` `migrate_config`, add a `feedback` entry
  **after** the `workers:` entry. Its presence check is the wider `^feedback:([[:space:]]|$)`
  (so the one-line flow form counts as present; see plan step 1), and
  its `<<'EOF'` heredoc is byte-identical to the T1 block. Add a comment above the entry
  saying the heredoc must stay byte-identical to the seed template, in the same style as
  the `workers:` entry.
- [ ] **T3** (R5) — Add `_cfg_feedback_value <file> <key>` next to `_cfg_pr_loop_value`.
  It reads a key from the top-level `feedback:` section only, strips any trailing comment,
  and returns empty when the key is unset.
- [ ] **T4** (R2, R5, R6, R7) — In `install_one` §2 (seed/preserve), work out whether the
  block was seeded this run. The fresh branch always seeds it. On the preserve branch, the
  probe `^feedback:([[:space:]]|$)` missed before `migrate_config` and matches after it. When the block was seeded
  and `SELF_MODE` is not `1`, emit exactly one `info` line containing the
  `_cfg_feedback_value … repo` value, the literal `feedback.enabled: false`, and
  `$H/harness.config.yaml`. Leave `migrate_config` itself silent.
- [ ] **T5** (R8) — Confirm there is no cascade-specific change to make: coordinator and
  children each go through `install_one`. Add no summary line.
- [ ] **T6** (R9, R10) — `docs/INSTALL.md`: add the `## Harness feedback (`feedback:`) — on by
  default` section, covering the defaults, the notice, the full resolution contract, the
  `enabled: false` opt-out and the fact that a deleted block is re-seeded, the host grammar
  (2 or 3 parts of `[A-Za-z0-9._-]+`, omitted host = `github.com`, anything else ⇒ OFF),
  the `enabled` bare-token rule, the `max_per_session` rule (negatives ⇒ `3`), the
  child-governs rule, and the fallback dir
  `progress/feedback/` under the governing harness dir. Add `feedback:` to the example
  list in `## Config migration on upgrade`.
- [ ] **T7** (R10) — `docs/CONFIG-LAYERING.md`: add the paragraph saying `feedback.*` is
  project layer (committed, per repo) and that a child's own block governs, with no
  inheritance from the coordinator.
- [ ] **T8** (R11) — Bump `VERSION` to `0.85.0`. Prepend a `## [0.85.0] — <date>` entry to
  `CHANGELOG.md` that names the block, `enabled: true` default-on, the default repo, the
  notice, and the `feedback.enabled: false` opt-out. Grep `tests/` for a bare `0.84.4`
  literal and sync any current-version pin.
- [ ] **T9** (R1–R11) — Create `tests/test_feedback_config.sh` (POSIX sh, `+x`, dash-clean)
  with one test per row of `E32-F01.tests.md`.
- [ ] **T10** — Update any existing assertion that assumed `workers:` is the final config
  block, if the suite shows one. Do not delete coverage.
- [ ] **T11** — Run `sh harness-install.sh --self` and confirm it leaves no diff in the
  generated glue or in `harness.config.yaml`. Then run `./init.sh` and
  `./tools/run-tests.sh` and make sure both are green before hand-off.
