# E27-F01 — Tasks

- [ ] T1 — `install_one`: compute `UMB_MODELS_CFG` once per target from
  `umbrella_body_dir "$H"` (empty for coordinator/single-repo). (R1, R6)
- [ ] T2 — Refactor `model_tier` into `_model_tier_resolve` (sets `_mtr_tier`,
  `_mtr_src`) + printer; add the coordinator fallback chain with its own warn-once
  key. (R1, R2, R7)
- [ ] T3 — `resolve_model`: coordinator pin fallback before the pin guards. (R3)
- [ ] T4 — Per-target report line when any role resolves from the umbrella. (R5)
- [ ] T5 — New suite `tests/test_models_cascade.sh`: R1–R7 fixture matrix. (R8)
- [ ] T6 — VERSION 0.73.0, CHANGELOG entry, run full suite. (R8)
