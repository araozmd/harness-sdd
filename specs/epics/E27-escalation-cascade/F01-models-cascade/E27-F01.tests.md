# E27-F01 — Tests (tests/test_models_cascade.sh)

Fixture: umbrella with coordinator `.harness` installed (real cascade run), coordinator
`models:` block set to `builder: standard`, `builder-heavy: reasoning`; two git
children — `child-a` (all-inherit config) and `child-b` (`builder: cheap` explicit).
Claude-only selection for speed. Sandbox `CODEX_HOME`.

- **R1 / cascade-resolves** — after the cascade, `child-a`'s `.claude/agents/builder.md`
  carries `model: sonnet` and `builder-heavy.md` carries `model: opus` (coordinator
  tiers flowed through the alias table).
- **R2 / child-wins** — `child-b`'s `builder.md` carries `model: haiku` (own `cheap`
  beats coordinator `standard`); its `builder-heavy.md` still gets `model: opus`
  (absent key cascades).
- **R3 / pin-cascade** — coordinator `pin.claude.standard: "sonnet-pinned"` with no
  child pin ⇒ `child-a` `builder.md` stamps `model: sonnet-pinned`; `child-b` keeps its
  own resolution unaffected.
- **R4 / verdict-recomputed** — `child-a/.harness/.escalation-arming` reads
  `armed` + `claude=raise` (sonnet→opus differs) purely from coordinator tiers.
- **R5 / report-line** — cascade output holds one line for `child-a` naming
  `builder=standard(umbrella)`; `child-b`'s line names `builder=cheap(own)`.
- **R6 / single-repo-unchanged** — a plain install with the same all-inherit config
  stamps NO `model:` key and prints no cascade report line; `test_model_routing.sh`
  stays green (regression net).
- **R7 / coordinator-garbage-tolerated** — coordinator `builder: turbo9` ⇒ cascade
  exits 0, warns once naming the umbrella source, `child-a` stamps no builder model.
- **R8 / version** — VERSION bumped MINOR vs the previous CHANGELOG entry, CHANGELOG
  names E27-F01. (Asserted by the existing changelog/version conventions, not frozen
  to an exact string.)

Mutation kills (manual, recorded in the PR): drop the coordinator fallback in
`_model_tier_resolve` → R1/R4 fail; drop the child-wins guard → R2 fails; skip pin
fallback → R3 fails; report unconditionally → R6 fails.
