# Thin the child: umbrella-resolved body with local fallback — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom, one at a
> time. Each task names the R-id(s) it satisfies. Check off when done.

- [x] **T1** (R1, R5) — Seed an `umbrella:` block with a documented, empty `root:` key in
      `harness.config.yaml`, and make the config seed/preserve stage treat it as
      project-owned (preserved on upgrade, never overwritten).
- [x] **T2** (R2, R5) — Add `umbrella_body_dir <harness-dir>`: read `umbrella.root`
      section-scoped with the same `awk`+quote-stripping shape
      `tools/harness-owned-paths.sh` uses for `telemetry.log`; print the absolute
      `<umbrella>/.harness` only when it exists, is a real directory (not a symlink) and
      holds `.harness-version`. Print nothing otherwise.
- [x] **T3** (R3, R7) — Add `gen_body_stub <relpath> <umbrella-body> <dest>` per the plan's
      template. The text must depend only on the relative umbrella path and the
      body-relative path — never on `VERSION` or file content. First line is the
      `<!-- harness:umbrella-stub -->` sentinel.
- [x] **T4** (R9) — Add `child_is_full_copy <harness-dir>`: true when any resolvable-tier
      file exists and does not begin with the sentinel.
- [x] **T5** (R2, R4, R5, R9) — Split §1's `copy` list into `HARNESS_BODY_PROSE` and
      `HARNESS_BODY_LOCAL`. Copy the local tier unconditionally. Stub the prose tier only
      when `umbrella_body_dir` resolves **and** `child_is_full_copy` is false; otherwise
      copy it. Report the layout chosen on stdout.
- [x] **T6** (R1) — In the cascade child loop, write `umbrella.root` into the child's
      `harness.config.yaml` **before** `install_one` runs, so §1 can act on it.
- [x] **T7** (R8) — Add a THIN CHILD section to `manifest.txt` listing the stub paths and the
      local paths, emitted only for a target that actually resolved an umbrella.
- [x] **T8** (R6) — In `init.sh`, resolve `umbrella.root` and print one `ℹ️` line: resolved
      (with the path) or unreachable (with the path and the fact that this is not an error).
      It must not affect the exit code on any path.
- [x] **T9** (R1, R2, R7, R9) — `tests/test_umbrella.sh`: the cascade records `umbrella.root`;
      a fresh child's prose tier is stubs while its standalone tier is full copies; a
      `VERSION` bump leaves stubs byte-identical **with a positive control** proving the
      standalone tier changed in that same run; an existing full-copy child is left alone and
      reported.
- [x] **T10** (R4, R5) — `tests/test_install.sh`: a single-repo install (no `umbrella.root`)
      writes the complete body with no sentinel anywhere; the standalone tier is a full local
      copy in both layouts.
- [x] **T11** (R6) — `tests/test_init_drift_guard.sh`: a thin child whose umbrella has been
      **moved away** still exits zero from `init.sh` and says so; paired with the reachable
      case in the same fixture so "exit 0" is not reachable by a check that never ran.
- [x] **T12** (R3) — Assert the stub contract itself: sentinel first line, the resolved path
      present, and the recovery instruction present.
- [x] **T13** — Update `docs/UMBRELLA.md`, `docs/INSTALL.md`, `docs/CONFIG-LAYERING.md`;
      bump `VERSION` (MINOR) and add the `CHANGELOG.md` entry, stating plainly that no
      existing target changes layout and that E24-F04 carries the migration.
- [x] **T14** — Mutation-test after committing: neuter `umbrella_body_dir` (R2 dies, R5
      lives), put `VERSION` in the stub text (R7 dies), make `init.sh` fail on an unreachable
      umbrella (R6 dies). Record the pairs in `progress/`.
- [x] **T15** — `sh tools/run-tests.sh` + `sh tools/change-size.sh`; touched suites under
      `dash`; `./init.sh` green before hand-off.
