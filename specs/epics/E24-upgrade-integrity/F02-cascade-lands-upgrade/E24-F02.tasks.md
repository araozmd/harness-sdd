# The cascade lands the upgrade — Task List

- [ ] **T1** — Create `tools/harness-owned-paths.sh` (POSIX sh, `body`/`all` modes), MOVING the
      pathspec logic out of `init.sh` verbatim — exclusions, `.agents/` narrowing, OpenCode
      glue, and the ledger resolution with its symlink + regular-file + `:(literal)` rules.
      *(R8)*
- [ ] **T2** — Rewire `init.sh` to read `HARNESS_BODY`/`HARNESS_OWNED` from the helper. Warn
      once and skip the drift check when the helper is absent or not executable. *(R8)*
- [ ] **T3** — Run the existing drift-guard suite unchanged. It must be green before any audit
      code is written — that is the extraction's regression contract. *(R8)*
- [ ] **T4** — Add the landing audit at the end of the cascade, over `$UMB` + `INSTALLED_CHILDREN`.
      Read-only git calls only. *(R1, R9)*
- [ ] **T5** — Report per target: `landed` / `unlanded <n> path(s)` / `no git`. *(R4, R5)*
- [ ] **T6** — Exit **3** when any target is unlanded; print one confirming line and exit 0
      otherwise. Document exit 3 in the usage header. *(R2, R3, R6)*
- [ ] **T7** — Verify `--dry-run` still returns before the audit and writes nothing. *(R7)*
- [ ] **T8** — Tests per `.tests.md`, including the R8 differential and the helper-missing
      degradation. *(R1–R9)*
- [ ] **T9** — `sh tools/run-tests.sh` fully green.
- [ ] **T10** — `VERSION` MINOR bump + `CHANGELOG.md`; document the audit, exit 3, and that it
      never commits, in `docs/INSTALL.md` and `docs/UMBRELLA.md`.
