# Per-developer local-overrides convention — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom, one at a time. Each task names the R-id(s) it satisfies. Check off when done.

- [x] **T1** (R1, R2, R3, R4, R16) — Modify `harness-install.sh` managed entrypoint generation so `AGENTS.md`, `CLAUDE.md`, and `GEMINI.md` include conditional local-override guidance for `AGENTS.local.md`, including committed-instructions-win precedence wording, while preserving non-harness prose outside the managed block.
- [x] **T2** (R6, R7, R8) — Extend the existing project-root `.gitignore` append-only seed block in `harness-install.sh` to include `AGENTS.local.md`, `CLAUDE.local.md`, and `AGENTS.override.md` without introducing a second seeding path.
- [x] **T3** (R5, R9, R10) — Update `docs/CONFIG-LAYERING.md` to document the personal prompt layer, portable/native local filenames, additive precedence, gitignored behavior, fresh-worktree caveat, and per-tool caveats.
- [x] **T4** (R11) — Update `umbrella.gitignore.example` to include `AGENTS.local.md`, `CLAUDE.local.md`, and `AGENTS.override.md` for umbrella/shared-spec repository roots.
- [x] **T5** (R12) — Extend `tests/test_install.sh` fresh-install assertions to require all personal prompt ignore entries in the project-root `.gitignore`.
- [x] **T6** (R13) — Extend `tests/test_install.sh` upgrade/idempotence assertions to prove the personal prompt ignore entries are not duplicated and existing user-authored `.gitignore` entries survive.
- [x] **T7** (R14, R1, R3, R4, R16) — Extend `tests/test_install.sh` generated-entrypoint assertions to check that `AGENTS.md`, `CLAUDE.md`, and `GEMINI.md` contain conditional local-override guidance and committed-instructions-win wording.
- [x] **T8** (R15) — Bump `VERSION` by one MINOR version and add a matching `CHANGELOG.md` entry for the local-overrides convention, installer ignore seeding, and docs/tests coverage.
- [x] **T9** — Write tests per `E09-F02.tests.md`.
- [x] **T10** — Run `./init.sh` plus the configured verification command; ensure green before Builder hand-off to Reviewer.
