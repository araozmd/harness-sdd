# Doc-critic: advisory review pass over harness-generated docs — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom,
> one at a time. Each task names the R-id(s) it satisfies. Check off when done.

- [x] **T1** (R1–R8) — Create `agents/doc-critic.md` as a portable reviewer contract:
  define the three `target-type` scopes (`plan-output`, `epic-decomposition`,
  `feature-spec`), the calibration (only real downstream problems across
  completeness/consistency/clarity/scope/YAGNI), advisory-only behavior,
  inline-fix-then-proceed, best-effort error/timeout handling, progress-note output,
  and the boundary that it reviews documents only.
- [x] **T2** (R9, R10) — Modify `agents/planner.md` to add the doc-critic checkpoint
  after `/sdd-plan` artifact generation and before re-validation/reporting, and add
  the drillable-minimum checklist for each seeded `epic.md`. Reconciled in round 2:
  the per-epic `epic.md` contract now unambiguously requires the drillable-minimum
  five elements while still prohibiting feature specs, F01, EARS, and detailed
  technical plans.
- [x] **T3** (R11) — Modify `agents/driller.md` to add the doc-critic checkpoint after
  epic decomposition and before re-validation/reporting.
- [x] **T4** (R12) — Modify `agents/architect.md` to add the doc-critic checkpoint after
  drafting the four-file spec and before the `spec-ready` hand-off.
- [x] **T5** (R13) — Modify `harness-install.sh` so that `agents/doc-critic.md` is copied
  into `.harness/agents/` on every install/upgrade.
- [x] **T6** (R14) — Extend `tests/test_install.sh` with assertions that
  `.harness/agents/doc-critic.md` is installed and that the installed Planner,
  Driller, and Architect references point at the doc-critic role.
- [x] **T7** (R1–R18) — Create `tests/test_doc_critic.sh` with static grep contract
  assertions over `agents/doc-critic.md` and the three checkpoint wirings.
- [x] **T8** (R16) — Modify `docs/WORKFLOW.md` to add a distinct `## Doc-critic checkpoints`
  section documenting the three checkpoints and the advisory/inline-fix/best-effort
  behavior.
- [x] **T9** (R15) — Bump `VERSION` by one MINOR version and add a matching
  `CHANGELOG.md` entry describing the doc-critic role and checkpoint wiring.
- [x] **T10** (R1–R18) — Modify `harness.config.yaml` to append
  `&& sh tests/test_doc_critic.sh` to `verification.test_command`.
- [x] **T11** — Write tests per `E09-F01.tests.md`.
- [x] **T12** — Run `./init.sh` + the test command; ensure green before hand-off.
