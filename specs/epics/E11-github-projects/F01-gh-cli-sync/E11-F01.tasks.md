# GitHub Projects sync via `gh` CLI (no MCP) — Tasks

> Atomic, sequential, independent steps. The Builder works these top to bottom,
> one at a time. Each task names the R-id(s) it satisfies. Check off when done.
> Order: preflight (tool) → docs pin → installer-test assertion → mirror-suite cases →
> gate wiring → VERSION bump → green gate.
>
> This feature **extends** the existing `tools/sync-board.mjs` `github-projects` path.
> Do **not** create a second GitHub tool/code path (R2), and do **not** add or expand
> `owner → assignee` semantics — that is E10 (R9).

- [x] **T1** (R1, R2, R6, R7, R9) — In `tools/sync-board.mjs`, strengthen the
  `github-projects` **preflight**: replace/extend the thin `gh --version` existence
  check with a `gh`-only capability check that verifies `gh` is present **and** meets the
  minimum version / has the `project` (Projects v2) + `repo` scopes needed, and exits
  **non-zero** with an actionable message naming `gh` and the Projects-v2/scope
  requirement — **before** any board-mutating `gh` call. Keep the transport `gh`-only (no
  MCP). Do **not** add a second GitHub code path; do **not** touch the `assignee`
  reconcile logic or the `jira`/`azure-boards` stubs; preserve the inert-default early
  return (empty provider ⇒ no `gh` contact).

- [x] **T2** (R6, R13) — In `store/board-mirror.md`, pin the contract: supported Project
  type is **Projects v2** (state Classic is unsupported); name the **minimum `gh`
  version** and the required **`project` + `repo`** auth scopes; reaffirm the **`gh`-only
  / no-MCP** transport and the **one-way** (`tasks.json` → board, agents never read the
  board) invariant.

- [x] **T3** (R12) — In `tests/test_install.sh`, add an assertion that a **fresh install**
  ships an **executable** `.harness/tools/sync-board.mjs` (installer wiring asserted).
  Sandbox `CODEX_HOME` for the installer-invoking case (existing suite guard); reuse the
  suite's temp-dir pattern.

- [x] **T4** (R7) — In `tests/test_mirror.sh`, add a behavioral case for the strengthened
  preflight: with `provider: github-projects` fully configured but a fake `gh` that is
  absent / reports a too-old version / lacks the required scope, the tool exits non-zero,
  the message names `gh` (and the Projects-v2/scope requirement), and it makes **no**
  board mutation. Node-absent skip-and-report pattern preserved. Assert **behavior**,
  never pin the exact VERSION.

- [x] **T5** (R1, R3, R4, R10, R11) — In `tests/test_mirror.sh`, confirm (add if missing)
  the regression guardrails for the completed feature: inert default is a true no-op that
  never calls `gh` (R4 — exists); a configured run dispatches only to `gh` and never to
  any MCP transport (R1); a re-run reconciles without duplicating (R3); `--dry-run`
  mutates nothing (R11); and the one-way invariant holds — the tool never writes
  `state/tasks.json` (R10). Extend the existing suite; do not fork it; do not diff
  `state/tasks.json` against `main`.

- [x] **T5b** (R5, R8) — In `tests/test_mirror.sh`, confirm (add if missing) the two
  pre-existing config guardrails the completed feature relies on: the tool reads
  `owner`/`project_number`/`repo` from the **existing** `mirror.board` shape and this
  feature introduces **no** new committed `mirror.board.*` key or secret (R5); and a
  `github-projects` run missing a required key exits non-zero, names the key, and errors
  **before** any `gh` call (R8 — existing `::ghprojects_misconfig_errors`). Extend the
  existing suite; assert behavior only.

- [x] **T6** (R14) — Bump `VERSION` one **MINOR** step (additive, backward-compatible;
  SemVer MINOR, not MAJOR) and add a matching `CHANGELOG.md` entry describing the
  completed `gh`-CLI GitHub Projects (v2) mirror: preflight, docs pin, installer-test
  assertion.

- [x] **T7** — Write/finish the tests per `E11-F01.tests.md`; ensure every R-id maps to a
  passing test (`tests/test_mirror.sh` + `tests/test_install.sh`, both already in
  `verification.test_command`).

- [x] **T8** — Run `./init.sh` + the full `verification.test_command`; ensure green
  before hand-off.
