# `specs/glossary.md` becomes project-owned — Tasks

> Atomic, sequential steps. Work them top to bottom, one at a time. Each names the `R-id`(s)
> it satisfies. T1 and T2 must land as one unit before anything is verified: the tier removal
> on its own would leave a fresh install with **no** glossary at all — and `agents/planner.md`
> names `specs/glossary.md` as a path an installed target holds, so a target without one would
> ship a dangling reference in a role prompt.
>
> Read `.plan.md` → *Test fixtures that currently stand on this path* before T9. Read
> `progress/lessons.md` before writing any test.

## A. The behaviour

- [ ] **T1** (R7, R8, R9) — `harness-install.sh` ~703: remove `specs/glossary.md` from
      `HARNESS_BODY_PROSE`, leaving exactly
      `HARNESS_BODY_PROSE='AGENTS.md agents docs specs/_templates'`. Change nothing else on
      that line and add no special case in any consumer of the variable.
- [ ] **T2** (R1–R6, R8) — `harness-install.sh` §2, immediately after the `specs/product.md`
      seed block (3755-3776): add the seed/pristine/stub/preserve block from `.plan.md` §2.
      Branch order is load-bearing: non-regular file → absent → `cmp -s` pristine → stub
      sentinel → preserve. Use the four **pinned verdict substrings** from `.plan.md` §2
      verbatim (`seeded specs/glossary.md`, `specs/glossary.md pristine`,
      `specs/glossary.md preserved`, `specs/glossary.md re-materialised`), each on exactly one
      `info` line, so a run emits exactly one stdout line containing `specs/glossary.md` (R5).
- [ ] **T3** (R3) — Confirm the first branch really is unfollowable: point the path at a file
      outside the tree with `ln -s`, run the installer by hand, and check the symlink and its
      target are both untouched. Fixed here, asserted in T11.
- [ ] **T4** — Smoke it by hand before touching any test: install into a temp dir, confirm the
      glossary appears and the run says `seeded`; re-run, confirm `pristine`; append a line,
      re-run, confirm `preserved` and that the appended line survives; delete it, re-run,
      confirm `seeded` again.

## B. The other owner of the same fact

- [ ] **T5** (R10) — `tools/harness-owned-paths.sh`, `emit_body()`: add
      `printf ':(exclude)%sspecs/glossary.md\n' "$pfx"` beside the `specs/product.md`
      exclusion, matching the surrounding style. Both consumers (`init.sh`'s drift guard and
      the installer's post-cascade landing audit) read this one function.

## C. Prose that must agree with the code

- [ ] **T6** (R11) — `harness-install.sh` header, lines 59-62: add `specs/glossary.md` to the
      PROJECT-authored enumeration.
- [ ] **T7** (R11) — `harness-install.sh` `manifest.txt` emitter: remove the path from the
      `HARNESS-OWNED` block (~3996) and from the `prose (stub-able)` tier line (~4060); add it
      to the `PROJECT-OWNED` block beside `.harness/specs/product.md`.
- [ ] **T8** (R11) — Docs and the config seed, one edit each:
      `harness.config.yaml` (~202, the `umbrella.root` prose-tier comment),
      `docs/INSTALL.md:63`, `docs/INSTALL.md:681`, `docs/INSTALL.md:993`,
      `docs/CONFIG-LAYERING.md:22`, `docs/UMBRELLA.md:101` — plus **one new sentence** under
      the UMBRELLA.md tier table recording decision D1 (a child's glossary is project-owned,
      real in every layout, never a stub). See `.plan.md` §3 for the per-file intent.

## D. Tests

- [ ] **T9** (R7, R8, R9) — `tests/test_umbrella.sh`: re-point the two fixtures that stand on
      this path, in this order, running the suite alone after each:
      (a) drop `specs/glossary.md` from `PROSE_TIER` (856) and `F04_TIER` (1306), **and add the
      `F04_TIER` equality assertion** — extract the `HARNESS_BODY_PROSE=` value from
      `$SRC/harness-install.sh` and compare it to `$F04_TIER`. `.plan.md` explains why this is
      not optional: without it, F01 silently retires a mutation kill E24-F04 recorded;
      (b) move the whitespace-only byte-identity edit (1522-1589) to another prose-tier regular
      file no other fixture in the suite touches, keeping both preconditions and the
      `differs: <path>` assertion;
      (c) move the "umbrella does not hold this tier entry" fixture (2321-2378) to another tier
      entry, updating the stderr grep, the `$SRC` control, the reference copy and the control
      loop.
      Update each fixture's comment to explain the **new** choice — do not leave prose
      describing the glossary.
- [ ] **T10** (R7, R8, R9) — `tests/test_umbrella.sh`: add the three new cases from
      `.tests.md` — `glossary_never_stubbed_in_any_layout`,
      `glossary_stub_rematerialised_in_thin_child`,
      `glossary_edit_does_not_block_thin_conversion`.
- [ ] **T11** (R1–R6) — `tests/test_install.sh`: add **six** shell functions, one per R-id,
      named exactly as `.tests.md`'s matrix names them (`test_glossary_seeded_on_fresh_install`,
      `test_glossary_pristine_verdict`, `test_glossary_edit_preserved_on_upgrade`,
      `test_glossary_reseeded_when_deleted`, `test_glossary_reports_one_verdict_line`,
      `test_glossary_seed_is_idempotent`), dispatched where that suite dispatches its other
      `test_*` functions. The matrix's `file::name` lookups depend on those names.
      `test_glossary_edit_preserved_on_upgrade` also carries the non-regular-file arm (R3):
      symlink the path outside the tree, re-run, assert the link and its target are both
      unchanged and that the shipped example was not written through it.
      Follow `test_lessons_ledger_seeded_and_preserved`'s shape (mutate → re-run → assert
      survival), and **write your own** distinctive glossary content before asserting
      preservation — asserting against content the fixture inherited from the repo proves the
      repo's state, not the code's.
- [ ] **T12** (R10) — `tests/test_init_drift_guard.sh`, the R4 project-owned fixture (~123):
      add an uncommitted `.harness/specs/glossary.md` edit to the perturbation and keep the
      existing harness-owned positive control on the same tree. Add the direct assertion too:
      `tools/harness-owned-paths.sh body <harness-dir>` prints
      `:(exclude).harness/specs/glossary.md` — that covers both consumers at the source rather
      than arguing for the second one.
- [ ] **T13** (R11) — `tests/test_install.sh`: add `test_glossary_ownership_documented` —
      **placement, not presence**: extract each span first (the `manifest.txt` HARNESS-OWNED
      and PROJECT-OWNED blocks, the installer header's two ownership bullets, the
      `docs/INSTALL.md` table rows, the `docs/UMBRELLA.md` Prose row), then assert inside it.
      Companion ablation assertion: grep both places a config key can be introduced — the
      shipped `harness.config.yaml` and `migrate_config`'s default table in
      `harness-install.sh` — for `glossar` **at any indentation**, and require zero key lines.
      (A column-0-only key sweep would miss the nested shape this config actually uses, and a
      fresh install `cp`s the seed verbatim, so comparing seed to install compares a file with
      itself.)
- [ ] **T14** (R12) — Create `tests/fixtures/glossary-shipped.md` as a byte copy of
      `specs/glossary.md` (`cp`, not retyped) and add `test_shipped_glossary_bytes_pinned` to
      `tests/test_install.sh` with the consequence-and-follow-up failure message from
      `.plan.md` §5. Add the two static greps of the suite file itself (for `.glossary-seed`
      and for the consequence phrase) — otherwise half of R12 can never fail.

## E. Release and hand-off

- [ ] **T15** — Bump `VERSION` (MINOR recommended — an installed path changes ownership class)
      and append a matching `CHANGELOG.md` entry describing the reclassification, the
      pristine/preserve rule, the thin-child decision (D1) and the migration story. Do not edit
      historical entries.
- [ ] **T16** — `tools/run-tests.sh test_umbrella.sh` alone, then `tools/run-tests.sh` for the
      full set, then `./init.sh`. A suite that fails only in the wide run gets re-run alone
      before it is debugged.
- [ ] **T17** — Write `progress/E30-F01/builder.md`: what changed, every fixture you re-pointed
      and why that entry, anything in `.plan.md` you deviated from, and the hand-verified
      migration smoke from T4.
