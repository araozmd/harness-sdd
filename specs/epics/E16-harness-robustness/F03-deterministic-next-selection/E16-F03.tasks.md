# Deterministic next-task selection — Tasks

> Atomic, sequential steps. The Builder works these top to bottom. Every task
> names the requirements it satisfies and follows test-driven development.

- [x] **T1** (R1, R2, R12, R13) — Create the initial
  `tests/test_next_task.sh` fixtures for strict CLI parsing, the fixed JSON
  envelope, source/installed path resolution, malformed input, stderr/exit
  behavior, and byte-identical repeated output; run them and confirm they fail
  because `tools/next-task.mjs` does not exist.
- [x] **T2** (R1, R2, R12, R13) — Create executable
  `tools/next-task.mjs` with the closed argument parser, script-relative default
  paths, minimal relevant YAML/manifest readers, structural validation, and
  single-write result/error envelope needed to pass T1.
- [x] **T3** (R3–R5, R7, R8, R12) — Add failing table-driven feature fixtures
  for every status/sdd/autonomous/approval combination, draft epics, exact
  dependencies, cycles, missing/cross-kind dependencies, empty/all-done boards,
  canonical numeric ordering, reordered equivalents, numeric-equivalent literal
  tie-breaking, duplicate ids, targeted selection, a done target with unrelated
  actionable work, and empty/all-done precedence over mine identity resolution.
- [x] **T4** (R3–R5, R7, R8, R12, R13) — Implement feature validation,
  canonical ordering, cycle/blocker collection, route selection, targeted
  fail-closed behavior, and exact E16-F01 top-level diagnostic results until T3
  passes.
- [x] **T5** (R6, R7) — Add failing ownership fixtures for feature-over-epic
  precedence, literal match/exclusion, unowned candidates, empty identity,
  successful and failed PATH-stubbed `gh` resolution for both `@me` and `self`,
  bare owner ignoring, no widening, and no owner mutation.
- [x] **T6** (R6, R7) — Implement mine-mode identity resolution and
  post-ordinary-gate owner filtering until T5 passes.
- [x] **T7** (R9–R12) — Add failing umbrella fixtures for inert absent/empty
  manifests, present manifest repo resolution, missing repo errors, slice
  topological order, reordered/numeric-equivalent ids, each slice status,
  `done+unmerged` observation priority, failed halt, all-merged integration,
  missing/non-done/failed/done-but-unmerged dependencies, cycles, and selected
  parent-only diagnostic scope.
- [x] **T8** (R9–R12) — Implement active-umbrella detection, manifest repo-key
  validation, deterministic slice routing, merge/failure/integration outcomes,
  and parent-scoped no-result diagnostics until T7 passes.
- [x] **T9** (R3–R14, R17) — Build an independent table-driven prose-contract
  oracle in `tests/test_next_task.sh` and compare every documented feature and
  slice scenario to the selector; snapshot the TaskStore, config, and manifest
  before/after success, blocked, halted, and error invocations and require
  byte-identical files.
- [x] **T10** (R14, R17) — Update `agents/orchestrator.md`,
  `.claude/commands/sdd-next.md`, `store/local.md`, `docs/WORKFLOW.md`, and
  `README.md` so successful selector output is authoritative, the prose is
  explicitly the differential oracle, and absent/failing Node uses a reported
  prose fallback without policy or write-ownership changes; pin positional
  `/sdd-next E##-F##` translation to selector `--feature E##-F##`.
- [x] **T11** (R15) — Update `harness-install.sh` and
  `tests/test_install.sh` to install `tools/next-task.mjs` verbatim and
  executable, exercise installed-layout defaults, and avoid exact future VERSION
  assertions; append `tests/test_next_task.sh` to `verification.test_command`.
- [x] **T12** (R16) — Add reordered-equivalence loops and a timed fixture of at
  least 2,000 acyclic features, requiring successful selection in under five
  seconds on Node.js 18 or newer without fragile exact-duration assertions.
- [x] **T13** (R15, R17) — Apply the compatible MINOR `VERSION` bump and
  `CHANGELOG.md` entry, then assert the schema, status enum, state board,
  E16-F01 Python helper, and `init.sh` behavior remain unchanged.
- [x] **T14** (R1–R17) — Run `sh tests/test_next_task.sh`,
  `sh tests/test_dependency_diagnostics.sh`,
  `sh tests/test_ownership.sh`, `sh tests/test_umbrella.sh`,
  `sh tests/test_install.sh`, the full configured verification command, and
  `./init.sh`; record exact green results in the Builder handoff.
