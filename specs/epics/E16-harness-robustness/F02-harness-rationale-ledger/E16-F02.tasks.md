# Harness rationale and deletion ledger — Tasks

> Atomic, sequential steps. The Builder follows documentation-first TDD and
> works only in the files authorized by the technical plan.

- [x] **T1** (R1–R8, R12–R13) — Create
  `tests/test_rationale_docs.sh` with failing source-layout checks for the
  required rationale sections, the exact table headers, all eight compensation
  labels, all thirteen durable labels, classification/evidence rules,
  descriptive primary-source links, ADR-0001/E16-F03 routing language, and
  text-only constraints; run it and confirm failure because
  `docs/RATIONALE.md` is absent.
- [x] **T2** (R1–R8, R12–R13) — Create `docs/RATIONALE.md` in the five-section
  order from the plan, including the complete 21-row human-first ledger and
  mechanism-specific evidence criteria; make the focused source assertions
  green without adding scripts, embedded images, Mermaid, or machine-readable
  policy.
- [x] **T3** (R8) — Audit each externally derived sentence against the five
  primary publisher links in the plan; retain setting/model/study limits,
  remove consensus or universal-causality language, and keep all source prose
  paraphrased and brief.
- [x] **T4** (R9–R10) — Add failing navigation assertions, then update
  `README.md`, the concise docs map in `AGENTS.md`, and `docs/HARNESS.md` with
  descriptive `docs/RATIONALE.md` links; add `RATIONALE` to the README layout
  and do not duplicate the ledger.
- [x] **T5** (R11, R15) — Add a failing fresh-install case to
  `tests/test_install.sh` that compares source and installed rationale bytes and
  checks installed navigation pointers; run it, confirming the existing
  `copy docs` path makes the case pass without editing `harness-install.sh`.
- [x] **T6** (R8–R12, R15) — Add offline local-link resolution checks for every
  new relative Markdown link and semantic assertions that each ledger row has a
  layer, purpose, evidence criterion, and repository pointer; avoid network
  fetches and brittle whole-paragraph snapshots.
- [x] **T7** (R14) — Bump `VERSION` exactly one SemVer MINOR step with patch zero
  and add a matching E16-F02 `CHANGELOG.md` entry; make the permanent version
  assertion find the E16-F02 changelog release and permit later versions rather
  than freezing the current value.
- [x] **T8** (R15) — Append `sh tests/test_rationale_docs.sh` to
  `verification.test_command` in `harness.config.yaml` and update only its suite
  comment; preserve every existing suite and command.
- [x] **T9** (R1–R15) — Run `sh -n tests/test_rationale_docs.sh`,
  `sh tests/test_rationale_docs.sh`, `sh tests/test_install.sh`, `./init.sh`,
  and the exact configured full verification command; resolve every failure
  before handoff.
- [x] **T10** (R1–R15) — Audit traceability and the final diff: every R-id has
  its named test; all 21 ledger rows are present; local links resolve; only files
  authorized by the plan plus these four feature specs changed in Builder scope;
  every DO-NOT-TOUCH path remains unchanged by the Builder. Ignore normal
  Orchestrator-owned state/history/telemetry/progress bookkeeping when evaluating
  this Builder scope, and write Builder evidence only in its assigned progress
  directory.

## Mandatory Reviewer behavioral gate
Before approval, the Reviewer shall perform a fresh install into a disposable
directory, compare `docs/RATIONALE.md` and
`.harness/docs/RATIONALE.md` byte-for-byte, and read the installed document as a
cold reader. The review shall record whether the two layers, retain-by-default
rule, complete 21-row inventory, source limits, and prose-routing/ADR boundary
are understandable without source-repository context. The Reviewer shall also
inspect the final diff for R13 and reject any runtime, role-contract, gate,
status-semantics, selection, schema, API, parser, or deletion-automation change.
Normal Orchestrator-owned feature-status/history bookkeeping is permitted and is
not a Builder deliverable.
