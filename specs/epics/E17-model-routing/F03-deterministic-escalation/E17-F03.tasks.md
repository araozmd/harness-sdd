# Deterministic escalation — Task Breakdown

Ordered so each task leaves the tree green. One commit per task.

- [x] **T1 — The rule, executable.**
      `tools/builder-role.sh`: `<complexity> <round> [--backend …] [--config …]` → `builder` |
      `builder-heavy` on stdout, advisories on stderr, exit 0 for any resolvable answer.
      Backend checked **first**; unknown complexity coerced to `standard` **with** an advisory;
      `0` disables BOTH triggers rather than inverting the comparison. Pure — no `progress/`, no spec body,
      no `git`. Covers **R1, R2, R3, R4, R8, R9**.

- [x] **T2 — Config, both copies.**
      `harness.config.yaml` **and** the `migrate_config` heredoc in `harness-install.sh` gain
      the `escalation:` block with `after_rejections: 2`. Missing either makes a fresh target
      and an upgraded one diverge — the defect caught on E17-F02. Covers **R6**.

- [x] **T3 — The tag and who sets it.**
      `specs/_templates/feature.spec.md` gains optional `complexity: standard` with the
      vocabulary in a trailing comment. `agents/architect.md` states when to write `complex`,
      tied to signals the Architect already produces (R-id count vs budget, an ADR the feature
      must honor, a spec that needed splitting) rather than to a feel. Covers **R1**.

- [x] **T4 — The call site.**
      `agents/orchestrator.md`: at the `in-progress` dispatch, call `tools/builder-role.sh`,
      spawn the role it returns, and do not substitute a judgment for its answer. Record the
      role and trigger in the `progress/history.md` line; keep the telemetry `phase` at
      `builder` and add the spawned role as a separate field. Covers **R10, R11**.

- [x] **T5 — Docs.**
      `docs/WORKFLOW.md`: the rule, the knob, `0` as the off-switch, that it is **inert until
      `models.builder-heavy` is configured**, and the `delegate` no-op.

- [x] **T6 — The suite.**
      `tests/test_escalation.sh` with every case from the contract: the truth table with its
      distinct-answers assertion, the n=2 boundary from both sides, the `0` case paired with a
      reachable escalation, the delegate case over inputs proven heavy in the same run, R7's
      silence paired with R2's advisory, and the strip-and-reinstall config migration case.
      Covers **R5, R6, R7** and pins the rest.

- [x] **T7 — Release.**
      `VERSION` MINOR bump, `CHANGELOG.md` entry. Run `sh tools/run-tests.sh`,
      `dash tests/test_escalation.sh`, `sh tools/change-size.sh`, and the **full mutation
      battery** from the test contract — committing first — **before** opening the PR.

## Verification gate

`sh tools/run-tests.sh` (33 suites with this one) + `sh tools/change-size.sh`. Suites must
also pass under `dash`, since CI `/bin/sh` is dash. `dash tests/test_pr_loop.sh` fails at
`R22 … (got 126)` **pre-existing on main**.

## Standing constraint

`tests/test_next_task.sh` runs `git diff --quiet` on `store/tasks.schema.json`,
`tools/task-diagnostics.py` and `init.sh` — the suite fails on any *uncommitted* change to
those. This feature touches none of them (R5 forbids the schema change outright), but commit
before re-running if that ever changes.
