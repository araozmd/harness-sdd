# Worker roster: invocable CLIs as versioned data — Test Contract

> Every R-id maps to a concrete test. The Reviewer fails the feature if any R-id lacks a
> passing test. All cases live in `tests/test_worker_roster.sh` unless noted.
>
> **The suite controls `PATH`.** Presence is the variable under test, so each case builds a
> `mktemp` bin directory holding stub executables for the keys it wants present and sets
> `PATH` to it. A case that inherits the developer's real `PATH` tests that machine, not the
> requirement. Stubs exist to be **found**, never run — see R8.

| R-id | Behavior | Test (file::name) | Type | Status |
|---|---|---|---|---|
| R1 | Where the roster is enabled, the installer writes `.harness/workers.json` | `tests/test_worker_roster.sh::R1_enabled_writes_roster` | integration | ✅ |
| R2 | Not enabled ⇒ no roster file, and no roster artifact beyond the two documented always-present items | `tests/test_worker_roster.sh::R2_absent_key_writes_nothing` | integration | ✅ |
| R3 | Roster present + gate off ⇒ removed and reported | `tests/test_worker_roster.sh::R3_ungated_reclaims_existing` | integration | ✅ |
| R4 | Top-level `schema` is the integer `1` | `tests/test_worker_roster.sh::R4_schema_is_one` | integration | ✅ |
| R5 | Exactly one entry per `AGENT_KEYS` key whose command resolves on `PATH`, selected or not, naming the key and the command — **and carrying no `path` key at all** | `tests/test_worker_roster.sh::R5_one_entry_per_present_cli` | integration | ✅ |
| R5 | An UNSELECTED but installed CLI still gets an entry | `tests/test_worker_roster.sh::R5_unselected_cli_is_still_rostered` | integration | ✅ |
| R6 | A command absent from `PATH` produces no entry | `tests/test_worker_roster.sh::R6_absent_cli_has_no_entry` | integration | ✅ |
| R4, R7 | `capability_vocabulary` **equals** the sorted three-tag set `["harness-selected", "host-detectable", "non-interactive"]`, and every `capabilities` value is one of those three — an extra tag, in the vocabulary or on an entry, fails | `tests/test_worker_roster.sh::R7_capabilities_are_closed` | integration | ✅ |
| R7 | Each tag is present exactly when its evidence is, absent when it is not — `harness-selected` on `SELECTED` membership, `host-detectable` on a `HOST_MARKERS` row, `non-interactive` on a `WORKER_INVOKE` claim | `tests/test_worker_roster.sh::R7_each_tag_tracks_its_evidence` | integration | ✅ |
| R7 | `harness-selected` reports selection, not a successful stamp: a selected front-end whose protected artifact the installer refused to overwrite still carries it | `tests/test_worker_roster.sh::R7_selected_tag_survives_a_refused_write` | integration | ✅ |
| R8 | Presence by `PATH` resolution alone; no rostered CLI is executed | `tests/test_worker_roster.sh::R8_never_executes_a_rostered_cli` | integration | ✅ |
| R9 | Two runs holding all three roster inputs fixed — the gate, the selection, and per-key presence — produce byte-identical rosters; **plus a control that a `PATH` reordering which changes nothing about presence is also byte-identical** | `tests/test_worker_roster.sh::R9_rerun_is_byte_identical` | integration | ✅ |
| R10 | An existing roster is overwritten from freshly detected state | `tests/test_worker_roster.sh::R10_rerun_overwrites_from_fresh_state` | integration | ✅ |
| R11 | The roster path is ignored by `.harness/.gitignore` | `tests/test_worker_roster.sh::R11_roster_is_gitignored` | integration | ✅ |
| R12 | A symlinked roster is left alone, with a warning and no write | `tests/test_worker_roster.sh::R12_symlink_is_refused` | integration | ✅ |
| — | The roster is valid, parseable JSON — loaded with a real parser (not `grep`), on a populated roster **and** on one where no CLI resolved, since the empty `workers[]` and the last-entry separator are where a hand-rolled emitter breaks | `tests/test_worker_roster.sh::roster_is_valid_json` | integration | ✅ |
| R1, R11 | The installer generates the roster wiring + the ignore entry | `tests/test_install.sh` (additive assertions) | integration | ✅ |
| R1, R2 | The seeded and migrated `workers:` config blocks are byte-identical | `tests/test_install.sh` (additive assertion) | integration | ✅ |

## How the harder cases must be written

These are the four that a plausible-looking test would fail to actually verify. Getting them
wrong is how a green suite ships a broken requirement.

- **R8 — a negative requirement needs a test that can catch a violation, AND an anchor.**
  Asserting the installer exits 0 proves nothing. Each stub on the sandbox `PATH` must be an
  executable that **records that it ran** (append its own name to a witness file) and exits
  non-zero. After the install, assert the witness file **does not exist**. A stub that merely
  exits 0 would let an implementation calling `<cli> --version` pass.
  **Anchor the negative assertion to a run that provably reached the detection path.** "The
  witness file does not exist" is equally true when the sandbox `PATH` was misconfigured, the
  gate was off, or the roster code never ran at all — an assertion satisfied by a second path
  proves nothing about the first. So the same case must also assert that
  `.harness/workers.json` exists and contains the stubbed keys.
- **R9 — byte-identity, not equivalence, and only across the three roster inputs.** `cmp -s
  run1.json run2.json`, not a field-by-field comparison. Ordering bugs (`AGENT_KEYS` order vs
  detection order, unsorted capabilities) are precisely what a semantic comparison hides, and
  R9 exists because of them. The fixture holds all three inputs fixed: same gate value, same
  `--agents`/`HARNESS_AGENTS`, same set of stubbed keys on the sandbox `PATH`.
  **Then add the `PATH`-reordering control, because input (c) is now presence-only.** With the
  `path` field dropped, *which* `PATH` component wins a lookup is no longer an input (spec →
  R9), so a third run over the **same stub directories in a different `PATH` order** — build
  two bin dirs that both hold a `claude` stub, run once with `PATH="$d1:$d2"` and once with
  `PATH="$d2:$d1"` — must also be byte-identical. Under the earlier resolved-path contract that
  run was *exempt*; it is now bound, and this control is the direct evidence the dependence is
  gone. It is a regression guard, not a discriminator: a correct implementation satisfies it by
  construction because it records nothing derived from the winning component. Say so in a
  comment, so a later reader does not mistake it for proof the emitter works.
  Conversely, do not assert identity across a **changed** input: an entry whose
  `harness-selected` tag moved when `--agents` moved, or which appeared/vanished when a stub
  was added or removed, is R7/R5/R6 working, and those cases already own the assertions.
- **R10 — the fixture's first roster must be DISTINGUISHABLE from a correct one.** Seed a
  roster containing a key whose command is **not** on the sandbox `PATH`, then re-run with the
  gate on and assert that key is gone. Seeding a roster that already matches the expected
  output makes the assertion pass whether or not anything was overwritten, which proves
  nothing.
- **R2 — enumerate the permitted deltas; do not assert "nothing changed".** The gate-off
  target is **not** byte-identical to a pre-feature target and cannot be: the installer seeds
  the documented `workers.roster: false` key, and `_ignores` is unconditional so the
  `.gitignore` line appears too (which is why R11 is ubiquitous). A test that asserts "no
  other installer output changed" therefore **fails a correct implementation**, and the Builder
  would resolve that by weakening the test or deleting the config key — one defeats the test,
  the other defeats R1. So: install with the gate off, and assert no `workers.json`, plus the
  two named deltas present, plus nothing else added.

Five further traps, from the same family — two were added after the doc-critic pass found that
nothing in the suite varied the selection or the capability evidence, and the entry-shape one
after the approval gate dropped the `path` field, which put the *shape* of an entry under the
same "an assertion satisfied by more than one implementation proves nothing" pressure:

- **R3 vs R12 must be independently reachable.** The symlink guard runs *before* the gate, so
  a symlinked roster with the gate off must be **left in place**, not reclaimed. Add that as a
  control inside `R12_symlink_is_refused` — otherwise an implementation that checks the gate
  first passes both tests while deleting a symlink it should never touch.
- **R6 needs a control that fires.** Pair "key absent from `PATH` ⇒ no entry" with the same
  fixture plus that one stub added, asserting the entry appears. Silence on a fixture that
  could never have produced an entry proves nothing — the discipline
  `tests/test_board_spec_consistency.sh` already applies to every rule expected to stay quiet.
- **The entry shape must be asserted as an EQUALITY, because "no `path`" is a decision a
  Builder can undo by helpfully adding one.** R5 says an entry records no filesystem path
  (spec → *Recorded decision 4*), and a test that only checks `key` and `command` are *present*
  passes an implementation that also emits `path`, `mtime` or anything else. So
  `R5_one_entry_per_present_cli` must parse the roster and assert each entry's key set **equals**
  `{"key", "command", "capabilities"}` — sorted, compared as a set, not a substring check.
  This is the same defect one level down from `R7_capabilities_are_closed`'s vocabulary
  equality, and it is the assertion that keeps the six review findings the decision closed from
  walking back in through the implementation. Unlike the vocabulary half of that case, it is
  **not** a trivially-passing guard: `path` was in this spec until the gate removed it, so an
  emitter that still carries it is a live possibility, not a hypothetical.
- **R5's selection case is the only thing standing between this feature and a silent bug.**
  `write_escalation_arming` filters its `AGENT_KEYS` loop with `agent_selected`; the roster
  must not, and a roster that wrongly copies that filter is **byte-identical to a correct one
  on any machine where everything is selected**. So `R5_unselected_cli_is_still_rostered` must
  install with a narrowed selection (e.g. `--agents=claude`) while stubs for at least two
  other keys sit on the sandbox `PATH`, and assert those keys still have entries — carrying
  `host-detectable`/`non-interactive` as earned, but **not** `harness-selected`. Without this
  case nothing in the suite varies the selection, and the bug ships.
- **R7 must test presence AND absence per tag.** The closure check
  (`R7_capabilities_are_closed`) is satisfied by an implementation that emits an empty
  vocabulary and empty capability lists everywhere, so on its own it verifies nothing about
  the derivation rules — which is where all the feature's real content lives.
  `R7_each_tag_tracks_its_evidence` pairs each tag with a key that has its evidence and a key
  that does not: `host-detectable` on a key with a `HOST_MARKERS` row vs `gemini` (which has
  none), `non-interactive` on `codex` vs `gemini`, `harness-selected` on a selected vs an
  unselected key. `non-interactive` is the one genuinely new fact the `WORKER_INVOKE` table
  exists to record, and nothing else in the suite asserts it.
  **The vocabulary itself needs an equality assertion — the same defect one level up.**
  Pairing closure with the per-tag evidence cases closed the empty-vocabulary hole but left the
  *set* unpinned: an emitter that invents a fourth tag, declares it in `capability_vocabulary`
  and stamps it on every entry passes the closure check (its capabilities are still a subset of
  what it declared) **and** every evidence check (those constrain only the three tags they
  name), while silently changing the format `schema: 1` promises. So
  `R7_capabilities_are_closed` must assert **equality** with the sorted literal
  `["harness-selected", "host-detectable", "non-interactive"]`, and must reject any entry
  capability outside those three — outside the *literal*, not outside whatever the file happens
  to declare, which is the comparison a self-consistent emitter satisfies for free. R4 carries
  the matching requirement (the vocabulary is fixed for `schema: 1`), deliberately: a test
  asserting a property no requirement states is how a Builder ends up "fixing" the test.
  *Honesty note — the vocabulary half of that case is a trivially-passing regression guard.*
  The vocabulary is a hardcoded literal in the emitter (plan → data model), so comparing it
  against the same literal here cannot fail on any implementation that leaves that constant
  alone; it discriminates nothing among plausible derivation-rule implementations the way the
  evidence cases do. It is kept because editing that constant is precisely the silent format
  change R4 forbids, and it should be read as pinning the contract, not as evidence the emitter
  works. The per-entry half is **not** in that class: it fails on any emitter that stamps a tag
  outside the three, declared or not.
  **`harness-selected` tracks the selection, and `R7_selected_tag_survives_a_refused_write`
  must pin that it tracks NOTHING else.** Install with a selection that includes `opencode`,
  having first placed a hand-edited
  `opencode.json` in the target so the installer refuses to overwrite it (the documented
  "left in place with a warning" path), and assert the entry **still** carries
  `harness-selected`. The tag's evidence is membership in `SELECTED`, so a refused write must
  not change it — and an implementation that quietly reached for a write outcome instead
  (making the tag mean `harness-stamped` by another name) fails here.

## Behavioral / end-to-end checks

- Install into a scratch target with `workers.roster: true` and a sandbox `PATH` holding
  stubs for `claude` and `agy` only. Confirm `.harness/workers.json` parses, has `schema: 1`,
  holds exactly two entries in `AGENT_KEYS` order, `gemini` is absent, and `claude` carries
  `host-detectable` while a key with no `HOST_MARKERS` row does not.
- Re-run that install with `--agents=claude`. Confirm `antigravity` still has an entry (it is
  installed on this machine), and that its capabilities lost `harness-selected` while keeping
  what it earned. This is the umbrella/selection distinction the whole design turns on.
- Flip `workers.roster` back to `false`, re-run, and confirm the file is gone and the
  reclamation was reported.
- Confirm `git status` in that target is clean — the roster must be ignored, not untracked
  noise (R11).

## Non-functional checks

- `./init.sh` — green (MANDATORY gate).
- `sh tools/run-tests.sh` — all suites green, including the new one.
- `lint_command` / `typecheck_command` are empty in this repo; nothing to run.
